---
id: TASK-0190.04.04
title: Verify PostgreSQL signup forms end to end
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-10 15:17'
updated_date: '2026-09-10 16:16'
labels:
  - postgresql
  - migration
dependencies:
  - TASK-0190.04.03
documentation:
  - BACKLOG_WORKFLOW.md
  - server/docs/postgres-core-migration-contracts.md
  - server/docs/postgres-anonymous-event-compatibility.md
parent_task_id: TASK-0190.04
priority: high
type: task
ordinal: 201400
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Add end-to-end coverage for PostgreSQL signup forms across API and browser layers: anonymous and signed-in creation, reads, editing, response mutation, capacity contention, archive, and deletion, using representative migrated fixtures. This is the final verification gate for TASK-0190.04; run the isolated backend suite and the Firefox E2E project against the isolated test stack.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Isolated route tests cover the full signup lifecycle and capacity contention with migrated representative fixtures.
- [x] #2 A Firefox browser spec covers anonymous and signed-in signup flows against the isolated test stack.
- [x] #3 Required frontend and backend checks pass for the signup migration.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Deliverables
1. Route coverage: confirm the dedicated signup route tests exercise the full lifecycle (account and guest create/read/update/rename/delete), capacity contention, block-validation, archive/unarchive, deletion, and canEdit/publicId exposure with representative multi-block capacity fixtures. Add only what is missing; do not duplicate passing coverage.
2. Firefox browser coverage: add e2e/specs/timed-event-signup-postgres-firefox.spec.ts (must match firefox-desktop's ^timed-event-.*firefox\.spec\.ts$) gated on E2E_POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED=true. Cover anonymous and signed-in signup flows on the event page against the isolated test stack, asserting the PostgreSQL rows and reload persistence.
3. Fix any frontend defect the browser spec exposes on the signed-in join path so AC2 can pass, with a unit regression test (the sign-up dialog currently disables submit when authUser is present because canSubmit requires a typed name while the name field is hidden for signed-in users).
4. Run the isolated backend suite and the Firefox E2E project, plus frontend lint/fmt/typecheck/build/test:unit.

Sequence
- Run the existing isolated signup route tests to confirm AC1 evidence.
- Author the Firefox signup spec (anonymous create + signed-in create, capacity, reload persistence, owner read).
- Run the spec; fix the signed-in join submit defect if reproduced and cover it with a unit test.
- Run the full required checks and record evidence.

Risks
- firefox-desktop testMatch requires the timed-event-*.firefox.spec.ts naming.
- Browser signup submission must go through the EVCC/eventVisitorId path; the owner cannot join its own blocks, so the signed-in journey needs a second account.
- The signed-in submit defect is pre-existing (introduced by commit 2222edc5 changing :disabled from !formValid to !canSubmit) and blocks the signed-in browser journey; fix is scoped to restoring signed-in submission.

Final outcome: no additional route tests were needed; existing coverage satisfies AC1. The Firefox spec plus the signed-in canSubmit fix are the new artifacts. The canSubmit defect was reproduced in the browser journey and confirmed by a failing unit test before the fix.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Route coverage (AC1) was already delivered by TASK-0190.04.01-03 and verified here: TestPostgresSignupCreationPersistsBlocksAndReadsCanonicalResponses seeds representative multi-block capacity fixtures with guest and account responses; TestPostgresSignupAccountResponseLifecycle/GuestResponseLifecycle cover create/read/update/rename/delete; TestPostgresSignupLifecycleAndAuthorization covers archive/unarchive/delete and stranger rejection; TestPostgresSignupCapacityAndBlockValidation covers capacity rejection and foreign blocks; postgres.TestSignupCapacityReservationAtomicUnderContention proves atomic admission under 8 concurrent workers.

Added e2e/specs/timed-event-signup-postgres-firefox.spec.ts (firefox-desktop testMatch requires the timed-event-*.firefox.spec.ts name). It is gated on E2E_POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED=true and asserts PostgreSQL rows directly through postgresScalar.

The signed-in browser journey exposed a pre-existing frontend defect in SignUpForSlotDialog: canSubmit required a typed name, but the name and email fields are hidden when authUser is set, so the Join slot button was permanently disabled for signed-in visitors. Fixed canSubmit to permit signed-in submission and added a unit regression test; the new test was confirmed to fail against the old logic before the fix.

No repository Markdown was authored; the Backlog task record is MCP-managed. backlog/backlog.md is tooling-generated. DoD #4 is not applicable.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Verified PostgreSQL signup forms end to end across API and browser layers.

Delivered:
- e2e/specs/timed-event-signup-postgres-firefox.spec.ts adds three Firefox journeys gated on E2E_POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED=true: (1) an anonymous visitor joins a capacity-3 block with name and email, and PostgreSQL stores a canonicalized guest response that survives a reload; (2) a signed-in responder joins a block without typing a name, PostgreSQL stores an account response keyed to the responder user id, the owner read exposes it, and the reload locks further joins; (3) a capacity-1 block admits one guest and shows (1/1) with no join control for a second visitor. Assertions read PostgreSQL rows through the isolated postgres-inspect helper.
- Fixed a pre-existing frontend defect: SignUpForSlotDialog.canSubmit required a typed name, but the name and email fields are hidden for signed-in visitors, so the Join slot button was permanently disabled for them. canSubmit now allows signed-in submission. A unit regression test fails against the old logic and passes with the fix.

Evidence:
- AC1: the isolated server-route-test suite passes for ./...; the focused TestPostgresSignup* tests pass 9/9; postgres.TestSignupCapacityReservationAtomicUnderContention proves atomic admission of exactly the capacity under 8 concurrent writers. The route tests exercise representative multi-block capacity fixtures with account and guest responses.
- AC2: the new Firefox spec passes 3/3 with the flag; the signed-in unit regression test fails without the canSubmit fix and passes with it.
- AC3: backend suite passes; frontend lint, fmt:check, typecheck, build, and 1082 unit tests pass; the existing chromium sign-up-form-event spec passes; e2e lint, fmt:check, and typecheck pass; graphify update . completed.

Risks/follow-ups:
- The canSubmit defect was pre-existing and affected signed-in signup on both stores; it was fixed here because AC2 could not pass otherwise.
- Route fixtures are representative PostgreSQL shapes rather than output of the in-development migration tooling (TASK-0190.06), which owns migrated fixtures.
<!-- SECTION:FINAL_SUMMARY:END -->
