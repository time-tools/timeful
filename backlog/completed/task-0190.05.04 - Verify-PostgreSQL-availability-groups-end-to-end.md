---
id: TASK-0190.05.04
title: Verify PostgreSQL availability groups end to end
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-10 16:20'
updated_date: '2026-09-10 18:05'
labels:
  - postgresql
  - migration
dependencies:
  - TASK-0190.05.01
  - TASK-0190.05.02
  - TASK-0190.05.03
documentation:
  - BACKLOG_WORKFLOW.md
  - server/docs/postgres-core-migration-contracts.md
  - server/docs/postgres-anonymous-event-compatibility.md
parent_task_id: TASK-0190.05
priority: high
type: task
ordinal: 202400
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Verify PostgreSQL availability groups end to end across API and browser layers using representative migrated fixtures: membership, invitations, availability updates, decline and rejoin, calendar-derived availability using a retained integration, and unchanged legacy MongoDB-path group flows. This is the final verification gate for TASK-0190.05; run the isolated backend suite and the Firefox E2E project against the isolated test stack with repo-tracked checks under e2e.

Depends on TASK-0190.05.01 through TASK-0190.05.03. Migrated fixture output is owned by TASK-0190.06; this subtask uses representative PostgreSQL shapes.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Isolated route tests exercise the full group lifecycle and availability paths using representative migrated group fixtures, including membership, invitations, availability updates, decline and rejoin, and calendar-derived availability through a retained integration.
- [x] #2 A Firefox browser spec covers PostgreSQL group membership and availability flows against the isolated test stack.
- [x] #3 Existing MongoDB-path group flows continue to pass.
- [x] #4 Required frontend and backend checks pass for the availability-group migration.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Deliverables
1. AC1 (isolated route tests): Confirm the existing TASK-0190.05.01-03 route tests already exercise the full group lifecycle and availability paths with representative PostgreSQL group fixtures: creation/invitations (postgres_group_test.go), membership add/remove and decline/undecline, reads, lifecycle and authorization, dashboard state, and response mutation with calendar-derived availability through a retained ICS integration (postgres_group_response_test.go). Add a focused integration test only if a genuine gap exists; do not duplicate passing coverage.
2. AC2 (Firefox browser spec): Add e2e/specs/timed-event-group-postgres-firefox.spec.ts matching the firefox-desktop testMatch (timed-event-*firefox.spec.ts) and gated on E2E_POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED=true. Seed a PostgreSQL group through the API with owner and invitee contexts (page.request so the page shares HttpOnly creation cookies) and cover:
   - group membership visibility (invitee pending membership, owner sees members),
   - availability submission through the schedule-overlap UI persisted to PostgreSQL event_responses/manual availability and surviving reload,
   - decline (reject invitation) and rejoin (accept) reflected in event_attendees.declined,
   - owner membership edit through the Edit group dialog reflected in event_attendees.
   Assert persistence with the isolated postgresScalar helper; keep locators user-facing or existing ids/classes.
3. AC3 (legacy MongoDB path): Run the existing legacy group browser journey (timed-event-weekly-firefox.spec.ts group edit test) and the isolated backend suite to confirm MongoDB-path group flows still pass.
4. AC4 (required checks): Run the isolated backend test suite plus frontend lint, fmt:check, typecheck, build, test:unit; e2e lint, fmt:check, typecheck; graphify update.

Sequence
- Mark in progress, run focused isolated backend group tests to confirm AC1/AC3 evidence.
- Author the Firefox group spec, run it in isolation, and fix any frontend defect it exposes with a unit regression test (prior verification pattern).
- Run the full required checks and record objective evidence per acceptance criterion.

Risks / open questions
- InvitationDialog "Accept Invitation" is gated on calendarPermissionGranted, which becomes false when a signed-in account has no calendar accounts (empty /user/calendars map). If this blocks the browser invitation/rejoin flow, seed a calendar account or treat it as a defect to fix with a unit regression test.
- The pre-existing models.Event.MarshalJSON duration-drop means the group manual availability window is not persisted by the live routes (noted in TASK-0190.05.03). Manual availability itself is stored in the response payload; verify the browser flow still round-trips it, and treat any user-visible breakage as a decision point before expanding scope.
- Route fixtures are representative PostgreSQL shapes; migrated fixture tooling remains TASK-0190.06.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Finalized 2026-09-10. Owner-add-member locator fixed by adding data-testid="email-input-combobox" to EmailInput.vue and clicking the .v-field before filling the now-visible input; the added attendee is stored with declined=false, so the spec asserts "false" rather than "unset". All required checks green; see final summary for command evidence.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Verified PostgreSQL availability groups end to end and closed the final TASK-0190.05 gate.

Outcome
- Added repo-tracked e2e/specs/timed-event-group-postgres-firefox.spec.ts (Firefox desktop, gated on E2E_POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED=true) covering invitation accept + availability submit + reload persistence, decline and rejoin, and owner membership edit through the Edit group dialog. All three journeys pass against the isolated test stack.
- Fixed the frontend defects the browser journeys exposed: group pages now render the desktop #desktop-primary-availability-btn (Event.vue v-if regression), userHasResponded honors the server-derived hasResponded before the legacy response-map key check, an empty calendar map counts as calendar permission granted (useEventLoader), InvitationDialog records the accepted PostgreSQL response id, and addAvailability keeps that selection instead of creating a duplicate response. Each fix has a unit regression test.
- Added data-testid="email-input-combobox" to EmailInput.vue and targeted it in the spec (the Vuetify 4 members input is zero-width until focused), and corrected the added-member assertion to the explicit declined=false the route stores.

Evidence
- AC1/AC3 backend: docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test -> all packages ok (timeful/server, /routes, /postgres, /db, /accounts, /scripts/...).
- AC2 browser: E2E_POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED=true npm run test:e2e -- --project=firefox-desktop --workers=1 specs/timed-event-group-postgres-firefox.spec.ts -> 3 passed.
- AC3 legacy browser: npm run test:e2e -- --project=firefox-desktop --workers=1 specs/timed-event-weekly-firefox.spec.ts -g "round-trips group canonical timed fields through the edit flow" -> 1 passed.
- AC4 checks: frontend lint, fmt:check, typecheck, build, test:unit (146 files, 1089 tests) pass; e2e lint, fmt:check, typecheck pass; graphify update . completed; npm run format:markdown:check clean.

Notes
- Migrated fixture tooling remains TASK-0190.06; route coverage uses representative PostgreSQL shapes.
- The pre-existing models.Event.MarshalJSON duration drop does not break the browser manual-availability round trip; manual availability persists in the response payload (manualAvailabilityCount=1 before and after reload).
<!-- SECTION:FINAL_SUMMARY:END -->
