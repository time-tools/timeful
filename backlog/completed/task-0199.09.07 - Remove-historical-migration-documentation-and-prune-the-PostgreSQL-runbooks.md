---
id: TASK-0199.09.07
title: Remove historical migration documentation and prune the PostgreSQL runbooks
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-11 14:02'
updated_date: '2026-09-11 14:29'
labels:
  - postgresql
  - migration
dependencies:
  - TASK-0199.09.06
references:
  - server/docs/postgres-core-migration-contracts.md
  - server/docs/postgres-core-migration-runbook.md
  - server/docs/postgres-retained-data-contracts.md
  - server/docs/postgres-anonymous-event-compatibility.md
  - docs/postgres-staging-rollout.md
  - DEPLOYMENT.md
  - docs/environments.md
  - docs/design/architecture/adr/ADR-016.md
modified_files:
  - server/docs/postgres-core-migration-contracts.md
  - server/docs/postgres-core-migration-runbook.md
  - server/docs/postgres-retained-data-contracts.md
  - server/docs/postgres-anonymous-event-compatibility.md
  - server/docs/postgres-data-boundaries.md
  - docs/postgres-staging-rollout.md
  - docs/postgres-operations.md
  - DEPLOYMENT.md
  - docs/environments.md
  - docs/design/architecture/adr/ADR-016.md
parent_task_id: TASK-0199.09
priority: medium
type: docs
ordinal: 237000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Remove the executed-migration documentation after TASK-0199.09.06 verifies the PostgreSQL-only system, while preserving every durable behavior contract and inbound link. This is a documentation-only change; no runtime code, configuration, tests, or generated artifacts.

Scope:
- Publish a durable successor contract under server/docs/ carrying the still-authoritative sections of server/docs/postgres-retained-data-contracts.md: the single-authoritative-store boundary, calendar account and sub-calendar fresh identities, OTP one-active-per-email hashed storage, daily-log identity and idempotent membership, the AES-256-GCM credential encryption boundary, and credential non-exposure.
- Keep server/docs/postgres-anonymous-event-compatibility.md as the active API contract and repoint its retained-data references to the successor.
- Delete server/docs/postgres-core-migration-contracts.md, server/docs/postgres-core-migration-runbook.md, and server/docs/postgres-retained-data-contracts.md.
- Rewrite docs/postgres-staging-rollout.md down to the ongoing PostgreSQL procedures: backup/restore with the backup-role grant, retention cleanup, and deployed-Mongo decommission; remove the scope, cutover, backfill, validation-gate, monitoring, rollback-history, rehearsal, and retirement-history sections; rename it to reflect its ongoing scope and update the links in DEPLOYMENT.md and docs/environments.md.
- Update ADR-016's link to the successor contract; leave ADR decisions and Backlog and handoff history untouched.

Gated on TASK-0199.09.06: do not start until the PostgreSQL-only verification is recorded, because the retirement record must stay intact until then.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The three migration documents are removed, and a successor contract under server/docs/ preserves the durable AES-256-GCM credential boundary, single-authority/fresh-identity rules, and OTP one-way-hash rules.
- [x] #2 server/docs/postgres-anonymous-event-compatibility.md remains the active API contract and links only to documents that exist.
- [x] #3 The staging rollout document or its renamed successor documents only ongoing backup/restore, backup-role grant, retention cleanup, and deployed-Mongo decommission procedures, and the links from DEPLOYMENT.md and docs/environments.md resolve.
- [x] #4 No tracked Markdown links to a removed path; searching for the removed filenames returns only Backlog task and handoff history.
- [x] #5 Changed Markdown passes npm run format:markdown and npm run format:markdown:check, and graphify update . refreshes the knowledge graph.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
## Implementation Plan

Gated on TASK-0199.09.06; do not start before it is Done.

1. Extract the durable sections of `server/docs/postgres-retained-data-contracts.md` into a new `server/docs/postgres-data-boundaries.md`: one authoritative store per record kind, calendar-account and sub-calendar fresh identities, OTP one-active-per-email hashed storage, daily-log identity and idempotent membership, the AES-256-GCM envelope with key/nonce rules and the encrypted-field list, fail-closed decryption, and credential non-exposure. Drop the MongoDB field inventory, fixtures, backfill, cutover, and removal sequencing.
2. Update `server/docs/postgres-anonymous-event-compatibility.md` to link to the successor contract and remove references to the deleted migration contracts.
3. Delete `server/docs/postgres-core-migration-contracts.md`, `server/docs/postgres-core-migration-runbook.md`, and `server/docs/postgres-retained-data-contracts.md`.
4. Rewrite `docs/postgres-staging-rollout.md` as `docs/postgres-operations.md` (or another scope-accurate name): keep the backup and restore commands, the backup-role grant, retention cleanup, and deployed-Mongo decommission guidance; remove the scope, cutover, backfill, validation-gate, monitoring, rollback-history, rehearsal, and retirement-history sections. Update the inbound links in `DEPLOYMENT.md` and `docs/environments.md`.
5. Repoint the ADR-016 retained-data link to the successor contract; leave ADR decisions and Backlog and handoff history untouched.
6. Verify with `rg` for the removed filenames and dangling links, `npm run lint:markdown`, `npm run format:markdown`, `npm run format:markdown:check`, and `graphify update .`.
7. Record evidence per acceptance criterion, write the final summary, mark this task Done, then finalize the parent TASK-0199.09 once its acceptance criteria are verified.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Implementation notes (2026-09-11)

- Created `server/docs/postgres-data-boundaries.md` as the durable successor contract and deleted the three executed-migration documents. The successor keeps the store-ownership table, fresh-identity and single-authority rules, the OTP one-way-hash rule with ten-minute expiry and five-attempt lockout, the AES-256-GCM envelope rules, fail-closed decryption, credential non-exposure, and reporting reads; it drops the MongoDB field inventory, fixtures, backfill, cutover, and removal sequencing.
- Rewrote `docs/postgres-staging-rollout.md` into `docs/postgres-operations.md` with only the ongoing procedures: backup/restore plus the backup-role grant, retention cleanup, and deployed-Mongo decommission. Removed the cutover scope, source-write coordination, validation gates, monitoring, rollback history, rehearsal, smoke test, and retirement-history sections.
- Repointed `server/docs/postgres-anonymous-event-compatibility.md`, `docs/design/architecture/adr/ADR-016.md`, `DEPLOYMENT.md`, `docs/environments.md`, and `.github/pull_request_template.md`.
- Verification: `rg` for the four removed filenames returns matches only under `backlog/`; a relative-link checker over every changed Markdown file found 0 dangling links; `npm run format:markdown`, `npm run format:markdown:check`, and `npm run lint:markdown` exited 0; `graphify update .` rebuilt the graph (5491 nodes, 9689 edges) with no references to removed paths.
- Purged the two tracked graphify semantic-cache entries whose source file was `docs/postgres-staging-rollout.md`, matching the repository precedent of purging stale cached extractions on source deletion.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
## Summary

Removed the executed-migration documentation and pruned the PostgreSQL runbooks to their ongoing procedures. Documentation-only change: no runtime code, configuration, or tests were modified.

## Changes

- Published the successor `server/docs/postgres-data-boundaries.md`: the single-authoritative-store table, fresh-identity and single-authority rules for calendar accounts/sub-calendars, OTP challenges, and daily logs; the AES-256-GCM envelope with key, nonce, versioned-envelope, encrypted-field, and plaintext-field rules; fail-closed decryption; credential non-exposure; and reporting reads. Dropped the MongoDB field inventory, fixtures, backfill, cutover, and removal sequencing.
- Repointed `server/docs/postgres-anonymous-event-compatibility.md` (still the active API contract) to the successor, and repointed ADR-016 and the PR template.
- Deleted `server/docs/postgres-core-migration-contracts.md`, `server/docs/postgres-core-migration-runbook.md`, `server/docs/postgres-retained-data-contracts.md`, and `docs/postgres-staging-rollout.md`.
- Added `docs/postgres-operations.md` covering only backup/restore with the backup-role grant, retention cleanup (`migration_ledger` and `migration_quarantine`), and deployed-Mongo decommission; updated the inbound links in `DEPLOYMENT.md` and `docs/environments.md`.
- Purged the two tracked graphify semantic-cache entries for the removed rollout doc, matching the repository's purge-on-delete convention (as in commit e2c3ee31).

## Verification per acceptance criterion

- #1: the three migration documents are deleted; the successor carries the AES-256-GCM credential boundary, single-authority/fresh-identity rules, and the OTP salted one-way-hash rule.
- #2: the compatibility contract links to `postgres-data-boundaries.md` and glossary documents that exist; a relative-link check over every changed Markdown file reported 0 dangling links.
- #3: `docs/postgres-operations.md` contains only Backup And Restore (with the `GRANT pg_read_all_data` backup-role grant), Retention Cleanup, and Deployed MongoDB Decommission; the `DEPLOYMENT.md` and `docs/environments.md` links resolve.
- #4: `rg 'postgres-core-migration-contracts|postgres-core-migration-runbook|postgres-retained-data-contracts|postgres-staging-rollout'` matches only under `backlog/` task and handoff history.
- #5: `npm run format:markdown`, `npm run format:markdown:check`, and `npm run lint:markdown` all exited 0; `graphify update .` rebuilt `graph.json`, `graph.html`, and `GRAPH_REPORT.md` (5491 nodes, 9689 edges) with no references to the removed paths.

## Notes

- Unit and E2E tests are exempt for this documentation-only change per the project Definition of Done.
- No commit was made.
<!-- SECTION:FINAL_SUMMARY:END -->
