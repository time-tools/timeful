---
id: TASK-0199.07
title: >-
  Backfill retained calendar documents and verify calendar integration end to
  end
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-10 19:57'
updated_date: '2026-09-11 04:53'
labels:
  - postgresql
  - migration
dependencies:
  - TASK-0199.03
references:
  - TASK-0199.03
  - TASK-0189
  - server/scripts/20260910_mongo_accounts_to_postgres/main.go
documentation:
  - BACKLOG_WORKFLOW.md
  - server/docs/postgres-core-migration-runbook.md
modified_files:
  - server/scripts/20260911_mongo_calendars_to_postgres/reconcile.go
  - server/scripts/20260911_mongo_calendars_to_postgres/main_integration_test.go
parent_task_id: TASK-0199
priority: high
type: task
ordinal: 227000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Provide the resumable backfill and end-to-end verification for moving existing retained calendar integration data into PostgreSQL.

Add migration tooling that reads retained MongoDB `users` calendar documents and preferences, resolves owners through platform_identities.external_user_id, writes PostgreSQL calendar records with encrypted credentials, and records progress in the migration ledger without modifying MongoDB source data.
Add reconciliation for counts, enabled flags, sub-calendars, and preferences, including interruption and rerun coverage.
Verify the connect, refresh, toggle, and remove flows in an isolated stack and a Firefox e2e run.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A resumable, idempotent backfill moves existing retained calendar documents and preferences into PostgreSQL using the migration ledger and platform_identities.external_user_id, leaving MongoDB source documents unmodified.
- [x] #2 Reconciliation verifies account, calendar-account, sub-calendar, and preference counts and values, and re-running after interruption resumes from the first incomplete unit.
- [x] #3 Isolated rehearsal covers representative Google, Outlook, Apple, and ICS accounts, preferences, and a retained account with no PostgreSQL account row yet.
- [x] #4 Firefox e2e verifies connect, token refresh, sub-calendar toggle, and remove flows against the PostgreSQL store.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
The implementation is delivered by the three completed subtasks:

- TASK-0199.07.01: `server/scripts/20260911_mongo_calendars_to_postgres/` resumable backfill, reconciliation, isolated rehearsal (runbook section added).
- TASK-0199.07.02: test-stack ENCRYPTION_KEY, `providerconfig` overrides, `server/mockprovider`, compose.test.yaml calendar-mock service.
- TASK-0199.07.03: `e2e/specs/timed-event-calendar-integration-firefox.spec.ts` connect/refresh/toggle/remove verification against PostgreSQL plus `e2e/helpers/mongo-inspect.ts`.

This finalization session independently re-verifies the parent acceptance criteria and records evidence:

1. Run the backfill rehearsal in the isolated stack: `go test ./scripts/20260911_mongo_calendars_to_postgres/ -count=1` via compose server-route-test.
2. Run the Firefox e2e calendar lifecycle spec from e2e/ with `npm run test:e2e -- --project=firefox-desktop specs/timed-event-calendar-integration-firefox.spec.ts`.
3. Confirm the three subtasks remain Done and map each parent acceptance criterion to their evidence.
4. Write the parent final summary, check the acceptance criteria and DoD, and mark the task Done.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Finalization session accepted the three Done subtasks (07.01 backfill+reconciliation+rehearsal, 07.02 test-stack credentials and mock provider, 07.03 Firefox lifecycle e2e) and independently re-ran their verification in the isolated stack.

The full isolated suite initially failed TestMigrateCalendarsRehearsal: loadQuarantineSummary counted migration_quarantine rows for every record kind, so the events backfill package's quarantine rows (including a second missing-owner-account) leaked into the calendar report. The earlier green run was package-ordering dependent because packages run in parallel. Scoped the query to kind = 'calendar-account' and added a deterministic regression case that seeds an unrelated event-kind quarantine row before reconciliation.

Evidence after the fix: full server-route-test suite all packages pass; Firefox e2e 4/4 passed; go build, go vet, gofmt, and npm run format:markdown:check clean.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Finalized TASK-0199.07 by independently re-verifying the retained calendar backfill and the PostgreSQL calendar end-to-end flows delivered by its three completed subtasks, and by fixing a reconciliation scope bug the full suite exposed.

Acceptance criteria evidence:
- AC1: TASK-0199.07.01 delivers the resumable, idempotent backfill at server/scripts/20260911_mongo_calendars_to_postgres/ using migration_ledger and platform_identities.external_user_id; the isolated rehearsal asserts migrated counts, idempotent replay (migrated=0 skipped=6), MongoDB source immutability, and Apple AES-CFB to GCM re-encryption.
- AC2: Reconciliation verifies unit, calendar_accounts, sub_calendars, credential_rows, and preferences counts, values (explicit-false vs absent enabled, credentials round-trip), and the v1: GCM envelope; the --limit 1 rehearsal proves resume from the first incomplete unit.
- AC3: The rehearsal fixtures cover Google, Outlook, Apple, ICS, preferences, a two-connection mixed-case legacy key account, and a retained owner with no PostgreSQL account row that quarantines as missing-owner-account.
- AC4: TASK-0199.07.03 adds e2e/specs/timed-event-calendar-integration-firefox.spec.ts; this session re-ran it in the isolated stack and all four tests passed (connect, refresh, sub-calendar toggle, remove) with assertions against the PostgreSQL calendar tables and no live provider calls.

Fix made during finalization:
- server/scripts/20260911_mongo_calendars_to_postgres/reconcile.go: the quarantine summary now filters migration_quarantine by kind = 'calendar-account'. The full isolated suite runs the events backfill package first, and its quarantine rows (including a second missing-owner-account) were leaking into the calendar report; the earlier package-only green run was ordering dependent.
- server/scripts/20260911_mongo_calendars_to_postgres/main_integration_test.go: the rehearsal now seeds an unrelated event-kind quarantine row before reconciliation and asserts the report contains exactly the one calendar missing-owner-account quarantine.

Verification:
- docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test go test ./... -count=1 -> all packages pass, including scripts/20260911_mongo_calendars_to_postgres.
- cd e2e && npm run test:e2e -- --project=firefox-desktop specs/timed-event-calendar-integration-firefox.spec.ts -> 4 passed (1.3m).
- cd server && go build ./... and go vet ./... pass; gofmt clean; npm run format:markdown:check passes.

Subtasks TASK-0199.07.01, TASK-0199.07.02, and TASK-0199.07.03 are all Done and provide the implementation record.
<!-- SECTION:FINAL_SUMMARY:END -->
