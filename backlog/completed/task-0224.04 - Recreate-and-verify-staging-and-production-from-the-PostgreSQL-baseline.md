---
id: TASK-0224.04
title: Recreate and verify staging and production from the PostgreSQL baseline
status: Done
assignee: []
created_date: '2026-09-14 15:36'
updated_date: '2026-09-14 17:07'
labels:
  - postgres
  - migration
  - ops
dependencies: []
references:
  - TASK-0224.01
  - TASK-0226
documentation:
  - docs/postgres-operations.md
  - docs/environments.md
parent_task_id: TASK-0224
priority: high
type: task
ordinal: 226500
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Phase 1 close-out follow-up of TASK-0224. TASK-0224.01 established that the development database was recreated from the baseline and that staging and production are handled by recreation rather than in-place cleanup. This task performs that recreation.

Outcome: every deployed database in scope is either recorded as already baseline-created or recreated from the baseline with a verified backup retained, and the observed constraint state is recorded so TASK-0224.02 and TASK-0224.03 can proceed.

Per-environment procedure:
1. Detect schema generation and record the basis: migration_ledger and migration_quarantine presence, goose_db_version containing retired versions below 20260912000000, and presence of baseline signature tables such as platform_identities.
2. For a pre-baseline environment, take a custom-format pg_dump with the backup role and verify the restore into a scratch database by reconciling counts and key digests before any destructive step, per docs/postgres-operations.md.
3. Recreate the database from the baseline and apply all migrations through the current version, then confirm the application stack reaches healthy status against it.
4. Record the FR-119 constraint state and keep the old backup intact until the replacement database is validated.

Constraints: the data reset is accepted for staging and production, so no data migration is in scope; no application code changes are in scope; if an environment turns out to hold data that must survive, stop and report instead of recreating. Coordinate backup-verification tooling with TASK-0226. The retained pre-baseline development evidence is at ~/timeful-backups/prebaseline-dev-20260914/.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 For each in-scope environment, schema generation is detected and recorded with its basis (migration_ledger and migration_quarantine presence, goose_db_version retired versions below 20260912000000, baseline signature tables); environments found baseline-created are recorded and skipped.
- [x] #2 For each pre-baseline environment, a custom-format pg_dump is taken with the least-privilege backup role and its restore into a scratch database is verified by reconciliation before any destructive step, following docs/postgres-operations.md.
- [x] #3 Each pre-baseline environment is recreated from the baseline with all migrations applied through the current version, and the application stack reports healthy against the recreated database.
- [x] #4 The postgres_events_name_length constraint state is recorded for each recreated database, old backups stay intact until the replacement database is validated, and the accepted data reset for staging and production is recorded.
- [x] #5 Commands, outputs, and results are recorded as task evidence; no repository code changes are made beyond documentation corrections if the runbook or environment docs are wrong.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Implementation plan (2026-09-14, session start):

1. Reconnaissance (done): host timeful-cloud-ru runs Compose projects timeful-production (server/postgres/mongo up 13 days, healthy) and timeful-staging (all containers exited 13 days ago). Checkout at ~/timeful is on main at 87ee9990; origin/migrate-from-mongo-to-postgres is at 40de9bb6 and includes the baseline plus migrations through 20260913000002, but the local validation migration 20260914000000 is not pushed.
2. Detect schema generation for each in-scope database (migration_ledger, migration_quarantine, goose_db_version versions below 20260912000000, baseline signature tables) and record the basis.
3. For each pre-baseline database: custom-format pg_dump with the backup role, retained under ~/timeful-backups/ with checksum, then restore into scratch database and reconcile per-table counts and key digests before any destructive step.
4. Recreate each database from the baseline: stop the stack, remove the postgres data volume, deploy the release that includes the migration chain through the current version, and start the stack.
5. Confirm health for staging and production, record postgres_events_name_length convalidated state, and keep old backups until validation.
6. Record commands, outputs, and results as task evidence; correct docs only if the runbook is wrong.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Execution record 2026-09-14 (all times UTC). No repository code changes were made; this task changed only Backlog records.

Host: vm1 (`timeful-cloud-ru`), checkout `~/timeful` clean at 97b052ed on `migrate-from-mongo-to-postgres` (baseline 20260912000000 + 20260913000000/1/2 + 20260914000000).

AC #1 - schema generation detected (read-only):
- staging `timeful-postgres-staging`: PRE-BASELINE. `goose_db_version` = 0, 20260814170000, 20260815100000 (3 applied retired versions below 20260912000000); `migration_ledger` and `migration_quarantine` absent; baseline signature tables absent (`platform_identities`, `accounts`, `calendar_accounts`, `folders`, `event_attendees`, `otp_challenges`, `event_signup_responses`); 3 events / 2 responses.
- production `timeful-postgres-production`: PRE-BASELINE by the same basis; 21 events / 28 responses, all anonymous and ownerless (`owner_external_id` NULL, created 2026-08-01..2026-09-10). No must-survive data found.
No environment was baseline-created, so none was skipped.

AC #2 - verified backups before any destructive step (16:58:54Z-16:58:59Z):
- Re-applied `GRANT pg_read_all_data TO timeful_backup WITH INHERIT TRUE` (runbook step for existing databases; the role existed but had no grant).
- staging dump 13,200 bytes, sha256 `bcaa6a9cb33ff1289bad031e99c7bd812c21fb37a8fdb46ec6cbdb3f3d31fba3`; scratch restore reconciliation goose_db_version 3/3, postgres_events 3/3, postgres_event_responses 2/2, counts and md5 row-content digests all MATCH.
- production dump 22,494 bytes, sha256 `03021979367b28fee395bf9b03786aa2b6f58545e321cf8c525227e61511a2d8`; reconciliation goose_db_version 3/3, postgres_events 21/21, postgres_event_responses 28/28 all MATCH.
- `pg_restore --list` validated both archives (38 TOC entries each). Dumps, `.sha256`, `*-toc.txt`, `*-reconciliation.txt`, and `PROVENANCE.txt` retained under `~/timeful-backups/prebaseline-staging-20260914/` and `~/timeful-backups/prebaseline-production-20260914/`.

AC #3 - recreation from the baseline:
- staging: `compose down`, removed `timeful-staging-postgres-data`, `up -d --build` exit 0. Migration log 17:04:11Z: baseline 56.24ms, 20260913000000 7.63ms, 20260913000001 9.91ms, 20260913000002 1.17ms, 20260914000000 1.37ms, `goose: successfully migrated database to version: 20260914000000`. `postgres-migrate` and `frontend-artifacts` Exited (0); server Up (healthy). Post-recreate verification: 23 baseline tables, no `migration_ledger`/`migration_quarantine`, all signature table counts 0.
- production: `compose down`, removed `timeful-production-postgres-data`, `up -d --build` exit 0. Migration log 17:05:40Z applied the same chain; server Up (healthy). Post-recreate verification identical to staging.
- staging was then stopped with `compose down` at the user's request because the host (3.8 GiB) cannot hold both stacks; its postgres data volume retains the verified baseline database and can be restarted with `up -d`.
- application health: production in-container `http://localhost:3005/api/health` -> `{"status":"ok"}`; public `https://timeful.fun/api/health` -> HTTP 200 and `https://timeful.fun/` -> HTTP 200 (17:05:58Z). Staging server reported Up (healthy) against its recreated database before the requested shutdown.

AC #4 - constraint state and retention:
- `SELECT convalidated FROM pg_constraint WHERE conrelid = 'postgres_events'::regclass AND conname = 'postgres_events_name_length'` -> `t` on both recreated databases (chain ends at 20260914000000). Before recreation both databases predated the constraint entirely.
- `timeful_backup` has `pg_read_all_data` with `inherit_option = t` on both fresh clusters.
- Retained pre-baseline dumps re-checked with `sha256sum -c` -> OK; they stay intact.
- Accepted data reset recorded: staging lost 3 events / 2 responses and production lost 21 anonymous events / 28 guest responses; no account-bound rows existed.

AC #5 - evidence and repository:
- Command outputs retained at `~/timeful-backups/task-0224.04-evidence-20260914/` (detect, roles, pre-recreate data, backup-verify, both post-recreate verifications, integrity check, EVIDENCE.txt) and in the session logs under `/tmp/opencode/0224-04/`.
- No repository code changes; `docs/postgres-operations.md` and `docs/environments.md` matched the executed procedure, so no documentation correction was needed.
- `npm run format:markdown:check` passes; only Backlog-managed Markdown changed, which the repository formatter excludes.

Remaining operational note: the retired `timeful-production-mongo-1` container is still running as an orphan (RestartCount=8, restarted during the earlier memory pressure) and the old `timeful-support-mail-stalwart-1` remains stopped; neither is part of the recreated stack and both are out of this task's scope.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
created: 2026-09-14 16:58
---
Recon evidence and schema-generation detection (2026-09-14, execution session; host vm1 alias timeful-cloud-ru).

Detection basis, read-only (`/tmp/opencode/0224-04/detect.out`):
- staging (`timeful-postgres-staging`): PRE-BASELINE. `goose_db_version` = 0, 20260814170000, 20260815100000 (3 applied versions below 20260912000000); `migration_ledger` and `migration_quarantine` absent; baseline signature tables absent (`platform_identities`, `accounts`, `calendar_accounts`, `folders`, `event_attendees`, `otp_challenges`, `event_signup_responses`); `postgres_events` 3 rows, `postgres_event_responses` 2 rows.
- production (`timeful-postgres-production`): PRE-BASELINE. Same retired goose versions; `migration_ledger`/`migration_quarantine` absent; baseline signature tables absent; `postgres_events` 21 rows (all `specific_dates`, all `owner_external_id` NULL, created 2026-08-01..2026-09-10), `postgres_event_responses` 28 rows (16 distinct events, 28 guests, 0 accounts).

Data-safety review: neither database holds account-bound or post-baseline rows; production rows are anonymous pre-account events including postgres smoke-test records. No must-survive data found; the accepted staging/production reset in this task applies.

Host readiness: `~/timeful` is clean at 97b052ed on `migrate-from-mongo-to-postgres` with the full chain (20260912000000 baseline + 20260913000000/1/2 + 20260914000000); `.env.staging` and `.env.production` present. `timeful_backup` role exists in both clusters but lacks `pg_read_all_data` and is NOINHERIT, so the runbook-prescribed `GRANT pg_read_all_data ... WITH INHERIT TRUE` is re-applied before each dump.
---

created: 2026-09-14 17:07
---
Execution complete (2026-09-14T17:08Z). Both deployed databases were pre-baseline and were recreated from the baseline after verified backups.

- Detection: both pre-baseline (retired goose 20260814170000/20260815100000, no migration_ledger/migration_quarantine, no baseline signature tables).
- Backups: custom-format pg_dump with timeful_backup (pg_read_all_data re-granted first); staging sha256 bcaa6a9c..., production sha256 03021979...; scratch restores reconciled by counts and md5 row-content digests, all MATCH.
- Recreation: fresh postgres volumes, `up -d --build`; goose applied 20260912000000 through 20260914000000 on both; postgres-migrate and frontend-artifacts exited 0; servers healthy.
- Post-recreate state: full 23-table baseline, no retired tooling tables, all signature counts 0, `postgres_events_name_length` convalidated = true, `timeful_backup` granted pg_read_all_data.
- Application health: production `{"status":"ok"}` in-container and HTTP 200 on https://timeful.fun/api/health and https://timeful.fun/; staging server was Up (healthy) before the requested shutdown that frees memory for production.
- Retention: pre-baseline dumps and evidence stay under ~/timeful-backups/ (sha256 re-verified OK). No repository code or documentation changes were needed.
---
<!-- COMMENTS:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Recreated and verified staging and production from the PostgreSQL baseline on 2026-09-14.

Both deployed databases were detected pre-baseline (goose_db_version 20260814170000 and 20260815100000, migration_ledger and migration_quarantine absent, baseline signature tables absent), so both were recreated. Before any destructive step, each was dumped with the least-privilege timeful_backup role (pg_read_all_data re-granted per the runbook) and the dump was restored into a scratch database and reconciled: staging counts 3/3/2 and production 3/3/21/28 across goose_db_version/postgres_events/postgres_event_responses all matched, with identical md5 row-content digests.

Each environment then got a fresh postgres volume and `compose up -d --build`; goose applied 20260912000000 through 20260914000000, postgres-migrate and frontend-artifacts exited 0, and the servers reached healthy. Post-recreate verification shows the full 23-table baseline schema, no retired tooling tables, zero rows in all signature tables, `postgres_events_name_length` convalidated = true, and the backup role granted. Production returns {"status":"ok"} from /api/health and HTTP 200 from https://timeful.fun/ and https://timeful.fun/api/health; staging was verified healthy and then stopped with `compose down` per the host memory constraint (its verified data volume is retained). Pre-baseline dumps, checksums, reconciliation, provenance, and run evidence remain under ~/timeful-backups/ and re-verify OK.

No repository code or documentation changes were needed; TASK-0224.02's deployed-database evidence is now available because no database in scope is left with an unvalidated constraint, and TASK-0224.03 can rely on recreated databases that start from the baseline.
<!-- SECTION:FINAL_SUMMARY:END -->
