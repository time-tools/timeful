---
id: TASK-0199.07.02
title: >-
  Add isolated test-stack calendar credential and provider support for
  end-to-end verification
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-10 21:09'
updated_date: '2026-09-10 21:31'
labels:
  - e2e
  - postgresql
milestone: >-
  Retire MongoDB after moving retained integration and supporting data to
  PostgreSQL
dependencies:
  - TASK-0199.03
references:
  - compose.test.yaml
  - e2e/.env.test
  - server/services/auth/auth.go
  - server/services/calendar/types.go
  - server/postgres/credentials.go
documentation:
  - e2e/AGENTS.md
  - docs/environments.md
modified_files:
  - .env.test.example
  - compose.test.yaml
  - docs/environments.md
  - e2e/AGENTS.md
  - server/mockprovider/main.go
  - server/routes/provider_override_contract_test.go
  - server/services/auth/auth.go
  - server/services/calendar/google_calendar.go
  - server/services/calendar/outlook_calendar.go
  - server/services/microsoftgraph/microsoftgraph.go
  - server/services/providerconfig/providerconfig.go
  - server/services/providerconfig/providerconfig_test.go
parent_task_id: TASK-0199.07
priority: high
type: task
ordinal: 227200
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Enable the isolated test stack to exercise calendar integration flows end to end without live provider access.

The current isolated stack cannot persist calendar credentials because ENCRYPTION_KEY is empty, so every add flow fails GCM encryption. Provide a valid 32-byte ENCRYPTION_KEY to the test server and route-test services.

Token refresh and non-ICS sub-calendar loading call hard-coded external provider endpoints. Add a test-only, env-overridable OAuth token endpoint (defaulting to the real provider URLs) and a mock provider reachable from server-test, so an expired OAuth2 account can refresh and a non-ICS account with sub-calendars can be loaded without real network access.

Keep all changes test-scoped and non-production: production behavior must remain unchanged when the override is unset. Update the environment contract and e2e guidance to document the test-only calendar configuration.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The isolated test stack provides a valid 32-byte ENCRYPTION_KEY so calendar credential writes and reads succeed in server-test and server-route-test.
- [x] #2 An OAuth token endpoint override lets the running test server reach a mock token provider while production keeps the real provider endpoints when the override is unset.
- [x] #3 A non-ICS calendar account with sub-calendars can be loaded and returned by GET /user/calendars in the isolated stack without real provider network access.
- [x] #4 Environment and e2e documentation record the test-only calendar configuration and that it must never be enabled in production.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
- [x] #5 The mock provider runs only in the isolated test stack and is not started by the production compose profile.
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Deliverables
1. `server/services/providerconfig/` (new): test-only, env-overridable provider endpoints with real-provider defaults. Read `TEST_GOOGLE_OAUTH_TOKEN_ENDPOINT`, `TEST_GOOGLE_CALENDAR_API_BASE_URL`, `TEST_MICROSOFT_OAUTH_TOKEN_ENDPOINT`, `TEST_MICROSOFT_GRAPH_API_BASE_URL`; blank/unset falls back to the real URLs so production is unchanged. Unit tests cover defaults, overrides, and whitespace trimming.
2. `server/services/auth/auth.go`: `getTokenEndpointFromCalendarType` resolves through providerconfig.
3. `server/services/calendar/google_calendar.go` and `outlook_calendar.go`, `server/services/microsoftgraph/microsoftgraph.go`: build request URLs from the providerconfig base URLs.
4. `server/mockprovider/main.go` (new): tiny test-only HTTP server serving the Google token/calendarList/events, Microsoft token/graph-me/calendars/calendarview, and `/health` endpoints. Refresh returns a fresh access token so token-refresh persistence is observable.
5. `compose.test.yaml`: add a `calendar-mock` service built from the server `testbase` target, reachable only inside the test network; wire `server-test` env overrides and `depends_on: calendar-mock` healthy; add `ENCRYPTION_KEY` to `server-route-test`. The mock is absent from production compose.
6. `.env.test.example`: set a fixed 32-byte `ENCRYPTION_KEY` for the isolated stack.
7. `docs/environments.md` and `e2e/AGENTS.md`: document the test-only calendar provider configuration, the mock provider, and that it must never be enabled in production.

Verification
- `cd server && go build ./... && go vet ./...`
- `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml config --quiet`
- `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test go test ./... -count=1`
- `npm run format:markdown` for changed Markdown.

Scope notes
- The task reference `e2e/.env.test` is stale; the root `.env.test` (generated from `.env.test.example`) is the isolated-stack env file.
- Firefox e2e calendar journeys stay in TASK-0199.07.03; this task only makes the stack capable.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Verification evidence
- AC1: `.env.test.example` now sets a 32-byte `ENCRYPTION_KEY`; `compose.yaml` inherits it into `server-test` and `compose.test.yaml` declares it for `server-route-test`. `docker compose config` shows the key on both services, and `printenv` inside a live `server-test` returned `timeful-test-encryption-key-32b!`.
- AC1/AC3 live: signed in via OTP, `POST /api/user/add-google-calendar-account` returned 200 (credentials encrypted and persisted), and `GET /api/user/calendars` returned `{"calendar-mock@example.invalid_google":{"calendarEvents":[]}}`.
- AC2: `server/services/providerconfig` unit tests prove real-provider defaults when unset/blank and honor overrides when set. The route test reaches an httptest token endpoint through the override, and the live stack reached the `calendar-mock` token endpoint.
- AC3: `TestGetUserCalendarsLoadsGoogleSubCalendarsFromTestProviderOverride` loads Google sub-calendars from an httptest provider through the override, persists them in PostgreSQL, and asserts the primary/work enabled flags. `calendarListRequests` proves the mock was reached.
- AC4: `docs/environments.md` lists the TEST_ variables and a "Test-only calendar provider overrides" section; `e2e/AGENTS.md` records the mock provider and that live provider calls and production overrides are forbidden.
- DoD #5: `calendar-mock` appears only in `compose.test.yaml`; the production overlay never defines it.

Commands run
- `cd server && go build ./...` (pass), `go vet ./...` (pass), gofmt clean.
- `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml config --quiet` (pass).
- `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test` -> all packages pass, including the new providerconfig and route override tests.
- `npm run format:markdown` (pass; `format:markdown:check` also passes).

Notes
- The task reference `e2e/.env.test` is stale; the root `.env.test` generated from `.env.test.example` is the isolated-stack env file.
- `calendar-mock` runs `go run ./mockprovider` in the testbase image and uses classic ServeMux routing because the module targets Go 1.20.
- Browser calendar lifecycle e2e remains in TASK-0199.07.03; this task only makes the isolated stack capable.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Enabled the isolated test stack to exercise calendar integration flows without live provider access.

What changed:
- .env.test.example: set a real 32-byte ENCRYPTION_KEY so add-calendar credential writes exercise AES-256-GCM instead of failing closed.
- compose.test.yaml: added ENCRYPTION_KEY to server-route-test; added a test-only `calendar-mock` service (server testbase image, `go run ./mockprovider`); pointed server-test at it with TEST_-prefixed endpoint overrides and a healthy dependency. The mock is defined only in the test overlay.
- server/services/providerconfig (new): resolves Google/Microsoft OAuth token endpoints and calendar API base URLs from TEST_-prefixed env vars, defaulting to the real provider URLs when unset or blank. Unit tests cover defaults, overrides, whitespace, and trailing-slash trimming.
- server/services/auth/auth.go, server/services/calendar/google_calendar.go, server/services/calendar/outlook_calendar.go, server/services/microsoftgraph/microsoftgraph.go: build outbound URLs through providerconfig.
- server/mockprovider (new): test-only HTTP provider serving Google token/calendarList/events, Microsoft token/graph-me/calendars/calendarview, and /health. Refresh returns a new access token.
- server/routes/provider_override_contract_test.go (new): signs in, adds a Google account through the override, loads its sub-calendars from the mock, persists them, and asserts GET /user/calendars succeeds with no live network.
- docs/environments.md and e2e/AGENTS.md: document the test-only provider overrides, the mock provider, and the prohibition on enabling them in production or staging.

Evidence:
- `go build ./...` and `go vet ./...` pass; gofmt clean.
- `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml config --quiet` passes.
- `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test` passes for all packages, including the new providerconfig and route override tests.
- Live isolated stack: server-test healthy with the overrides and 32-byte key; OTP sign-in, add-google-calendar-account (200), and GET /user/calendars returned the mock-backed account `calendar-mock@example.invalid_google`.
- `npm run format:markdown` / `format:markdown:check` pass.

Scope note: browser calendar lifecycle e2e remains in TASK-0199.07.03; this task only makes the stack capable.
<!-- SECTION:FINAL_SUMMARY:END -->
