---
id: TASK-0214.04
title: Add supporting indexes and typed UUID binds
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-12 22:06'
updated_date: '2026-09-13 10:07'
labels: []
dependencies:
  - TASK-0214.03
parent_task_id: TASK-0214
priority: low
type: task
ordinal: 218000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
# Goal

Add the indexes that existing query shapes need and stop defeating indexes with UUID-to-text casts.

# Scope

- Add one goose migration in `server/migrations/` covering the indexes below.
  Keep it compatible with the temp-table migration harness at `server/postgres/migration_helpers_test.go:57-59`: plain `CREATE INDEX`, no `CONCURRENTLY`, and no schema-qualified names.
  - `postgres_event_responses (event_id, created_at, id)` for the `ListResponses` ordering in `server/postgres/repository.go`, and drop the now-redundant `postgres_event_responses_event_id_idx` in the same migration.
  - `event_attendees (event_id, created_at, id)` for the `ListAttendees` ordering in `server/postgres/attendees.go`, and `event_attendees (event_id, lower(email))` for the dashboard membership EXISTS in `server/postgres/dashboard.go`.
  - A `postgres_events` index that serves the analytics day-spine windows in `server/postgres/analytics.go`, with columns and predicate stated explicitly (candidate shapes: `(created_at, creator_posthog_id)`, `(created_at) INCLUDE (creator_posthog_id)`, or a partial index on non-empty `creator_posthog_id`).
    The existing `postgres_events_active_creator_posthog_id_idx` is partial `WHERE NOT is_deleted` and cannot serve these queries because analytics deliberately does not filter `is_deleted`; record whether that now-unused partial index stays or is replaced.
  - `daily_user_log_members (daily_user_log_id, first_seen_position, id)` to match the `ORDER BY ... m.first_seen_position, m.id` in `ListActiveUserDays` and the `MAX(first_seen_position)` append path.
  - Decide and record whether `event_signup_responses (event_visitor_identity_id)` is needed for account-deletion scans; removing the `::text` cast in `deleteAccountAuthority` restores no index because none exists on that column today.
- After TASK-0214.03 lands, re-locate every line reference against the post-TASK-0214.03 code and replace the remaining `::text` casts on UUID columns with typed UUID binds plus validation:
  - `GetEventVisitorIdentity`, `GetEventVisitorCredential`, and `GetResponseByPublicID` in `server/postgres/identities.go` (the last one was missed by the original audit and must be included; it backs client `responseId` on `server/routes/postgres_event_routes.go` and `server/routes/postgres_group.go`).
  - `LockAccessTransfer` in `server/postgres/transfers.go`.
  - `deleteAccountAuthority` in `server/postgres/accounts.go` (typed `uuid[]` bind, type consistency only).
  - `GetSignupResponseByPublicID` and `DeleteSignupResponse` public_id lookups in `server/postgres/signup.go` (TASK-0214.03 removes the block-membership casts).
- Validate opaque client identifiers inside the repository with `models.ParseUUID` before binding, following `validPlatformIdentityID`/`guardPlatformIdentityID`; do not push UUID parsing into routes.
  - Non-canonical identifiers keep returning `pgx.ErrNoRows` at the repository boundary so each caller preserves its existing mapping: 404 for response, signup-response, and transfer lookups; anonymous/no-credential handling for event-visitor public ID and credential lookups.
  - Non-canonical signup block identifiers must keep `ErrSignupBlockNotFound` (400 `signup-block-not-found`), never `pgx.ErrNoRows` or `22P02`.
    Resolve with TASK-0214.03 whether block-ID validation lands there (its constraints require no cast error) or here (its notes defer it); if here, specify the `normalizeSignupBlockIDs` or capacity error path, because that helper currently returns `[]string` only.

# Constraints

- The baseline migration is immutable; add a new migration instead.
- Preserve not-found behavior for non-canonical IDs; do not surface `22P02` errors to routes.
- Do not map non-canonical signup block identifiers to `pgx.ErrNoRows`; their contract is `ErrSignupBlockNotFound`.
- SQL stays in `server/postgres/`.

# Verification

- Seed representative rows (or force plans with `SET enable_seqscan = off`) before `EXPLAIN (ANALYZE, BUFFERS)`; an empty isolated test database yields seq scans and proves nothing.
- Confirm index usage for the analytics, response-listing, attendee-listing, daily-log, and transfer-lock queries.
- Add non-canonical regression tests for every newly typed bind (event-visitor public ID, credential ID, response public ID, signup response public ID, transfer ID) asserting `pgx.ErrNoRows`; keep signup block-ID tests asserting `ErrSignupBlockNotFound`.
- Run the route and repository test suites under `compose.test.yaml`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A goose migration adds postgres_event_responses (event_id, created_at, id) and drops the redundant (event_id) index, event_attendees (event_id, created_at, id) and (event_id, lower(email)), an explicitly shaped postgres_events index for the analytics windows with the partial is_deleted index limitation recorded, and daily_user_log_members (daily_user_log_id, first_seen_position, id).
- [x] #2 UUID comparisons use typed UUID parameters or validated values instead of casting indexed columns to text, covering identities.go GetResponseByPublicID, LockAccessTransfer, and the signup.go public_id lookups.
- [x] #3 Non-canonical UUID inputs keep their current repository-level mapping: pgx.ErrNoRows for event-visitor, credential, response, signup-response, and transfer lookups, and ErrSignupBlockNotFound for signup block identifiers; no 22P02 reaches routes.
- [x] #4 EXPLAIN (ANALYZE, BUFFERS) against representative seeded data (or forced plans) confirms index usage for the analytics, response-listing, attendee-listing, daily-log, and transfer-lock queries.
- [x] #5 Non-canonical regression tests cover every newly typed bind, and the migration stays compatible with the temp-table migration harness.
- [x] #6 The PostgreSQL-backed test suite passes under compose.test.yaml.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
# Implementation plan

## Findings

- Cast sites re-located against post-TASK-0214.03 code: `identities.go` `GetEventVisitorIdentity` (`public_id::text`), `GetEventVisitorCredential` (`id::text`), `GetResponseByPublicID` (`public_id::text`); `transfers.go` `LockAccessTransfer` (`id::text`); `accounts.go` `deleteAccountAuthority` (`event_visitor_identity_id::text`); `signup.go` `GetSignupResponseByPublicID` and `DeleteSignupResponse` (`public_id::text`). `UpdateSignupResponse` already binds `public_id = $1` typed but accepts an unvalidated opaque value.
- Signup block-identity comparisons (`reserveSignupCapacity`, `writeSignupResponseMemberships`, `ReplaceSignupBlocks`) stay text-based. Decision: do not convert them. They are always event-scoped, so `event_id` drives index access; non-canonical client text is a first-class input with distinct `ErrSignupBlockNotFound` (membership) and `pgx.ErrNoRows` (replace) contracts, and TASK-0214.03 deliberately structured them so client text never reaches a uuid cast. AC #2 and AC #5 enumerate only the five opaque IDs for typed binds.
- The migration harness executes each file's Up into temp tables and rewrites `CREATE TABLE` to `CREATE TEMP TABLE`; unqualified `CREATE INDEX`/`DROP INDEX` resolve to pg_temp first, so the new migration stays harness-compatible.
- `postgres_events_active_creator_posthog_id_idx` (`(creator_posthog_id, created_at DESC) WHERE NOT is_deleted`) is referenced by no query; analytics deliberately does not filter `is_deleted` and needs `created_at` leading. Decision: replace it with an explicitly shaped partial index.
- `event_signup_responses` has no index on `event_visitor_identity_id` or `platform_identity_id`, and the account-deletion statement ORs both columns; one new index cannot serve the OR. Decision: add no index; the `accounts.go` change is type consistency only, recorded in the migration comment.

## Steps

1. Add `server/migrations/20260913000001_supporting_indexes.sql`: `postgres_event_responses (event_id, created_at, id)` and drop `postgres_event_responses_event_id_idx`; `event_attendees (event_id, created_at, id)` and `event_attendees (event_id, lower(email))`; replace `postgres_events_active_creator_posthog_id_idx` with `postgres_events (created_at, creator_posthog_id) WHERE creator_posthog_id IS NOT NULL AND creator_posthog_id <> ''`; `daily_user_log_members (daily_user_log_id, first_seen_position, id)`; reversible Down that restores the replaced index.
2. `identities.go`: add a canonical-UUID guard for opaque identifiers; validate event-visitor public ID, credential ID, and response public ID before binding, returning `pgx.ErrNoRows` for non-canonical values, and drop the `::text` casts.
3. `transfers.go`: validate the transfer ID in `LockAccessTransfer` and bind `id` typed.
4. `accounts.go`: bind `visitorIDs` as `$2::uuid[]` in `deleteAccountAuthority`.
5. `signup.go`: validate public IDs in `GetSignupResponseByPublicID` and `DeleteSignupResponse` (returning `pgx.ErrNoRows`), drop their casts, and validate the opaque public ID path of `UpdateSignupResponse` defensively.
6. Tests: index schema test (columns, partial predicate, dropped baseline index) and forced-plan `EXPLAIN (ANALYZE, BUFFERS)` coverage for analytics, response listing, attendee listing, daily-log order/append, and transfer lock; non-canonical regression tests for all five newly typed binds plus the signup public IDs.
7. Run the isolated suite under `compose.test.yaml`, `gofmt`, `go vet`, markdown formatting, and `graphify update .`; record per-criterion evidence and finalize.

1. Add a repository HasNonDeclinedAttendeeEmail EXISTS backed by event_attendees (event_id, lower(email)) and use it in postgresGroupViewerIsInvitee, removing the in-memory EqualFold scan and the now-unused attendee load on the responses route.

2. Replace membership.response_id::text <> $3 with an empty-safe typed predicate in reserveSignupCapacity.

3. Isolate the ANY($2) branch in the accounts deletion test by dropping the visitor FK cascade inside the test transaction, and assert both the direct account response and the account-owned visitor response are removed while another guest's response survives.

4. Correct the migration comment for the signup-response index decision and add the missing <> '' half of the partial-predicate schema assertion.

5. Extract production SQL constants for ListResponses, ListAttendees, ListDashboardEvents, ListActiveUserDays, LockAccessTransfer, and both analytics queries, then plan-test those constants instead of copied SQL.

6. Drop the redundant $2::uuid[] cast in deleteAccountAuthority and keep the type-consistency comment accurate.

7. Run the targeted and full PostgreSQL-backed suites under compose.test.yaml plus gofmt and go vet, update the Final Summary, and run graphify update.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Corrected on 2026-09-13: now depends on TASK-0214.03, which rewrites the signup block-membership statements and invalidates the original signup.go line references (131, 254, 335, 355, 384).

The block-membership casts are removed by TASK-0214.03; only the remaining public_id casts stay here. Non-canonical signup block identifiers keep ErrSignupBlockNotFound (400 signup-block-not-found), not pgx.ErrNoRows.

Corrected on 2026-09-13 after a second audit of the corrected scope.

Corrections:

- The cast list missed `GetResponseByPublicID` in `server/postgres/identities.go` (HEAD:111; 155 after TASK-0214.01), which backs client responseId on non-signup and group mutations; it must be included or a typed bind would surface 22P02.
- `accounts.go:263` is stale (now 296 in `deleteAccountAuthority`, and it will move again); use function names. Upstream subtasks also shift the identities.go references, so all references must be re-located, not only the signup block-membership ones.
- The accounts.go cast defeats no index because `event_signup_responses` has no index on `event_visitor_identity_id`; treat it as type consistency.
- Index shapes tightened: drop the redundant `postgres_event_responses (event_id)` index; add `(event_id, lower(email))` for dashboard.go (the Go-side EqualFold in postgres_group.go never reaches SQL); the postgres_events index must be explicit because the existing partial index is unusable by analytics; add `id` to the daily_user_log_members index to match `ORDER BY first_seen_position, id`.
- EXPLAIN verification was not meaningful on an empty test database; require seeded data or forced plans.
- The blanket `pgx.ErrNoRows (404)` claim was wrong for `GetEventVisitorIdentity` and `GetEventVisitorCredential`, where callers treat ErrNoRows as anonymous/no-credential; acceptance criterion #3 now enumerates mappings.
- ParseUUID validation is placed in the repository rather than "the boundary" so every caller is protected.
- Block-ID validation ownership between TASK-0214.03 and this task is now an explicit decision item; TASK-0214.03's notes defer it while its constraints forbid the cast error.
- Added non-canonical regression tests and migration-harness compatibility to the acceptance criteria.

Block-ID validation ownership resolved on 2026-09-13 after TASK-0214.03 landed.

- Block-ID validation lands in this task if it converts the signup block-identity comparisons. TASK-0214.03 keeps them on the text path (`block.id::text = ANY($2)` in `reserveSignupCapacity`, `block.id::text = claimed.block_text` in `writeSignupResponseMemberships`, and `existing.id::text = incoming.id_text` plus `id::text <> ALL($2::text[])` in `ReplaceSignupBlocks`) so non-canonical client text never reaches a uuid cast; its notes defer typed block binds here.
- Preserve the two distinct block-ID contracts when converting:
  - Response membership (`CreateSignupResponse`/`UpdateSignupResponse` `BlockIDs`): a non-canonical, unknown, or foreign identifier returns `ErrSignupBlockNotFound` (400 `signup-block-not-found`), never `pgx.ErrNoRows` and never `22P02`. `normalizeSignupBlockIDs` currently returns `[]string` only, so add validation there (returning an error) or in the capacity path before binding `uuid[]`.
  - `ReplaceSignupBlocks`: a canonical, non-empty block identity that does not name a same-event block keeps returning `pgx.ErrNoRows` (404 `event-not-found`), as tested in `server/postgres/signup_test.go`; TASK-0214.03 acceptance criterion #4 owns that behavior, and the "never pgx.ErrNoRows" requirement covers non-canonical identifiers only.
- Non-canonical client `_id`s never reach `ReplaceSignupBlocks` over HTTP: `canonicalSignupBlockID` in `server/routes/postgres_event_routes.go` coerces them to new blocks before the repository. Do not move that coercion into the repository.
- The signup.go `public_id::text` lookups (`GetSignupResponseByPublicID`, `DeleteSignupResponse`) remain this task's typed-bind scope as listed in the description.

Implementation (2026-09-13). Migration: server/migrations/20260913000001_supporting_indexes.sql adds postgres_event_responses_event_created_idx (event_id, created_at, id) and drops postgres_event_responses_event_id_idx; event_attendees_event_created_idx (event_id, created_at, id) and event_attendees_event_email_lower_idx (event_id, lower(email)); daily_user_log_members_log_position_idx (daily_user_log_id, first_seen_position, id); and replaces postgres_events_active_creator_posthog_id_idx with the explicit partial postgres_events_creator_created_at_idx (created_at, creator_posthog_id) WHERE creator_posthog_id IS NOT NULL AND creator_posthog_id <> ''. The Down restores the two retired indexes. Decision: the retired (creator_posthog_id, created_at DESC) WHERE NOT is_deleted index is replaced because no query uses its shape while analytics deliberately includes soft-deleted events and ranges on created_at. Decision: no event_signup_responses (event_visitor_identity_id) index; account deletion ORs platform_identity_id and event_visitor_identity_id, neither indexed, so one index cannot serve the OR and deletion is a cold path; the accounts.go cast removal is type consistency only. Decision: signup block-identity comparisons stay on the TASK-0214.03 text path, so ErrSignupBlockNotFound for membership and pgx.ErrNoRows for ReplaceSignupBlocks are untouched and no block-ID validation lands here; AC #2/#5 enumerate only the five opaque IDs.

Typed binds: identities.go adds validUUID (canonical models.ParseUUID check; validPlatformIdentityID delegates) and GetEventVisitorIdentity, GetEventVisitorCredential, and GetResponseByPublicID validate the opaque identifier, return pgx.ErrNoRows for non-canonical values, and bind public_id/id typed. transfers.go LockAccessTransfer validates the transfer ID and binds id=$2. accounts.go binds event_visitor_identity_id = ANY($2::uuid[]). signup.go GetSignupResponseByPublicID, DeleteSignupResponse, and the opaque public-ID path of UpdateSignupResponse validate the public ID and return pgx.ErrNoRows before binding public_id typed.

Tests: server/postgres/supporting_indexes_test.go adds the temp-schema index schema test (columns, partial predicate, retired indexes, Down round-trip through pg_my_temp_schema) and an EXPLAIN (ANALYZE, BUFFERS) forced-plan test (enable_seqscan and enable_bitmapscan off; hash/merge joins off for the full active-user-days report) asserting postgres_events_creator_created_at_idx for both analytics queries, postgres_event_responses_event_created_idx, event_attendees_event_created_idx, event_attendees_event_email_lower_idx, daily_user_log_members_log_position_idx (order, MAX, and full report), and access_transfers_pkey for the lock. Non-canonical regression tests cover event-visitor public ID, credential ID, response public ID (identities_test.go), signup read/delete/update public ID (signup_test.go), and transfer ID (transfers_test.go); existing signup block tests keep asserting ErrSignupBlockNotFound. accounts_test.go adds account deletion with a signup response present for the typed uuid[] bind.

Verification: docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test passes all packages after a migrator-image rebuild applied the new migration to the isolated test database; go vet ./... and gofmt are clean (pre-existing routes/auth_otp_test.go and utils/response_utils.go remain unformatted and untouched); npm run format:markdown ran from the repo root; Firefox desktop E2E with E2E_FRONTEND=bundled passed 56 with 1 skipped and 0 failed (5.7m, log /tmp/opencode/task-0214.04-e2e.log); graphify update . ran after the changes.

Reopened on 2026-09-13 to resolve the critical review in backlog/handoffs/handoff-2026-09-13T09-51-42Z.md; findings 1-6 are recorded here and the Final Summary is corrected after the fixes land.

Finding 1: TASK-0214.01 acceptance criterion #5 deferred the case-insensitive attendee email check to this task, but postgresGroupViewerIsInvitee still scans loaded attendees with strings.EqualFold, and the new event_attendees_event_email_lower_idx was reached only by server/postgres/dashboard.go. Fix: add a repository EXISTS method over event_attendees (event_id, lower(email)) and use it in the group route so the deferral is actually closed.

Finding 2: server/postgres/signup.go reserveSignupCapacity still compared membership.response_id::text <> $3, a response-identity comparison on a UUID column rather than a block-identity comparison. Fix: replace the cast with an empty-safe typed predicate covering both the empty and non-empty exclude values.

Finding 3: TestAccountRepositoryDeletionRemovesOwnSignupResponses cannot isolate the event_visitor_identity_id = ANY($2) branch because the owner response is inserted with platform_identity_id set, so the platform_identity_id = $1 clause matches it, and the later visitor delete cascades it regardless. Fix: make the test observe the typed array branch independently of the FK cascade.

Finding 4: the migration comment claimed neither event_signup_responses deletion column carries an index, but event_signup_responses_account_unique_idx (event_id, platform_identity_id) WHERE respondent_kind = 'account' exists; the accurate statement is that no index serves a lookup by platform_identity_id alone. Fix the comment.

Finding 5: server/postgres/supporting_indexes_test.go duplicated the production SQL for response listing, attendee listing, dashboard membership, the daily-log report, and the transfer lock, so the forced-plan assertions can drift from the real queries. Fix: extract shared SQL constants that production and the plan tests both use, as monthlyActiveCreatorDaySpine already does for analytics.

Finding 6 nits: the explicit $2::uuid[] cast in deleteAccountAuthority is redundant because the surrounding ANY($2) binds already infer uuid[] for []string, and the schema test checks creator_posthog_id IS NOT NULL but not the <> '' half of the partial predicate. Fix both.

Review fixes (2026-09-13). Finding 1 closure: server/postgres/attendees.go adds nonDeclinedAttendeeEmailExistsQuery and HasNonDeclinedAttendeeEmail, and routes/postgres_group.go postgresGroupViewerIsInvitee uses it, so the case-insensitive attendee email check deferred by TASK-0214.01 acceptance criterion #5 now reaches event_attendees (event_id, lower(email)) instead of scanning loaded attendees; the responses route no longer loads attendees only for the visibility check. TestAttendeeRepositoryHasNonDeclinedAttendeeEmail covers case-insensitive matches, declined exclusion, other-event exclusion, unknown emails, and the empty-email guard.

Review fixes (2026-09-13). Finding 2 closure: reserveSignupCapacity now joins on membership.response_id IS DISTINCT FROM NULLIF($3, '')::uuid, so the response identity is a typed UUID comparison and an empty exclude still joins every membership. TestSignupCapacityClaimsAndMissingBlocks covers the non-empty exclude (re-saving the claiming response) and the create paths cover the empty exclude.

Review fixes (2026-09-13). Finding 3 closure: TestAccountRepositoryDeletionRemovesOwnSignupResponses drops event_signup_responses_visitor_event_fk inside the test transaction and inserts a guest response on the account-owned visitor, so the event_visitor_identity_id = ANY($2) branch is observable independently of the FK cascade; the direct account response still covers the platform_identity_id = $1 clause and another visitor's response is the control.

Review fixes (2026-09-13). Finding 4 closure: the migration comment now names event_signup_responses_account_unique_idx and states that its event_id-leading column order cannot serve a lookup by platform_identity_id alone.

Review fixes (2026-09-13). Finding 5 closure: listResponsesQuery, listAttendeesQuery, listDashboardEventsQuery, listActiveUserDaysQuery, recordDailyUserLogMembershipQuery, lockAccessTransferQuery, countDistinctMonthlyActiveEventCreatorsByDayQuery, and countDistinctMonthlyActiveEventCreatorsWithMoreThanXEventsByDayQuery are the production statements, and supporting_indexes_test.go plan-tests those constants instead of copied SQL.

Review fixes (2026-09-13). Finding 6 closure: deleteAccountAuthority binds event_visitor_identity_id = ANY($2) without the redundant ::uuid[] cast because the uuid column infers the array type, and TestSupportingIndexesSchema now asserts both creator_posthog_id IS NOT NULL and creator_posthog_id <> '' in the partial predicate.

Review fixes verification (2026-09-13): targeted postgres tests and the full docker compose server-route-test suite pass; gofmt lists only the pre-existing routes/auth_otp_test.go and utils/response_utils.go; go vet ./... is clean; Firefox desktop E2E with E2E_FRONTEND=bundled passed 56 with 1 skipped and 0 failed (log /tmp/opencode/task-0214.04-review-fixes-e2e.log).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
## Summary

Added the supporting indexes for the current query shapes, retired the two indexes those shapes cannot use, and replaced every remaining UUID-to-text cast outside the deliberately text-based signup block path with validated typed binds. A post-review pass then closed the TASK-0214.01 attendee-email deferral, typed the last response-identity comparison, and made the forced-plan tests run the production statements.

### Changes

- **Migration** (`server/migrations/20260913000001_supporting_indexes.sql`): `postgres_event_responses (event_id, created_at, id)` replacing the narrower `(event_id)` index; `event_attendees (event_id, created_at, id)` and `(event_id, lower(email))`; `daily_user_log_members (daily_user_log_id, first_seen_position, id)`; and `postgres_events (created_at, creator_posthog_id) WHERE creator_posthog_id IS NOT NULL AND creator_posthog_id <> ''` replacing the unusable `(creator_posthog_id, created_at DESC) WHERE NOT is_deleted` index. The Down restores both retired indexes. The comment records that the only index on either deletion column is `event_signup_responses_account_unique_idx (event_id, platform_identity_id) WHERE respondent_kind = 'account'`, which cannot serve a lookup by `platform_identity_id` alone.
- **Typed binds** (`identities.go`, `transfers.go`, `accounts.go`, `signup.go`): a canonical-UUID guard returns `pgx.ErrNoRows` for non-canonical event-visitor public IDs, credential IDs, response public IDs, signup response public IDs, and transfer IDs before binding; `deleteAccountAuthority` binds `event_visitor_identity_id = ANY($2)` with `uuid[]` inferred from the column. The signup block-identity comparisons stay on the TASK-0214.03 text path so `ErrSignupBlockNotFound` and `pgx.ErrNoRows` contracts are preserved.
- **Response-identity comparison** (`signup.go`): `reserveSignupCapacity` joins on `membership.response_id IS DISTINCT FROM NULLIF($3, '')::uuid`, so the last UUID-to-text cast outside the block path is gone and an empty exclude still counts every membership.
- **Viewer invitee check** (`attendees.go`, `routes/postgres_group.go`): `HasNonDeclinedAttendeeEmail` answers the check with one EXISTS over `event_attendees (event_id, lower(email))`, closing the TASK-0214.01 acceptance criterion #5 deferral; the group read no longer scans loaded attendees and the responses route no longer loads them just for visibility.
- **Recorded decisions**: the partial `is_deleted` creator index is replaced; no `event_signup_responses (event_visitor_identity_id)` index is added because one index cannot serve the OR and deletion is a cold path.

### Verification

- `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test`: all packages pass.
- `supporting_indexes_test.go` asserts index shape and the Down round-trip, and forced-plan `EXPLAIN (ANALYZE, BUFFERS)` runs the production SQL constants for analytics (both queries), response listing, attendee listing, the dashboard membership query, the invitee EXISTS, the daily-log append/report, and the transfer lock; non-canonical regression tests cover all five typed binds plus signup block IDs; the accounts deletion test isolates the `event_visitor_identity_id = ANY($2)` branch from the visitor FK cascade.
- Firefox desktop E2E (`E2E_FRONTEND=bundled`): 56 passed, 1 skipped, 0 failed.
- `go vet ./...`, `gofmt`, and `graphify update .` clean.
<!-- SECTION:FINAL_SUMMARY:END -->
