---
id: TASK-0190.03
title: 'Support PostgreSQL signed-in polls, dashboard, and folders'
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-09 21:45'
updated_date: '2026-09-10 18:28'
labels:
  - postgresql
  - migration
dependencies:
  - TASK-0190.02
documentation:
  - BACKLOG_WORKFLOW.md
  - server/docs/postgres-anonymous-event-compatibility.md
parent_task_id: TASK-0190
priority: high
type: task
ordinal: 200000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Add signed-in poll persistence and account-facing event organization using PostgreSQL.
Support mixed-store dashboard reads during transition without permanent dual writes.
Move folders and folder-event membership with explicit references that support legacy events during migration.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Signed-in timed and dates-only polls support creation, ownership, responses, settings, archive, and deletion in PostgreSQL.
- [x] #2 Dashboard lists owned/responded events correctly across the transition without duplicates or unauthorized visibility.
- [x] #3 Folder create/update/delete and membership operations work for new and legacy events with stable identifiers.
- [x] #4 Focused API and browser tests cover account association, dashboard reload, folders, archive/delete, and unauthorized access.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Parent task rolls up four completed subtasks and owns no separate implementation:
1. TASK-0190.03.01: signed-in timed/dates-only PostgreSQL event lifecycle, ownership, responses, settings, archive, delete, counter (AC1).
2. TASK-0190.03.02: mixed-store signed-in dashboard listing across PostgreSQL and MongoDB (AC2).
3. TASK-0190.03.03: PostgreSQL folders with legacy-compatible membership and stable identifiers (AC3).
4. TASK-0190.03.04: end-to-end Firefox verification of polls, dashboard, folders, archive/delete, unauthorized access (AC4).

Finalization: confirm the four subtasks are Done and committed, verify the roll-up acceptance criteria against the committed tests/code, re-run the isolated backend suite and required frontend checks, run the focused Firefox PostgreSQL specs and legacy MongoDB regression flows where feasible, then check the parent AC/DoD and record a roll-up final summary.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Parent verification re-run 2026-09-10 after all four subtasks were committed (e838ad5c, 35bec11a, 2ca3cf61, c708daec). The parent owns no separate implementation; it rolls up the subtask deliverables.

AC1 (signed-in lifecycle): TASK-0190.03.01 removed the signed-in short-circuit from postgresCreationEnabled (server/routes/postgres_event_routes.go:943) and made postgresCreateEvent resolve the account platform identity, associate owner_platform_identity_id/owner_external_id, and increment the account created-events counter in the creation transaction. Backed by TestSignedInPostgresEventLifecycle, TestSignedInPostgresResponseAssociation, and TestSignedInCreationFallsBackToMongoWhenPostgresDisabled (server/routes/postgres_signed_in_event_test.go).

AC2 (mixed-store dashboard): TASK-0190.03.02 added postgres.Repository.ListDashboardEvents (server/postgres/dashboard.go:31) and merged PostgreSQL owned/responded events into routes/user.go getEvents, exposing the canonical bare short_id as _id/shortId with no duplicates and no unauthorized visibility. Backed by TestSignedInDashboardListsPostgresOwnedAndResponded, TestSignedInDashboardMergesMongoAndPostgresEvents, and TestSignedInDashboardExcludesDeletedPostgresEvent (server/routes/postgres_dashboard_test.go) plus the eventPublicId helper (frontend/src/utils/eventIdentity.ts:8).

AC3 (folders): TASK-0190.03.03 added migration server/migrations/20260910140000_folders.sql and postgres/folders.go (CreateFolder, AssignEventToFolder, account-scoped reads/writes), with exactly one explicit membership reference (event_id or legacy_event_id) so TASK-0190.06 can rewrite legacy ids without a permanent map. Backed by TestSignedInFoldersCrudAndIsolation and TestSignedInFolderMembershipAcrossEventSources (server/routes/postgres_folders_test.go).

AC4 (API and browser tests): focused API tests above plus the Firefox spec e2e/specs/timed-event-postgres-dashboard-folders-firefox.spec.ts and helper e2e/helpers/postgres-inspect.ts from TASK-0190.03.04.

Objective checks run for this finalization: docker compose isolated server-route-test `go test ./... -count=1` passed for every package including /postgres and /routes; frontend lint, fmt:check, typecheck, build, and test:unit (146 files, 1089 tests) passed; E2E_POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED=true Firefox run of timed-event-dashboard-postgres-firefox.spec.ts and timed-event-postgres-dashboard-folders-firefox.spec.ts passed 5/5; flag-off chromium account-deletion plus firefox timed-event-create and timed-event-days-only-timezone passed 9/9. No non-Backlog Markdown changed, so DoD #4 is not applicable.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
author: @opencode
created: 2026-09-10 14:11
---
Decomposed into four sequential subtasks: TASK-0190.03.01 signed-in event lifecycle in PostgreSQL (AC#1), TASK-0190.03.02 mixed-store dashboard listing (AC#2), TASK-0190.03.03 PostgreSQL folders with legacy-compatible membership (AC#3), and TASK-0190.03.04 end-to-end browser verification (AC#4). This task stays open as the parent and is complete when all four subtasks and its acceptance criteria are satisfied.

Durable decisions recorded for the subtasks and for TASK-0190.06:
- Signed-in creation reuses the existing POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED flag by removing the signed-in short-circuit; no second flag is introduced, and removal stays in TASK-0190.07.
- Folder membership uses two explicit references: the contract's post-cutover folder_events.event_id (PostgreSQL event UUID) plus a nullable legacy MongoDB event reference, exactly one populated. This keeps referential integrity and lets TASK-0190.06 rewrite legacy references without a permanent legacy-ID map.
- End-to-end browser coverage is a dedicated final subtask (TASK-0190.03.04); each feature subtask also carries focused API tests.
---
<!-- COMMENTS:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Completed the PostgreSQL signed-in polls, dashboard, and folders migration (TASK-0190.03) as a parent roll-up of four done subtasks.

What changed:
- TASK-0190.03.01 removed the signed-in short-circuit from postgresCreationEnabled and made postgresCreateEvent associate account ownership and advance the created-events counter, so signed-in timed and dates-only polls are stored in PostgreSQL when the existing POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED transition flag is enabled; responses, settings, schedule save/clear, archive/unarchive, and delete operate on PostgreSQL with unauthorized callers rejected, and flag-disabled creation still falls back to the unchanged MongoDB path.
- TASK-0190.03.02 added postgres.Repository.ListDashboardEvents and merged PostgreSQL owned/responded events into the MongoDB dashboard listing, exposing the canonical bare short_id as _id/shortId with no duplicates and no unauthorized visibility; the frontend eventPublicId helper centralizes the canonical public identifier.
- TASK-0190.03.03 made PostgreSQL authoritative for folder create/update/delete and folder-event membership through migration 20260910140000_folders.sql and postgres/folders.go, storing exactly one explicit reference per membership (event_id or legacy_event_id) and scoping every operation to the owning account.
- TASK-0190.03.04 added the Firefox spec e2e/specs/timed-event-postgres-dashboard-folders-firefox.spec.ts and the postgres-inspect helper to verify creation, dashboard reload, folder membership, archive/delete, and cross-account denial through the real browser UI.

Evidence:
- Isolated backend suite: `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test` -> all packages ok, including timeful/server/postgres and timeful/server/routes.
- Frontend: lint, fmt:check, typecheck, build, and test:unit (146 files, 1089 tests) all pass.
- Browser PostgreSQL: E2E_POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED=true Firefox -> timed-event-dashboard-postgres-firefox.spec.ts (1) and timed-event-postgres-dashboard-folders-firefox.spec.ts (4) = 5 passed, covering account association, dashboard reload, folder membership, archive/delete, and unauthorized access.
- Browser legacy MongoDB: flag-off chromium account-deletion (2) and firefox timed-event-create (6) plus days-only-timezone (1) = 9 passed.
- No non-Backlog Markdown changed, so DoD #4 is not applicable.

Acceptance criteria satisfied: PostgreSQL signed-in timed and dates-only poll creation, ownership, responses, settings, archive, and delete (AC1); mixed-store dashboard listing without duplicates or unauthorized visibility (AC2); PostgreSQL folder CRUD and legacy-compatible membership with stable identifiers (AC3); focused API and browser tests covering account association, dashboard reload, folders, archive/delete, and unauthorized access (AC4). Legacy event authorization and TASK-0190.06 migration of existing records remain out of scope.
<!-- SECTION:FINAL_SUMMARY:END -->
