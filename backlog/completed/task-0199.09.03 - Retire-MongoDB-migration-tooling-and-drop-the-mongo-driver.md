---
id: TASK-0199.09.03
title: Retire MongoDB migration tooling and drop the mongo driver
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-11 08:30'
updated_date: '2026-09-11 13:19'
labels:
  - postgresql
  - migration
dependencies:
  - TASK-0199.09.02
  - TASK-0199.10
references:
  - server/go.mod
  - server/go.sum
  - server/scripts/20260910_mongo_events_to_postgres
  - server/scripts/20260910_mongo_accounts_to_postgres
  - server/scripts/20260911_mongo_calendars_to_postgres
  - server/scripts/20260815_mongo_anonymous_events_to_postgres
  - server/docs/postgres-core-migration-runbook.md
parent_task_id: TASK-0199.09
ordinal: 233000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Retire the MongoDB migration tooling and remove the mongo driver from the server module after TASK-0199.10 and the live cutover have recorded their evidence.

Delete the remaining server/scripts backfill tools and their representative fixture sets, including the accounts, events, calendars, and anonymous-events migration commands and the backup/restore rehearsal, once their executed evidence is captured in the runbooks.
Remove respondents record-resolution helpers that only served legacy Mongo reads.
Run go mod tidy so go.mongodb.org/mongo-driver leaves go.mod and go.sum, and remove any indirect driver-only dependencies.
Update CI, compose test commands, and runbook instructions that referenced deleted scripts so no documented procedure still points at them.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 TASK-0199.10 backfill and its reconciliation evidence are recorded before any MongoDB tooling or fixture is deleted.
- [x] #2 All Mongo-backed one-off scripts and their representative fixtures are deleted, and no repository command, workflow, or runbook instructs running them.
- [x] #3 go.mod and go.sum no longer contain go.mongodb.org/mongo-driver, and go build, go vet, and the test suite pass.
- [x] #4 The runbook records the final collection retirement and that removal is irreversible without the retained source.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
## Implementation Plan

Dependencies TASK-0199.09.02 and TASK-0199.10 are Done, and their reconciliation evidence plus the TASK-0190.08 backup/restore evidence are recorded, so the tooling and fixtures can be deleted.

### Approach
1. Delete the Mongo-backed one-off scripts and their representative fixtures: `20260815_mongo_anonymous_events_to_postgres`, `20260910_mongo_accounts_to_postgres`, `20260910_mongo_events_to_postgres` (including `backup_restore_integration_test.go`), `20260911_mongo_calendars_to_postgres`, `20260911_mongo_dailyuserlogs_to_postgres`, and the script-only `server/scripts/internal/legacybson` package. Keep `20240721_apple_calendar_test` (not Mongo-backed and not a backfill tool).
2. Remove the respondents legacy record-resolution helper: delete `server/respondents/records.go` (`ResolveStoredUserID`) and simplify `populateSignUpResponsePayloadIdentity` to the explicit PostgreSQL account/guest identity path, updating the caller in `postgres_event_routes.go`.
3. Run `go mod tidy` in `server/` so `go.mongodb.org/mongo-driver` and its driver-only transitive dependencies leave `go.mod` and `go.sum`.
4. Update the runbooks that referenced deleted scripts: `server/docs/postgres-core-migration-runbook.md` (retire the tooling/rehearsal command sections, record the executed evidence and the irreversible tooling retirement) and `docs/postgres-staging-rollout.md` (replace script-path evidence references). CI and Compose use `go test ./...`, so they need no script-reference change; MongoDB Compose/environment removal stays with TASK-0199.09.04.
5. Verify: `gofmt`, `go build ./...`, `go vet ./...`, driver-free greps, the isolated Compose backend suite, focused and full Firefox e2e, `npm run format:markdown:check`, and `graphify update .`. Record evidence per acceptance criterion and finalize.

### Guardrails
- Do not remove MongoDB Compose, environment, or CI configuration here (TASK-0199.09.04).
- Do not rewrite requirements, glossary, or the broader docs surface here (TASK-0199.09.05); only retire runbook instructions that pointed at deleted scripts.
- Removal is irreversible: capture the executed evidence in the runbook before deleting.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Implementation (2026-09-11)

Dependencies TASK-0199.09.02 and TASK-0199.10 are Done, and their rehearsal and reconciliation evidence was recorded before any deletion.

- Evidence captured first: `server/docs/postgres-core-migration-runbook.md` now documents the five one-off commands, the executed rehearsal evidence, and the irreversible tooling retirement, and its runnable `go run`/`go test` blocks pointing at the deleted scripts are gone. `docs/postgres-staging-rollout.md` evidence references were reworded to the retired rehearsals.
- Deleted the Mongo-backed one-off scripts and their representative fixtures: `20260815_mongo_anonymous_events_to_postgres`, `20260910_mongo_accounts_to_postgres`, `20260910_mongo_events_to_postgres` (with `backup_restore_integration_test.go`), `20260911_mongo_calendars_to_postgres`, `20260911_mongo_dailyuserlogs_to_postgres`, and the script-only `server/scripts/internal/legacybson` package. Kept `20240721_apple_calendar_test` (not Mongo-backed, not a backfill tool).
- Removed the legacy respondents record-resolution helper: deleted `server/respondents/records.go` (`ResolveStoredUserID`) and simplified `populateSignUpResponsePayloadIdentity` to the explicit PostgreSQL account/guest identity path, dropping the unused `storedKey` plumbing in `postgresSignUpResponses`.
- `go mod tidy` removed `go.mongodb.org/mongo-driver` and its driver-only indirect dependencies (`golang/snappy`, `klauspost/compress`, `montanaflynn/stats`, `xdg-go/pbkdf2`, `xdg-go/scram`, `xdg-go/stringprep`, `youmark/pkcs8`) plus the stale `gopkg.in/gomail.v2`/`quotedprintable.v3` left over from the mail-utils deletion.
- CI and Compose needed no script-reference change because both invoke `go test ./...`; MongoDB Compose/environment removal stays with TASK-0199.09.04, and requirements/glossary work stays with TASK-0199.09.05.

## Verification (evidence per acceptance criterion)

- AC1: TASK-0199.10 is Done with its reconciliation evidence, and this session's first change was recording the backfill and TASK-0190.08 backup/restore results in the runbook before deleting any tooling.
- AC2: deletion list above; `rg` for `20260815|20260910|20260911|legacybson|go run ./scripts|go test ./scripts` finds no executable or procedural references outside Backlog and generated graph output. The only remaining mentions are the runbook's historical "Backfill Tooling And Retirement" record, which states the commands were deleted.
- AC3: `rg go.mongodb.org server/go.mod server/go.sum` returns nothing; `go build ./...` and `go vet ./...` pass; the isolated suite `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test` exited 0 with every package `ok` (log `/tmp/opencode/task-0199.09.03-backend-1.log`).
- AC4: the core runbook records the tooling retirement, that the commands cannot run again from the repository, and that recovery depends on a verified PostgreSQL backup or the retained MongoDB source until the collections are retired.
- DoD #2: backend suite above; no frontend runtime change.
- DoD #3: focused signup Firefox spec passed 3/3 (`/tmp/opencode/task-0199.09.03-e2e-signup.log`); the full Firefox desktop run at `--workers=1` reported 55 passed, 1 skipped, and one 30s default-timeout test (`/tmp/opencode/task-0199.09.03-e2e-firefox.log`); that guest-access transfer test passed in isolation at a 60s timeout (31.1s) (`/tmp/opencode/task-0199.09.03-e2e-transfer-rerun-60s.log`), matching the host-load flake recorded in TASK-0199.09.02. The user directed skipping the full 60s rerun; CI runs the suite.
- DoD #4: `npm run format:markdown`, `npm run format:markdown:check`, and `npm run lint:markdown` pass.
- `graphify update .` rebuilt the graph (5581 nodes, 9788 edges, 401 communities).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Retired the MongoDB backfill tooling and removed the mongo driver from the server module.

Delivered:
- Deleted the anonymous-event, account, event, calendar, and daily-log backfill commands and their representative fixtures plus the script-only `server/scripts/internal/legacybson` package, keeping the non-Mongo `20240721_apple_calendar_test`.
- Removed `respondents.ResolveStoredUserID` and simplified `populateSignUpResponsePayloadIdentity` to the explicit PostgreSQL account/guest identity path.
- `go mod tidy` dropped `go.mongodb.org/mongo-driver`, its driver-only indirect dependencies, and stale gomail dependencies.
- Rewrote `server/docs/postgres-core-migration-runbook.md` to record the executed rehearsal evidence and the irreversible tooling retirement, and reworded `docs/postgres-staging-rollout.md` evidence references so no runbook points at deleted scripts.

Evidence: `go build ./...`, `go vet ./...`, and the isolated backend suite pass with no driver in `go.mod`/`go.sum`; focused signup Firefox e2e passed 3/3 and the full Firefox run reported 55 passed, 1 skipped, and one 30s host-load timeout that passes at a 60s timeout (31.1s); Markdown format and lint checks pass; `graphify update .` completed.
<!-- SECTION:FINAL_SUMMARY:END -->
