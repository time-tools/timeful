---
id: TASK-0199.09.06
title: 'Verify PostgreSQL-only startup, suites, and E2E'
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-11 08:30'
updated_date: '2026-09-11 14:14'
labels:
  - postgresql
  - migration
dependencies:
  - TASK-0199.09.03
  - TASK-0199.09.04
  - TASK-0199.09.05
references:
  - AGENTS.md
  - compose.test.yaml
  - e2e/package.json
  - server/docs/postgres-core-migration-runbook.md
parent_task_id: TASK-0199.09
ordinal: 236000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Verify and record that the application runs with PostgreSQL as its only database, then record the umbrella evidence.

Start the isolated stack with no MongoDB service, confirm the server process starts and /api/health reports ready with only PostgreSQL, and run the full isolated backend suite without MONGODB_* variables.
Run the frontend required checks and the Firefox desktop browser suite against the PostgreSQL-only stack.
Run the Markdown format check and update the knowledge graph.
Map the recorded evidence to every TASK-0199.09 acceptance criterion and write the final summary; leave the parent task open for TASK-0199.09.07 to remove the historical migration documentation and finalize it.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The server starts and /api/health reports ready with only PostgreSQL running.
- [x] #2 The isolated backend suite passes with no MongoDB service and no MONGODB_* variables configured.
- [x] #3 Frontend lint, fmt:check, typecheck, build, and unit checks pass, and the Firefox desktop E2E suite passes against the PostgreSQL-only stack.
- [x] #4 Every TASK-0199.09 acceptance criterion is verified with recorded evidence and changed Markdown is formatted; the parent finalization moves to TASK-0199.09.07.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
## Implementation Plan

Dependencies TASK-0199.09.03, .04, and .05 are Done. This is a verification-only task: no runtime code changes are expected; the deliverable is recorded evidence mapped to every TASK-0199.09 acceptance criterion.

### Preconditions confirmed
- `server/go.mod` has no `go.mongodb.org/mongo-driver`; only historical comments in `server/models/event.go` and `server/routes/anonymous_event_compatibility_test.go` mention Mongo.
- `.env.test` and `.env.test.example` contain no `MONGODB_*` variables; Compose overlays define no `mongo*` service.
- `compose.test.yaml` test stack is PostgreSQL-only (`postgres-test`, bootstrap, migrate, `server-test`, `calendar-mock`); the mock is a calendar provider, not a database.
- Parent TASK-0199.09 is still To Do and its AC3/AC4 evidence sources are the completed TASK-0190.08, TASK-0199.10, and the runbooks updated in TASK-0199.09.03/.05.

### Steps
1. Create the external Go cache volumes; start the isolated stack PostgreSQL services plus `server-test`; wait for the server healthcheck and curl `http://127.0.0.1:3003/api/health` to record a ready response with only PostgreSQL running (AC1).
2. Prove no MongoDB service or `MONGODB_*` variable reaches the suite (`docker compose config`, `env` inside `server-route-test`), then run `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test` and capture the full log (AC2).
3. Run the frontend required checks (`lint`, `fmt:check`, `typecheck`, `build`, `test:unit`) and the full Firefox desktop Playwright project against the PostgreSQL-only stack with `E2E_FRONTEND=bundled` (AC3).
4. Run `npm run format:markdown:check` and `graphify update .` (AC4).
5. Map every TASK-0199.09 acceptance criterion to recorded evidence in the implementation notes and final summary, complete this task, record the umbrella evidence as a comment on the parent, and leave the parent open for TASK-0199.09.07 to remove the historical migration documentation and finalize it.

### Verification
- AC1 evidence: health response JSON and container list showing only PostgreSQL-family services.
- AC2 evidence: backend suite exit code and per-package results, plus a clean `env | grep -i mongo` and `docker compose config` grep.
- AC3 evidence: five frontend command results and the Playwright summary. Known host-load flake in `timed-event-access-transfer` at two workers: use bundled mode as e2e/AGENTS.md directs; if a 30s timeout recurs, rerun that spec in isolation per the documented failure loop.
- AC4 evidence: `format:markdown:check` result, `graphify update .` output, and the criterion-by-criterion evidence mapping.

### Guardrails
- Do not modify runtime code, tests, or configuration; this task verifies the completed removal.
- Do not start, stop, or alter the running development stack (`timeful-development-*`, `timeful-*`); only the isolated `timeful-test` stack is in scope.
- Leave TASK-0199.09 open; finalization is TASK-0199.09.07.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Verification (2026-09-11)

Verification-only task; no files changed outside Backlog-managed records.

- AC1: isolated stack `up -d server-test` started only postgres-test, bootstrap, migrate, calendar-mock, and server-test; `/api/health` and `/api/health/live` returned 200 `{"status":"ok"}`; no MongoDB container or `MONGODB_*` in the server environment (log `/tmp/opencode/task-0199.09.06-ac1-health.log`).
- AC2: rendered test Compose and the `server-route-test` container environment have zero case-insensitive `mongo` matches; full suite exited 0 with every package `ok` (log `/tmp/opencode/task-0199.09.06-backend.log`).
- AC3: frontend lint, fmt:check, typecheck, build, and unit (1088 tests) all exit 0 (log `/tmp/opencode/task-0199.09.06-frontend-checks.log`); Firefox desktop with `E2E_FRONTEND=bundled` passed 56, skipped 1, failed 0 in 6.4m (log `/tmp/opencode/task-0199.09.06-e2e-firefox.log`).
- AC4: `npm run format:markdown:check` exited 0; `graphify update .` exited 0 with no topology changes; TASK-0199.09 evidence mapping recorded in the final summary and as a comment on the parent, which stays open for TASK-0199.09.07.
- Guardrails held: no runtime code, tests, or configuration modified; the running development stack was not touched; the manual test stack was torn down before Playwright owned it, and E2E teardown removed the isolated stack.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Verified that the application runs with PostgreSQL as its only database and recorded the TASK-0199.09 umbrella evidence. This was a verification-only change: no runtime code, tests, or configuration were modified.

## Evidence per TASK-0199.09.06 acceptance criterion

### AC1 - Server starts and /api/health reports ready with only PostgreSQL
- Started the isolated stack with `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml up -d server-test`; Compose brought up only `postgres-test`, `postgres-test-bootstrap`, `postgres-test-migrate`, `calendar-mock` (test calendar provider, not a database), and `server-test`.
- `GET http://127.0.0.1:3003/api/health` returned `HTTP/1.1 200 OK` with `{"status":"ok"}`; `/api/health/live` also returned 200.
- `docker compose ps` showed no MongoDB container; `docker inspect timeful-test-server-test-1` environment had no `MONGODB_*` entry; the running server log shows route registration and PostgreSQL-only startup (no Mongo initialization).
- Log: `/tmp/opencode/task-0199.09.06-ac1-health.log`.

### AC2 - Isolated backend suite passes with no MongoDB service and no MONGODB_* variables
- `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml config | grep -i mongo` returned nothing, and `docker compose ... run --rm --no-deps server-route-test sh -c 'env | grep -i mongo'` returned nothing, proving neither service nor environment carries MongoDB configuration.
- Full suite `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test` exited 0 with every package `ok`, including `routes`, `postgres`, `accounts`, `models`, and `services/auth`.
- Log: `/tmp/opencode/task-0199.09.06-backend.log`; pre-check log: `/tmp/opencode/task-0199.09.06-ac2-precheck.log`.

### AC3 - Frontend checks and Firefox desktop E2E pass against the PostgreSQL-only stack
- From `frontend/`: `npm run lint` (0), `npm run fmt:check` (0), `npm run typecheck` (0), `npm run build` (0), `npm run test:unit` (0; 146 files, 1088 tests passed). Log: `/tmp/opencode/task-0199.09.06-frontend-checks.log`.
- From `e2e/`: `E2E_FRONTEND=bundled npm run test:e2e -- --project=firefox-desktop` exited 0 with 56 passed, 1 skipped, 0 failed (6.4m), including all eight access-transfer journeys and the PostgreSQL calendar-credential integration spec. Log: `/tmp/opencode/task-0199.09.06-e2e-firefox.log`.

### AC4 - Umbrella evidence mapped and Markdown formatted
- `npm run format:markdown:check` exited 0. `graphify update .` exited 0 (449 code files re-extracted from cache, no code-graph topology changes; outputs left untouched).
- The parent TASK-0199.09 evidence mapping is recorded below and as a comment on the parent, which is intentionally left open for TASK-0199.09.07.

## Mapping to every TASK-0199.09 acceptance criterion
- #1 (MongoDB initialization, driver, and collection variables removed): `server/db` is absent; `grep go.mongodb.org server/go.mod server/go.sum` returns nothing; `MONGODB_` appears in no Go, Compose, env example, JSON, shell, or Dockerfile file; `server/main.go` initializes only PostgreSQL.
- #2 (Compose, environment, health checks, docs no longer require MongoDB; server starts and passes route suite with PostgreSQL only): rendered Compose for `compose.yaml` plus each overlay has no `mongo` service; `.env.test`/`.env.test.example` have no `MONGODB_*`; the operational docs were de-Mongoed by TASK-0199.09.05; the server started and the route suite passed against PostgreSQL only (AC1/AC2 above).
- #3 (core-record and retained-data cutovers passed before removal with rollback boundaries recorded): TASK-0190.08 is Done with backup/restore reconciliation (11 relations, ~75 KB dump) and the final isolated rehearsal; TASK-0199.07, TASK-0199.08, and TASK-0199.10 are Done. `server/docs/postgres-core-migration-runbook.md` (Cutover Gate And Rollback Boundaries) and `docs/postgres-staging-rollout.md` (Rollback Limits, Final MongoDB Collection Retirement) record the per-kind write-freeze boundary and its irreversibility.
- #4 (legacy fixtures and historical handoffs preserved; runbook documents final collection retirement): `backlog/handoffs/` retains 18 tracked entries plus the three 2026-09-11 entries, and the non-Mongo `server/scripts/20240721_apple_calendar_test` is kept; `server/docs/postgres-core-migration-runbook.md` section "Final MongoDB Retirement" and `docs/postgres-staging-rollout.md` section "Final MongoDB Collection Retirement" document the executed backfills and the final collection retirement.

## Verification
No new warnings or regressions: backend suite, frontend required checks, Firefox desktop E2E, Markdown format check, and the graph update all passed. No commit was made.

## Follow-ups
TASK-0199.09.07 removes the historical migration documentation, prunes the PostgreSQL runbooks, and finalizes the parent; the parent task remains open with the umbrella evidence recorded.
<!-- SECTION:FINAL_SUMMARY:END -->
