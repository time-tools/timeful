---
id: TASK-0190.04.01
title: Add PostgreSQL signup schema and repository with atomic capacity
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-10 15:17'
updated_date: '2026-09-10 15:33'
labels:
  - postgresql
  - migration
dependencies:
  - TASK-0190.02
  - TASK-0190.03.01
documentation:
  - BACKLOG_WORKFLOW.md
  - server/docs/postgres-core-migration-contracts.md
  - server/docs/postgres-anonymous-event-compatibility.md
parent_task_id: TASK-0190.04
priority: high
type: task
ordinal: 201100
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Implement the PostgreSQL persistence layer for signup forms: the event_signup_blocks and event_signup_responses tables, the signup event-kind constraint, and the repository operations that back blocks and signup responses. Preserve block/response relations and enforce block capacity atomically. Do not add route behavior in this subtask.

Schema targets follow server/docs/postgres-core-migration-contracts.md (Signup Form Fields): legacy Mongo block identifiers and signup-response keys are rewritten to fresh PostgreSQL identities during TASK-0190.06, so this subtask builds the runtime target shapes rather than a legacy-identifier map. Capacity enforcement is new behavior required by that contract.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Migration adds event_signup_blocks and event_signup_responses and extends postgres_events.type to admit the signup kind.
- [x] #2 Repository supports block create/replace/list and signup response create/update/delete keyed by opaque public ID and Event Visitor Identity.
- [x] #3 Capacity reservation is atomic under the event row lock and rejects over-capacity signups without partial writes.
- [x] #4 Isolated repository and migration tests cover relations, canonical guest names, and capacity contention.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Deliverables
1. Migration `server/migrations/20260910150000_signup_forms.sql`: create `event_signup_blocks` (event FK, name, nullable capacity, millisecond instants start_date/end_date, position for stable list order) and `event_signup_responses` (opaque `public_id`, event/Event Visitor Identity relation, respondent identity columns, `name`, `email`, `block_ids UUID[]`, partial unique indexes for account and canonical guest name), and extend `postgres_events.type` to admit the `signup` kind.
2. Repository in `server/postgres/signup.go`: `SignupBlock` and `SignupResponse` types plus block create/replace/list and response create/get/list/update/delete keyed by opaque public ID and Event Visitor Identity. Canonicalize guest names through `respondents.NormalizeGuestName` (never reimplement it). Validate block membership. Enforce capacity atomically by locking the event row and counting other responses that already claim each limited block before writing.
3. Update `deleteAccountAuthority` to remove the account's own signup responses and include their visitor identities so account deletion releases signup relations.
4. Isolated tests: repository tests over temp-table migrations for relations, canonical guest names, replace/list ordering, and update/delete; a real-pool concurrency test proving capacity contention admits exactly the capacity count with no partial writes; a focused migration constraint test for the signup kind and referential integrity.

Verification
- `cd server && go build ./... && go vet ./...`
- `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test go test ./... -count=1`
- `graphify update .` after changes.

Sequence: migration, types/repository, deletion integration, tests, full isolated suite.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Implementation complete. Delivered migration 20260910150000_signup_forms.sql (event_signup_blocks with ordered positions and nullable capacity, event_signup_responses with opaque public_id, Event Visitor Identity relation, respondent identity columns, block_ids text[], partial unique indexes for account and canonical guest name, and the extended postgres_events type constraint admitting `signup`), repository server/postgres/signup.go (create/replace/list blocks, create/get/list/update/delete responses keyed by opaque public ID and Event Visitor Identity, guest names canonicalized via respondents.NormalizeGuestName, and atomic capacity reservation under the event row lock), the EventTypeSignup constant, and signup cleanup in deleteAccountAuthority.

AC#1 evidence: TestSignupMigrationSchemaConstraints applies the migration into temp tables and proves the signup kind is admitted while an unknown kind is rejected, the visitor/event composite FK is enforced, negative capacity is rejected, and guest/account identity columns are required. The real stacked migration also applied via goose on the isolated test database.

AC#2 evidence: TestSignupBlocksCreateReplaceAndListPreserveOrder and TestSignupResponseCanonicalGuestNameAndMembership exercise create/replace/list, opaque public-ID lookup, update, and delete; TestSignupResponseAccountIdentityAndBlockValidation covers account identity and foreign-block validation.

AC#3 evidence: TestSignupCapacityReservationAtomicUnderContention runs 8 concurrent signups against a capacity-2 block on a real pool and asserts exactly 2 are admitted, 6 return ErrSignupCapacityExceeded, and exactly 2 rows are stored (no partial writes). TestSignupUnlimitedBlockAdmitsAll proves nil capacity is not limiting.

AC#4 evidence: the isolated temp-table repository and migration tests above cover relations, canonical guest names, and capacity contention.

Checks run: `cd server && go build ./...`, `go vet ./...`, and `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test go test ./... -count=1` all pass.

DoD #3: no route or browser behavior is added by this subtask (explicitly out of scope), so no e2e coverage is required. DoD #4: no repository Markdown was authored; the Backlog task record is MCP-managed.

Also added the folders and signup migrations to newAccountsTestRepository so the account-deletion path now exercises temp tables instead of the real test-stack event_signup_responses/folders tables.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added the PostgreSQL signup persistence layer: schema, repository, and isolated tests.

Delivered:
- server/migrations/20260910150000_signup_forms.sql creates event_signup_blocks (ordered positions, nullable capacity, millisecond start/end instants) and event_signup_responses (opaque UUID public_id, Event Visitor Identity relation, account/guest respondent identity, name, email, block_ids text[], partial unique indexes), and extends postgres_events.type to admit the `signup` kind.
- server/postgres/signup.go adds SignupBlock and SignupResponse types and repository operations: CreateSignupBlock, ReplaceSignupBlocks, ListSignupBlocks, CreateSignupResponse, GetSignupResponseByPublicID, ListSignupResponses, UpdateSignupResponse, and DeleteSignupResponse. Guest names are canonicalized through respondents.NormalizeGuestName. Capacity is reserved atomically inside the transaction that holds the event row lock, so concurrent signups for a limited block admit exactly the capacity count and reject the rest without partial writes. Replacement detaches removed block identities from responses so relations never dangle.
- server/postgres/types.go records EventTypeSignup.
- deleteAccountAuthority now removes the account's signup responses and includes their visitor identities before identity removal.
- server/postgres/signup_test.go and the newAccountsTestRepository migration chain additions.

Evidence: TestSignupMigrationSchemaConstraints (kind/FK/capacity/identity constraints), TestSignupBlocksCreateReplaceAndListPreserveOrder (relations and ordering), TestSignupResponseCanonicalGuestNameAndMembership (canonical guest names and unique/delete semantics), TestSignupResponseAccountIdentityAndBlockValidation (account identity and foreign block), TestSignupCapacityReservationAtomicUnderContention (capacity contention), TestSignupUnlimitedBlockAdmitsAll.

Verification: `cd server && go build ./...` and `go vet ./...` pass; `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test go test ./... -count=1` passes. No route behavior or Markdown was added.
<!-- SECTION:FINAL_SUMMARY:END -->
