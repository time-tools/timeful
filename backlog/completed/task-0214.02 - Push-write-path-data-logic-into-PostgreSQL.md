---
id: TASK-0214.02
title: Push write-path data logic into PostgreSQL
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-12 22:06'
updated_date: '2026-09-12 23:24'
labels: []
dependencies:
  - TASK-0214.01
modified_files:
  - server/postgres/attendees.go
  - server/postgres/calendar.go
  - server/postgres/folders.go
  - server/postgres/repository.go
  - server/postgres/attendees_test.go
  - server/postgres/calendar_test.go
  - server/postgres/folders_test.go
  - server/postgres/repository_test.go
  - server/routes/postgres_event_routes.go
  - server/routes/postgres_group.go
  - server/routes/postgres_owner.go
  - server/routes/postgres_group_test.go
  - server/accounts/calendar.go
parent_task_id: TASK-0214
priority: medium
type: task
ordinal: 216000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
# Goal

Push write-path loops and read-modify-write sequences into set-based PostgreSQL statements without schema changes.

# Scope

- `server/postgres/attendees.go:41-59`, `server/routes/postgres_group.go:208-257`, `server/routes/postgres_event_routes.go:1121-1135`: batch attendee account resolution and inserts with `unnest` and `INSERT ... SELECT ... LEFT JOIN LATERAL`, and replace per-email removals with one owner-guarded `DELETE`. Duplicate input emails are tolerated today because each `AddAttendee` call is a separate statement; a set-based `ON CONFLICT DO UPDATE` over duplicates raises PostgreSQL `21000` ("ON CONFLICT DO UPDATE command cannot affect row a second time"), so the batch must dedupe on the exact `(event_id, email)` unique key (case-sensitive) before upserting. Preserve owner protections, decline state, first-occurrence ordering (`WITH ORDINALITY`), and the existing COALESCE identity-fill-on-conflict behavior.
- `server/routes/postgres_group.go:229-239`: the departed-member response cleanup currently does one `GetResponseByPlatformIdentityID` plus `DeleteResponse` per removed member. Replace it with one `DELETE ... RETURNING` scoped by event, `respondent_kind = 'account'`, and the removed platform identities, and decrement `NumResponses` by the returned row count, preserving the owner guard.
- `server/accounts/calendar.go:145-168`: replace the per-sub-calendar upsert, re-read, and per-row delete with one set-based upsert from `unnest` and one `DELETE ... <> ALL(...)`. Resolve the calendar account ID first so a missing account still returns `pgx.ErrNoRows`; a bare `INSERT ... SELECT` plus `DELETE` silently succeeds when the incoming set is empty, and a nil Go slice binds SQL `NULL` so `<> ALL(NULL)` deletes nothing (bind an empty array instead). The batched path avoids `GetCalendarAccountByKey` credential decryption; record that as an intentional behavior change. Keep the Go-side empty-sub-calendar-ID validation that the per-row helper performed, and run through `withTransaction` so `SaveCalendarAccount`'s transaction composes.
- `server/postgres/repository.go:165-179`: keep `UpdateEvent` for settings, schedule, and creation writes, and add targeted repository methods for the response-counter mutations at `server/routes/postgres_event_routes.go:826,854` and `server/routes/postgres_group.go:561,630`. The decrement at `server/routes/postgres_group.go:235` is persisted by the caller's full-row settings write at `server/routes/postgres_event_routes.go:622`; that write persists the group settings payload and must not be replaced by a counter-only update.
- `server/routes/postgres_owner.go:186-204`: set the archive and delete flags with targeted UPDATE statements instead of full-row `UpdateEvent` writes; the `postgresOwnerMutation` row lock and authorization pairing is unchanged.
- `server/postgres/folders.go:93-123`: replace the dynamically assembled SET list with a static `COALESCE` update. Preserve the no-op branch: when both name and color are nil the current code checks existence and returns without bumping `updated_at`, so the static statement needs `updated_at = CASE WHEN $3 IS NULL AND $4 IS NULL THEN updated_at ELSE clock_timestamp() END` (or an explicit existence pre-check). The route cannot express clearing color to `NULL`, so `COALESCE` is semantically equivalent for supplied values.

# Constraints

- Keep the event row lock and transaction boundaries that serialize response mutations.
- Preserve `23505` to duplicate-name error mapping and `pgx.ErrNoRows` not-found behavior, including the calendar sync empty-incoming-set and missing-account cases.
- The batched attendee insert must tolerate duplicate emails, preserve first-occurrence insertion order, and keep the existing owner-removal protection and decline preservation.
- SQL stays in `server/postgres/`.
- No schema changes in this subtask.

# Verification

- Run the backend route and repository suites with the isolated test overlay in `compose.test.yaml`: create the external Go cache volumes, start `postgres-test`, and run `server-route-test`.
- Add or update tests covering the batched write paths before or with the fix: duplicate attendee emails, owner protection on removal, case-sensitive email keys, empty sub-calendar set, missing calendar account, folder no-op, and the counter-only response create/delete paths.
- `LockEvent` is owned by TASK-0214.01 and is out of scope here.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Attendee creation and group attendee edits use set-based INSERT or DELETE statements instead of per-email queries, tolerating duplicate emails, preserving first-occurrence order, owner-removal protection, and the existing COALESCE decline/identity conflict behavior.
- [x] #2 Departed-member response cleanup in group attendee edits deletes responses in one statement and adjusts the response count by the deleted row count, preserving the owner guard.
- [x] #3 Calendar sub-calendar sync uses one set-based upsert and one set-based delete, preserves enabled nil-versus-false semantics, reports pgx.ErrNoRows for a missing account even when the incoming set is empty, and treats an empty incoming set as delete-all.
- [x] #4 Response-counter updates at server/routes/postgres_event_routes.go:826,854 and server/routes/postgres_group.go:561,630 use targeted UPDATE statements instead of full-row UpdateEvent writes, with row-lock and transaction pairing preserved; the group settings write at server/routes/postgres_event_routes.go:622 stays a full-row write.
- [x] #5 Archive and delete flags use targeted UPDATE statements instead of full-row UpdateEvent writes, while UpdateEvent remains for settings, schedule, and creation writes.
- [x] #6 Folder updates use a static statement and preserve the no-op semantics, including no updated_at bump when neither field is supplied.
- [x] #7 Error mapping, owner protections, and ordering contracts are unchanged; the PostgreSQL-backed test suite passes under compose.test.yaml.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
# Implementation plan

## Findings

- `AddAttendee` (`server/postgres/attendees.go:41-59`) resolves one account and runs one `INSERT ... ON CONFLICT DO UPDATE`; routes loop over emails at group creation (`postgres_event_routes.go:1155-1168`) and group edits (`postgres_group.go:239-247`).
- `postgresApplyGroupAttendeeEdits` (`postgres_group.go:203-252`) also loops removals with one response lookup plus delete per removed platform identity.
- `syncCalendarSubCalendars` (`accounts/calendar.go:145-168`) upserts per sub-calendar, re-reads the connection through `GetCalendarAccountByKey` (credential decryption), then deletes per stored sub-calendar.
- Timed and group response paths mutate `locked.NumResponses` then rewrite the full event row via `UpdateEvent` (`postgres_event_routes.go:856-891`, `postgres_group.go:552-631`); archive/delete set one flag with a full-row write (`postgres_owner.go:186-205`).
- `UpdateFolder` (`postgres/folders.go:93-123`) assembles a dynamic SET list and has a no-op existence branch that does not bump `updated_at`.

## Steps

1. `postgres/attendees.go`: add `AddAttendees` (one `INSERT ... SELECT` over `unnest(...) WITH ORDINALITY` deduped by exact email with `DISTINCT ON`, `LEFT JOIN LATERAL` oldest-account resolution, existing `COALESCE` conflict fill and decline preservation), `RemoveAttendees` (one `DELETE ... email = ANY(...)`), and `DeleteAccountResponses` (one `DELETE` by event, `respondent_kind = 'account'`, and removed platform identities, returning the deleted count). Keep the per-row methods used by the repository tests.
2. `routes/postgres_group.go`: batch the owner-guarded removal, delete departed account responses in one statement and decrement `event.NumResponses` by the returned count, and batch the added-emails insert. Keep the in-memory decrement persisted by the caller's settings write.
3. `routes/postgres_event_routes.go`: create the group owner plus invitees through one `AddAttendees` call, owner first.
4. `accounts/calendar.go` + `postgres/calendar.go`: add `SyncCalendarSubCalendars`, resolving the connection first (missing account stays `pgx.ErrNoRows` even for an empty set), upserting from `unnest` with `COALESCE` enabled semantics, and deleting via `<> ALL` with a non-nil empty array. Keep Go-side empty-sub-calendar-ID validation and run through `withTransaction`; record that the batched path no longer decrypts credentials.
5. `postgres/repository.go`: add `AdjustEventResponseCount`, `SetEventArchived`, and `SetEventDeleted`; use them at the four counter call sites and the two flag call sites while keeping `UpdateEvent` for settings, schedule, and creation writes.
6. `postgres/folders.go`: replace the dynamic SET list with a static `COALESCE` update whose `updated_at` only advances when a field was supplied.
7. Tests: batch attendee dedupe/order/case-sensitivity/decline, owner-protected removal and departed-response count, calendar empty-set delete-all and missing-account `pgx.ErrNoRows`, folder no-op without an `updated_at` bump, targeted counter and flag updates, plus route assertions for duplicate attendee emails.
8. Run `docker compose ... server-route-test`, `gofmt`, `go vet`, and `graphify update .`; finalize with per-criterion evidence.

# Review fixes (2026-09-13)

Reopened to fix the defects recorded in `backlog/handoffs/handoff-2026-09-12T23-15-50Z.md`, all within this task's acceptance criteria:

1. `SyncCalendarSubCalendars` must dedupe `SubCalendarID`s (first occurrence) so duplicate input cannot raise PostgreSQL `21000` and roll back the sync; add a duplicate-ID regression test.

2. `DeleteAccountResponses` deliberately deletes every matching account response; document that widened semantics and clamp the group-edit counter decrement at zero so a stale count cannot violate the `num_responses >= 0` check and roll back the settings save. Apply the SQL-side clamp at the counter-only delete path too.

3. `AdjustEventResponseCount`, `SetEventArchived`, and `SetEventDeleted` must check `RowsAffected` and return `pgx.ErrNoRows` like `UpdateEvent` and `SetCalendarAccountEnabled`; add a missing-event regression test.

4. Record the bundled Firefox desktop CI-equivalent run (or an explicit exception) as the E2E evidence instead of an unqualified check.

5. Delete the row-wise `AddAttendee`, `RemoveAttendee`, `resolveAttendeeAccount`, `UpsertCalendarSubCalendar`, and `RemoveCalendarSubCalendar` methods, which production no longer calls, and migrate their tests to the batch APIs.

6. Simplify the `UpdateFolder` `errors.Is` branch to return the error directly.

7. Keep the accepted calendar credential-decryption removal documented and confirm credential failures still surface on the read path that needs them.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Corrected on 2026-09-13 after a code audit of the original scope.

Key corrections:

- `server/routes/postgres_group.go:235` is not an `UpdateEvent` call site; it is the in-memory `NumResponses--` inside `postgresApplyGroupAttendeeEdits`, persisted by the caller's settings write at `server/routes/postgres_event_routes.go:622`. That write also persists the group settings payload, so it is excluded from the targeted-update list.
- `LockEvent` (`server/postgres/identities.go:115-121`) is owned by TASK-0214.01 per that task's corrected scope and notes, and is removed from this subtask's scope and acceptance criteria.
- A set-based `INSERT ... ON CONFLICT DO UPDATE` over duplicate input emails raises PostgreSQL `21000` ("ON CONFLICT DO UPDATE command cannot affect row a second time"), while the current per-row `AddAttendee` calls tolerate duplicates; the batch must dedupe on the exact `(event_id, email)` key.
- A bare calendar `INSERT ... SELECT FROM unnest` plus `DELETE ... <> ALL` silently succeeds when the incoming set is empty and the account is missing, and binds `NULL` for a nil Go slice; the batch must resolve the account first and bind an empty array.
- The folder no-op branch returns without bumping `updated_at`; the static `COALESCE` statement must reproduce that.
- The batched calendar sync avoids `GetCalendarAccountByKey` credential decryption; record during implementation whether that changes expected error behavior.

Implementation notes (2026-09-13):

- `AddAttendees` is one `INSERT ... SELECT` over `unnest(...) WITH ORDINALITY`; `DISTINCT ON (email)` collapses exact duplicates to the first occurrence, `LEFT JOIN LATERAL` resolves the oldest case-insensitive account, and the existing `COALESCE` conflict update preserves stored decline state while filling a missing identity. `RemoveAttendees` is one `DELETE ... email = ANY(...)` and `DeleteAccountResponses` is one event-scoped `DELETE` for `respondent_kind = 'account'` over the removed platform identities, returning the deleted row count.

- Group attendee edits keep the owner guard, decrement `event.NumResponses` by the returned count in memory, and let the caller's full-row settings write persist it; the counter-only paths use `AdjustEventResponseCount`, and archive/delete use `SetEventArchived`/`SetEventDeleted`, leaving `UpdateEvent` for settings, schedule, and creation.

- `SyncCalendarSubCalendars` resolves the connection first (missing account stays `pgx.ErrNoRows` even with an empty set), upserts from `unnest`, and deletes with `<> ALL` over a non-nil empty array so an empty incoming set is delete-all; `withTransaction` composes with `SaveCalendarAccount`.

- Intentional behavior change recorded: the batched calendar sync no longer calls `GetCalendarAccountByKey`, so a provider refresh no longer decrypts credentials and cannot fail on credential decryption it does not need.

- `UpdateFolder` is a static `COALESCE` update whose `updated_at` advance is gated on a supplied field, preserving the no-op semantics.

Verification (2026-09-13):

- `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test`: all packages pass.

- New/updated tests pass: batch attendee dedupe/order/case-sensitivity/identity-fill/decline, batch removal, account-response cleanup count, calendar batch sync (empty-set delete-all, missing-account `pgx.ErrNoRows`, nil-versus-false enabled), folder static no-op without an `updated_at` bump, targeted counter/flag mutations, and route-level duplicate attendee emails.

- Firefox desktop E2E: 54 passed, 1 skipped, and 2 failures in the heavy access-transfer approval journeys caused by the documented 30s unbundled-dev-server budget; the full `timed-event-access-transfer-firefox.spec.ts` approval group passes in bundled mode (`E2E_FRONTEND=bundled`, 3 passed at one worker).

- `npm run format:markdown` run from the repo root; `gofmt` clean for all changed Go files; `graphify update .` run after the code changes.

Review fixes (2026-09-13, follow-up to `backlog/handoffs/handoff-2026-09-12T23-15-50Z.md`):

- Finding 1 fixed: `SyncCalendarSubCalendars` dedupes exact duplicate sub-calendar IDs with `DISTINCT ON (...) ... WITH ORDINALITY`, keeping the first occurrence, so duplicate input can no longer raise PostgreSQL `21000` and roll back the sync; `TestSyncCalendarSubCalendarsBatch` now starts with a duplicate ID and asserts the first occurrence and explicit false survive.

- Finding 2 fixed: the widened `DeleteAccountResponses` semantics (every matching account response, including multiple responses for one platform identity) are documented on the method; `AdjustEventResponseCount` clamps with `GREATEST(num_responses + $2, 0)`, and `postgresApplyGroupAttendeeEdits` clamps the in-memory decrement at zero so a stale counter cannot violate the non-negative check and roll back the group settings save.

- Finding 3 fixed: `AdjustEventResponseCount`, `SetEventArchived`, and `SetEventDeleted` check `RowsAffected` and return `pgx.ErrNoRows` for a missing event, matching `UpdateEvent` and `SetCalendarAccountEnabled`; `TestTargetedEventMutations` covers the clamp and all three missing-event errors.

- Finding 4 reconciled with an explicit exception: the full bundled Firefox desktop run was attempted in this session and aborted, so it is not re-run here. The canonical unbundled Firefox desktop run had 54 passed, 1 skipped, and 2 access-transfer approval failures caused by the documented 30s unbundled-dev-server budget; that approval group passes in bundled mode (3 passed at one worker), and `e2e/AGENTS.md:57` documents that the Firefox desktop CI job runs with `E2E_FRONTEND=bundled`. The DoD #3 check is therefore qualified by this recorded exception rather than an unqualified pass.

- Finding 5 fixed: removed the production-dead row-wise `AddAttendee`, `RemoveAttendee`, `resolveAttendeeAccount`, `UpsertCalendarSubCalendar`, and `RemoveCalendarSubCalendar`; their tests now exercise `AddAttendees`/`RemoveAttendees` and `SyncCalendarSubCalendars`, so the two SQL paths cannot drift.

- Finding 6 fixed: `UpdateFolder` returns the `RETURNING` scan error directly instead of wrapping `pgx.ErrNoRows` with `errors.Is`.

- Finding 7: the accepted calendar credential-decryption change stays documented on `syncCalendarSubCalendars`; credential decryption failures still surface through `ListCalendarAccountsForUser` and the session read paths that need credentials.

Verification after the review fixes (2026-09-13):

- `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test`: all packages pass, including `timeful/server/postgres` and `timeful/server/routes`.

- `gofmt -l` clean for every changed Go file and `go vet ./...` clean.

- Not run: the full browser E2E suite; the user aborted the bundled Firefox desktop invocation and the explicit exception above applies.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
## Summary

Pushed the write-path loops and read-modify-write sequences into set-based PostgreSQL statements without schema changes.

### Changes

- **Attendees** (`postgres/attendees.go`, `routes/postgres_group.go`, `routes/postgres_event_routes.go`): one `AddAttendees` batch insert per call (`unnest ... WITH ORDINALITY`, `DISTINCT ON (email)` for exact-duplicate collapse and first-occurrence order, `LEFT JOIN LATERAL` oldest-account resolution, existing `COALESCE` identity-fill with preserved decline state), one `RemoveAttendees` delete, and one event-scoped `DeleteAccountResponses` returning the deleted row count. Group creation and group edits now use them; the owner guard still protects the owner membership, and the departed-response count is persisted by the caller's settings write.
- **Calendar sync** (`accounts/calendar.go`, `postgres/calendar.go`): `SyncCalendarSubCalendars` resolves the connection first (missing account stays `pgx.ErrNoRows` even with an empty set), upserts via `unnest($2::text[], $3::text[], $4::bool[])` with `COALESCE` enabled semantics, and deletes absent sub-calendars with `<> ALL` over a non-nil empty array (empty set = delete-all). It runs through `withTransaction` and intentionally no longer decrypts credentials via `GetCalendarAccountByKey`.
- **Targeted event mutations** (`postgres/repository.go`, `routes/postgres_event_routes.go`, `routes/postgres_group.go`, `routes/postgres_owner.go`): `AdjustEventResponseCount`, `SetEventArchived`, and `SetEventDeleted` replace the full-row `UpdateEvent` writes at the four response-counter sites and the archive/delete flags; `UpdateEvent` stays for settings, schedule, and creation writes.
- **Folders** (`postgres/folders.go`): static `COALESCE` update with `updated_at = CASE WHEN $3 IS NULL AND $4 IS NULL THEN updated_at ELSE clock_timestamp() END`, preserving the no-op semantics.

### Verification

- `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test` passes every package.
- Added repository and route tests for duplicate/case-sensitive attendee emails, first-occurrence order, decline and identity fill, batch removal, departed-response cleanup count, calendar batch sync semantics, folder no-op, and targeted counter/flag updates.
- Firefox desktop E2E: 54 passed, 1 skipped; the 2 failures were the documented 30s unbundled-dev-server timeouts on the heavy access-transfer approval journeys, and that approval group passes in bundled mode (3 passed).
- `npm run format:markdown`, `gofmt`, and `graphify update .` run after the changes.

### Review fixes (2026-09-13)

- Calendar sync now collapses duplicate sub-calendar IDs to their first occurrence with `DISTINCT ON ... WITH ORDINALITY`, so a duplicate input cannot raise `21000`.

- The response-counter delete path clamps at zero in SQL (`GREATEST`) and in the group settings decrement, and the widened `DeleteAccountResponses` semantics are documented.

- `AdjustEventResponseCount`, `SetEventArchived`, and `SetEventDeleted` return `pgx.ErrNoRows` for a missing event.

- Removed the test-only row-wise repository methods and migrated their tests to the batch APIs; `UpdateFolder` returns the existence-check error directly.

- E2E exception recorded: the bundled Firefox desktop run was attempted and aborted; the task notes cite the bundled approval-group evidence and the documented CI configuration.
<!-- SECTION:FINAL_SUMMARY:END -->
