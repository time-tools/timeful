---
id: TASK-0208.01
title: Record the single account identifier decision in a new ADR
status: Done
assignee:
  - opencode
created_date: '2026-09-11 21:02'
updated_date: '2026-09-12 14:39'
labels: []
dependencies: []
documentation:
  - docs/design/architecture/adr/ADR-019.md
  - docs/design/architecture/adr/ADR-018.md
  - docs/design/architecture/adr/TEMPLATE.md
  - docs/requirements/functional/fr/FR-121.md
  - docs/requirements/functional/fr/FR-059.md
  - server/docs/postgres-data-boundaries.md
  - server/migrations/20260908160000_visitor_identities.sql
parent_task_id: TASK-0208
priority: medium
type: docs
ordinal: 248000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
ADR-019 currently decides that platform_identities.external_user_id holds the account's 24-character hexadecimal identifier and is the one resolution point. Replace that decision with a new ADR: platform_identities.id, a native UUIDv7, is the account's sole identifier, and the separate external_user_id column is removed. The new ADR decides the canonical wire form and encoding, how guest and unowned zero identity is represented, native uuid storage for the consolidated reference columns, the hard-cutover policy (single guarded backfill, no compatibility lookup, existing sessions force re-sign-in), and the rejected alternatives (keep 24-hex, add a UUIDv7 external_user_id, transitional dual-format support, keep TEXT storage). It supersedes ADR-019, which keeps its history with superseded_by. Update the affected requirement and boundary documentation.

Constraints:
- Follow docs/AGENTS.md sentence-per-line authoring and the ADR frontmatter in docs/design/architecture/adr/TEMPLATE.md.
- Supersede ADR-019 rather than rewriting its decision; ADR-019 keeps its original text plus superseded status and superseded_by.
- Preserve FR-121 traceability and the ADR-018 boundary link in the new record.
- Update FR-059 if its ObjectID-like name rejection depends on the account identifier format.
- Update server/docs/postgres-data-boundaries.md for the consolidated uuid references.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A new ADR with the next free ADR id records the decision, options, outcome, and consequences, and states that platform_identities.id UUIDv7 is the sole account identifier while external_user_id is removed
- [x] #2 The ADR fixes the canonical wire form and encoding, the guest and unowned zero-identity representation, native uuid storage for consolidated references, the one-time backfill with no compatibility lookup, and the forced re-sign-in consequence
- [x] #3 ADR-019 is marked superseded with superseded_by naming the new ADR, and its existing text is retained apart from the supersession notice
- [x] #4 FR-121 traceability, FR-059 if its name-rejection rule depends on the identifier format, server/docs/postgres-data-boundaries.md, and the ADR index row are consistent with the decision
- [x] #5 npm run format:markdown:check, npm run lint:markdown, npm run test:markdown-format, and npm run fmt:check pass from the repo root
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Research complete. Current state: ADR-019 (accepted) resolves accounts through 24-hex `platform_identities.external_user_id`; the baseline schema still has `external_user_id` plus TEXT reference columns; `models.ZeroID()` is the 24-hex sentinel for guest `userId`/unowned `ownerId`; FR-059 rejects ObjectID-like guest names because the frontend message is "Name cannot look like an account ID".
2. Author `docs/design/architecture/adr/ADR-021.md` (next free id) from TEMPLATE.md: platform_identities.id UUIDv7 is the sole account identifier, external_user_id removed; canonical wire form is lowercase hyphenated RFC 9562 UUID; zero identity is the all-zero UUID for guest `userId` and unowned `ownerId`; consolidated references are native uuid columns referencing platform_identities(id) with account_deletion_tombstones carrying the uuid without a foreign key; one guarded backfill then drop legacy columns; no compatibility lookup; old 24-hex sessions force re-sign-in; supersedes ADR-019; addresses.satisfies FR-121; link ADR-018.
3. Update ADR-019 frontmatter (status: superseded, superseded_by: ADR-021, updated_date) and prepend a supersession notice, keeping all existing text.
4. Add the ADR-021 row to the architecture records table in `docs/design/README.md`.
5. Update `docs/requirements/functional/fr/FR-059.md`: the name-rejection rule depends on the account identifier format, so restate the ObjectID-like criterion as rejecting names that resemble the account identifier (UUID form).
6. Update `server/docs/postgres-data-boundaries.md`: point durable decisions at ADR-021 instead of ADR-019 and describe the consolidated native-uuid account references and tombstone-without-FK rule.
7. Verify with `npm run format:markdown:check`, `npm run lint:markdown`, `npm run test:markdown-format`, and `npm run fmt:check` from the repo root; fix any failures; run graphify update after edits.

Risks: keep sentence-per-line authoring and terminology linking; keep ADR-019 text intact apart from the notice; do not change FR-121 (format-agnostic).
<!-- SECTION:PLAN:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Recorded the single account identifier decision as ADR-021.

- Added `docs/design/architecture/adr/ADR-021.md`: `platform_identities.id` (native UUIDv7) is the sole account identifier and `external_user_id` is removed. The ADR fixes the lowercase hyphenated RFC 9562 UUID wire form, the all-zero UUID sentinel for guest `userId` and unowned `ownerId`, native `uuid` consolidated references with `account_deletion_tombstones` carrying the uuid without a foreign key, one guarded backfill with no compatibility lookup, and forced re-sign-in for existing 24-hex sessions. It records the rejected alternatives and keeps `satisfies: FR-121` plus the ADR-018 boundary link.
- Marked ADR-019 superseded by ADR-021 with a supersession notice; its decision text is otherwise unchanged.
- Pointed ADR-018 at the live `platform_identities.id` resolution decision and bumped its updated_date.
- Updated FR-059 to reject names that look like the account identifier in its lowercase hyphenated UUID form, since the frontend rejection message is "Name cannot look like an account ID".
- Updated `server/docs/postgres-data-boundaries.md` with the single account identifier rule and an Accounts subsection for the consolidated native-uuid references, and pointed its durable-decision links at ADR-021 instead of superseded ADR-019.
- Added the ADR-021 row to the architecture records table in `docs/design/README.md`.

Evidence: `npm run format:markdown:check`, `npm run lint:markdown`, `npm run test:markdown-format` (30 passed), and `npm run fmt:check` all pass from the repo root. FR-121 needed no text change because it is format-agnostic; the ADR keeps its traceability. Unit and e2e tests are exempt for this documentation-only change.
<!-- SECTION:FINAL_SUMMARY:END -->
