---
id: TASK-0190.08
title: Rehearse final PostgreSQL cutover and validate retained MongoDB integrations
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-09 21:45'
updated_date: '2026-09-11 08:16'
labels:
  - postgresql
  - migration
dependencies:
  - TASK-0190.07
documentation:
  - BACKLOG_WORKFLOW.md
  - server/docs/postgres-anonymous-event-compatibility.md
modified_files:
  - .github/workflows/backend-ci.yml
  - .github/workflows/e2e-ci.yml
  - DEPLOYMENT.md
  - compose.test.yaml
  - docs/environments.md
  - docs/postgres-staging-rollout.md
  - e2e/helpers/account-auth.ts
  - e2e/specs/timed-event-access-transfer-firefox.spec.ts
  - postgres/init/01-create-roles.sh
  - server/Dockerfile
  - server/docs/postgres-core-migration-runbook.md
  - >-
    server/scripts/20260910_mongo_events_to_postgres/backup_restore_integration_test.go
parent_task_id: TASK-0190
priority: high
type: task
ordinal: 205000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Prepare operational readiness for core-data cutover after default routing and migration stages pass.
Validate in an isolated environment; staging/production deployment and live data migration are separate operational actions.
Update event-backed analytics consumers to follow authoritative event storage while retaining historical logs in MongoDB.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 PostgreSQL backup and restore are exercised against representative migrated data with reconciliation evidence.
- [x] #2 A final isolated end-to-end rehearsal covers account sign-in, every event type, folders, existing links, identity/owner authority, and retained calendar/OTP/friend/log functionality.
- [x] #3 Event-backed analytics correctly include migrated/new events without double counting across stores.
- [x] #4 The staging/production runbook records prerequisites, source-write coordination, validation gates, monitoring, rollback limits, and handling of failed/quarantined records.
- [x] #5 Remaining MongoDB collections and integration-only user fields are explicitly documented with deferred migration scope.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Rehearse the final PostgreSQL cutover and produce operational-readiness evidence. Dependencies TASK-0190.07 and TASK-0199.01-.08 are Done; TASK-0199.06 already delivered the analytics cutover, and TASK-0199.08 retired friend requests, so AC#2's "friend" item is satisfied by retirement evidence rather than a rehearsal. Daily-log reads/writes are PostgreSQL-owned already; only historical backfill (TASK-0199.10) and MongoDB runtime removal (TASK-0199.09) remain deferred.

1. Backup/restore rehearsal (AC#1):
   - Add a `testbase-pg` Dockerfile stage extending `testbase` with `postgresql18-client`; point `server-route-test` at it and update both CI workflow build targets.
   - Add `POSTGRES_BOOTSTRAP_URI` and `POSTGRES_BACKUP_URI` to the `server-route-test` environment in `compose.test.yaml`.
   - Grant `pg_read_all_data` to the backup role in `postgres/init/01-create-roles.sh` and in the test bootstrap command so the least-privilege role can dump existing databases.
   - Add `server/scripts/20260910_mongo_events_to_postgres/backup_restore_integration_test.go`: seed the representative fixture set, run the event/folder migration, `pg_dump --format=custom` through the backup role, `pg_restore` into a fresh scratch database through the bootstrap role, reconcile per-table counts plus ownership/relationship invariants, print a reconciliation report, and drop the scratch database and dump.
2. Final isolated rehearsal (AC#2):
   - Add a "Final Isolated Cutover Rehearsal" section to the runbook with the exact commands and an AC#2 coverage map (sign-in, event types, folders, links, identity/owner authority, calendar/OTP/log, friend retirement).
   - Run the full isolated backend suite (includes account/event/calendar migration rehearsals and analytics tests) and the browser suites: firefox-desktop, chromium desktop/mobile, firefox-touch, and the production pair.
3. Analytics evidence (AC#3): confirm `server/postgres/analytics_test.go` proves single-store counting of migrated and new events, and cite the deleted MongoDB analytics path; add a migrated-event analytics assertion in the migration rehearsal if it can be deterministic.
4. Runbooks (AC#4):
   - Expand `docs/postgres-staging-rollout.md` into the staging/production cutover runbook: prerequisites, source-write coordination and freeze, validation gates, monitoring, rollback limits, failed/quarantined handling, and the backup/restore procedure with rehearsal evidence.
   - Update `server/docs/postgres-core-migration-runbook.md` cutover/retention sections and `DEPLOYMENT.md`/`docs/environments.md` backup wording.
5. Remaining MongoDB scope (AC#5): document the runtime collections (`events`, `eventResponses`, `attendees`, `folders`, `folderEvents`), recovery-only `users`, and historical `dailyuserlogs`, with deferred-removal scope owned by TASK-0199.09 and TASK-0199.10, and record the retained integration field destinations.
6. Verify: isolated backend suite, required e2e projects, Markdown format check, `graphify update .`; tick acceptance criteria with recorded evidence; write the final summary and mark Done.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Progress (session 2026-09-11)

### Backup/restore rehearsal (AC#1)
- Added `testbase-pg` Dockerfile stage with `postgresql18-client`, pointed `server-route-test` at it, and updated both CI workflow build targets.
- Added `POSTGRES_BOOTSTRAP_URI` and `POSTGRES_BACKUP_URI` to `server-route-test` in `compose.test.yaml`.
- Granted `pg_read_all_data` with `INHERIT TRUE` to the backup role in `postgres/init/01-create-roles.sh` and in the test bootstrap command so existing volumes become dumpable too.
- Added `server/scripts/20260910_mongo_events_to_postgres/backup_restore_integration_test.go`: seeds the representative fixture set, migrates events/folders/quarantine, runs `pg_dump --format=custom` through the backup role, restores into a fresh scratch database through the bootstrap role, and reconciles the restored schema table list plus 11 batch-scoped relations by row count and full-row md5 digest. Result: `backup/restore reconciliation passed: 11 relations, dump ~76 KB`.

### Final isolated rehearsal (AC#2)
- Full isolated backend suite `go test ./... -count=1` (compose `server-route-test`): all packages pass, including the account backfill, event backfill, calendar backfill, and the new backup/restore rehearsal.
- Found and fixed two regressions introduced by the interleaved TASK-0199.08 retained-account removal, which had dropped MongoDB profile-name seeding without moving the names to PostgreSQL:
  - `e2e/helpers/account-auth.ts` now sends `firstName`/`lastName` through the authoritative OTP verify payload.
  - `e2e/specs/timed-event-access-transfer-firefox.spec.ts` local `verifySignIn` does the same for the `Transfer Test` account-switch dialog names.
- Browser rehearsal evidence: `firefox-desktop` with `E2E_FRONTEND=bundled`: 56 passed, 1 skipped, 0 failed; `chromium-desktop` + `chromium-mobile` + `firefox-touch`: 55 passed, 20 skipped, 0 failed; `chromium-production-desktop` + `chromium-production-mobile`: 3 passed.
- E2E lint, fmt:check, and typecheck pass after the spec/helper edits.

### Analytics (AC#3)
- TASK-0199.06 already deleted `server/db/analytics.go` and moved both creator aggregations to `postgres_events`; the deterministic temp-table tests in `server/postgres/analytics_test.go` cover the window, creator filter, thresholds, and migrated-plus-new counted once. Full-suite run confirms they pass.

### Remaining
- Runbook updates for AC#4 and the remaining-MongoDB/deferred-scope inventory for AC#5 are next, followed by Markdown formatting, `graphify update .`, and finalization.

### Runbooks and scope (AC#4, AC#5)
- Expanded `docs/postgres-staging-rollout.md` into the full staging/production cutover runbook: prerequisites, deploy, source-write coordination and freeze, validation gates, failed and quarantined record handling, backup/restore procedure with rehearsal evidence, monitoring, rollback limits, smoke test, and the Final Isolated Cutover Rehearsal with commands, coverage map (including event analytics), and recorded results.
- Updated `server/docs/postgres-core-migration-runbook.md` with the backup/restore section, cutover/retention wording, the final rehearsal section, and the Remaining MongoDB Collections And Deferred Scope table with retained integration field destinations and TASK-0199.09/TASK-0199.10 removal ownership.
- Updated `DEPLOYMENT.md` and `docs/environments.md` backup wording: custom-format `pg_dump` through the least-privilege backup role, bootstrap-superuser restore, and isolated reconciliation.

### Verification (continuation)
- Re-ran `TestBackupRestoreRehearsal`: restored schema 24 tables, 11 relations reconciled with matching row counts and md5 digests, dump 75,315 bytes.
- Re-ran `server/postgres/analytics_test.go` (3 tests, all pass): migrated and new PostgreSQL events counted once with no second store.
- `npm run format:markdown:check` passes for all tracked Markdown; `graphify update .` rebuilt the knowledge graph.
- Full isolated backend suite and the required browser projects passed earlier in this task session; changes since were documentation and this runbook evidence only.

### Full-suite verification rerun (2026-09-11)
- Ran the required full isolated backend suite for real with `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test` (default `go test ./... -count=1`) against the current working tree.
- Result: every package `ok` with no failures, including `timeful/server/postgres` (4.630s), `timeful/server/routes` (3.950s), `timeful/server/scripts/20260910_mongo_accounts_to_postgres`, `timeful/server/scripts/20260910_mongo_events_to_postgres` (which contains `TestBackupRestoreRehearsal`), and `timeful/server/scripts/20260911_mongo_calendars_to_postgres`.
- This supersedes the earlier claim that the full suite had passed and closes the finalize-time verification gap; no code changes were made after the run.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Rehearsed the final PostgreSQL core-data cutover in isolation and recorded operational readiness.

## Changes
- Backup/restore (AC#1): added the `testbase-pg` Dockerfile stage with `postgresql18-client`, pointed `server-route-test` and both CI build targets at it, added `POSTGRES_BOOTSTRAP_URI`/`POSTGRES_BACKUP_URI` to the test stack, granted `pg_read_all_data` to the backup role in the production and test bootstraps, and added `server/scripts/20260910_mongo_events_to_postgres/backup_restore_integration_test.go`.
- Final rehearsal (AC#2): the full isolated backend suite passed, and the browser projects passed for firefox-desktop, chromium desktop and mobile, firefox-touch, and the production desktop/mobile pair. Two `e2e` regressions from the retained-account removal were fixed by sending `firstName`/`lastName` through the authoritative OTP verify payload in `e2e/helpers/account-auth.ts` and `e2e/specs/timed-event-access-transfer-firefox.spec.ts`.
- Analytics (AC#3): confirmed TASK-0199.06 moved both creator aggregations to `postgres_events` and deleted the MongoDB `server/db/analytics.go` path; `server/postgres/analytics_test.go` proves migrated and new events are each counted once with no cross-store double counting.
- Runbooks (AC#4): expanded `docs/postgres-staging-rollout.md` into the staging/production cutover runbook with prerequisites, source-write coordination and freeze, validation gates, failed/quarantined handling, backup/restore with rehearsal evidence, monitoring, rollback limits, smoke test, and the isolated rehearsal commands, coverage map, and recorded results; updated `server/docs/postgres-core-migration-runbook.md`, `DEPLOYMENT.md`, and `docs/environments.md`.
- Remaining MongoDB scope (AC#5): documented the retained runtime collections (`events`, `eventResponses`, `attendees`, `folders`, `folderEvents`), recovery-only `users`, historical `dailyuserlogs`, expiring `otpCodes`, and retired `friendrequests`, with removal deferred to TASK-0199.09/TASK-0199.10 and retained integration fields mapped to their PostgreSQL destinations.

## Verification
- `TestBackupRestoreRehearsal`: restored schema 24 tables; 11 representative relations reconciled by row count and full-row md5 (dump 75,315 bytes).
- `server/postgres/analytics_test.go`: all three tests pass.
- Full isolated backend suite (`go test ./... -count=1`) and required e2e projects passed; `npm run format:markdown:check` passes; `graphify update .` completed.

## Follow-ups
MongoDB runtime removal (TASK-0199.09) and historical daily-log backfill (TASK-0199.10) remain deferred and are not part of this cutover.
<!-- SECTION:FINAL_SUMMARY:END -->
