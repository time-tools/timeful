---
id: TASK-0233.01.01
title: >-
  Back up, recreate, and verify the staging and production databases from the
  renamed schema chain
status: Done
assignee:
  - opencode
created_date: '2026-09-14 20:07'
updated_date: '2026-09-15 12:08'
labels:
  - cleanup
  - postgres
  - migrations
  - operations
  - deployment
dependencies:
  - TASK-0233.04
references:
  - docs/postgres-operations.md
  - docs/environments.md
  - DEPLOYMENT.md
  - server/migrations/20260912000000_baseline_schema.sql
documentation:
  - docs/postgres-operations.md
  - docs/environments.md
parent_task_id: TASK-0233.01
priority: high
type: chore
ordinal: 237000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Outcome: staging and production run on the edited migration chain from TASK-0233.01, expose only events and event_responses with no postgres-prefixed relation, constraint, or index, and each has a retained, verified pre-recreate backup with recorded evidence.

Context:
- Staging and production are remote Compose deployments (projects timeful-staging and timeful-production) on the deployment host; this task runs there, not in the local checkout.
- TASK-0233.01 edits the baseline and incremental migrations in place, renaming postgres_events to events, postgres_event_responses to event_responses, and dropping the postgres_ prefix from related constraints and indexes; no rename migration exists, so a database not recreated from the edited chain silently keeps the old schema while the new code expects the new names.
- Pre-rename server binaries are incompatible with the renamed schema, so the renamed server code (TASK-0233.01 through TASK-0233.04) and the edited chain ship in one release, and rollback requires restoring a backup plus the previous release.
- Recreating a database destroys data accrued since the last recreate unless that data is carried over from the backup.

Required work:
- Back up each environment with the custom-format pg_dump procedure in docs/postgres-operations.md, verify each archive by restoring it into a scratch maintenance database and reconciling per-table row counts and key content digests, and retain the verified archives outside the environment data volumes.
- Record per-table row counts and the events_name_length constraint validation state (convalidated from pg_constraint) before recreation.
- Recreate each environment's database from the edited chain by discarding the environment's PostgreSQL data volume and letting postgres-migrate apply the full chain with the renamed release, following docs/environments.md, then carry data over from the verified backup or record the explicitly approved decision to discard it.
- Verify each recreated database has no postgres-prefixed relation, constraint, or index, that events_name_length is validated, and that /api/health reports healthy for the environment.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A verified custom-format backup of staging and of production was created with the docs/postgres-operations.md procedure, restored into a scratch maintenance database for reconciliation, and retained outside the environment data volumes.
- [x] #2 Pre-recreate per-table row counts and the events_name_length validation state are recorded for each environment.
- [x] #3 Staging and production databases were recreated from the edited migration chain, and data carry-over from the verified backup is recorded or an explicit approval to discard the data is recorded.
- [x] #4 Querying pg_class, pg_constraint, and pg_indexes shows no postgres-prefixed relation, constraint, or index in either recreated database, and events_name_length is validated.
- [x] #5 The renamed release and edited chain were deployed together and /api/health reports healthy for staging and production.
- [x] #6 All evidence (backup locations, row counts, constraint state, verification query output, and deployed revision) is recorded in the task.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Execution plan (2026-09-15, host timeful-cloud-ru):

1. Recon + pre-recreate evidence (done): both deployed databases (timeful-postgres-staging, timeful-postgres-production) are on the pre-rename chain (goose through 20260914000000), expose postgres_events/postgres_event_responses, have 0 rows in every data table, and postgres_events_name_length convalidated = t. The renamed release is origin/main 03c34a37; the deployment checkout is at 97b052ed on migrate-from-mongo-to-postgres with origin/main already fetched.
2. Back up each environment with the docs/postgres-operations.md custom-format pg_dump using timeful_backup (re-apply pg_read_all_data per runbook), retain under ~/timeful-backups/renamed-<env>-<ts>/, and verify each archive with pg_restore --list plus a scratch-database restore reconciled per-table against the live database by row counts and row-content digests.
3. User approved recreation with no data carry-over (both databases are empty; the TASK-0224.04 reset was already accepted and nothing accrued since), and approved stopping staging after verification for host memory.
4. Fast-forward the checkout to origin/main 03c34a37 (code and edited migration chain ship together); pre-build images.
5. Staging: down, remove timeful-staging-postgres-data, up -d --build; verify goose applied the full renamed chain, catalog has no postgres-prefixed relation/constraint/index, events_name_length validated, /api/health healthy; then stop staging.
6. Production: same recreate; verify catalog, /api/health (in-container and public), and record deployed revision.
7. Record evidence, check acceptance criteria, finalize TASK-0233.01.01, then finalize parent TASK-0233.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Execution record

Outcome: staging and production run the renamed schema chain on release 03c34a37, expose only events and event_responses with no postgres-prefixed relation, constraint, or index, and each has a retained, verified pre-recreate backup with recorded evidence.

### Pre-recreate state (before any destructive step)

- Both timeful-postgres-staging and timeful-postgres-production were on the pre-rename chain: goose_db_version = 0, 20260912000000, 20260913000000, 20260913000001, 20260913000002, 20260914000000; postgres_events and postgres_event_responses present; 11 postgres-prefixed relations, 35 constraints, and 9 indexes; postgres_events_name_length convalidated = t.
- Per-table row counts: every data table 0 rows in both databases; only goose_db_version had rows (6). Both databases were recreated from the baseline by TASK-0224.04 on 2026-09-14, whose accepted reset discarded the pre-baseline rows, and nothing accrued since.
- Backup role re-granted per the runbook: `GRANT pg_read_all_data TO timeful_backup WITH INHERIT TRUE` (already granted; notice recorded).
- Full pre-recreate catalogs: renamed-staging-20260915T115455Z/pre-recreate-catalog.txt and renamed-production-20260915T115506Z/pre-recreate-catalog.txt.

### Verified backups (retained outside the environment data volumes)

- staging: ~/timeful-backups/renamed-staging-20260915T115455Z/timeful-staging-20260915T115455Z.dump, 73,448 bytes, sha256 be40d7138f0ab7d26c15f82d42ed798d39f4d35d5c178e9fcbb05dbd5ac1d8ad; `pg_restore --list` produced 183 TOC entries; restored into scratch database timeful-restore-check with `pg_restore --no-owner --exit-on-error` as the bootstrap superuser; per-table row counts and md5 row-content digests reconciled MATCH (reconcile.diff empty).
- production: ~/timeful-backups/renamed-production-20260915T115506Z/timeful-production-20260915T115506Z.dump, 73,472 bytes, sha256 20b97eeb892c2a93f9467a401848ca706855238eee9e4ad9081b368529b129a6; 183 TOC entries; scratch restore reconciled MATCH.
- Each directory also retains the .sha256 file, toc.txt, scratch-restore.txt, reconcile-live.txt, reconcile-restore.txt, and PROVENANCE.txt.

### Approval

- The user explicitly approved recreating both databases with no data carry-over (both empty, prior reset already accepted) and stopping staging after verification for host memory.

### Deployed release and recreate

- Checkout fast-forwarded from 97b052ed (migrate-from-mongo-to-postgres) to origin/main 03c34a37 on branch main, so the renamed server code and the edited migration chain shipped together; worktree clean.
- staging: `down`, removed timeful-staging-postgres-data, `up -d` with pre-built images; postgres-migrate logged `goose: successfully migrated database to version: 20260915000000` at 11:59:37Z.
- production: same procedure (images pre-built while production still running); migration log at 12:06:16Z applied the same chain through 20260915000000.
- No data carry-over was performed.

### Post-recreate verification (both databases)

- events and event_responses present; 0 postgres-prefixed relations, constraints, and indexes in pg_class/pg_constraint/pg_indexes; goose_db_version has 7 rows through 20260915000000.
- All 15 contract names present: constraints events_short_id_format, events_type, events_payload_object, events_num_responses_check, events_owner_edit_token_hash_check, events_owner_visitor_fk, events_name_length, event_responses_kind, event_responses_payload_object, event_responses_visitor_event_fk; indexes events_owner_platform_idx, events_creator_created_at_idx, event_responses_event_created_idx, event_responses_visitor_idx, event_responses_platform_identity_idx.
- events_name_length convalidated = t; every data table 0 rows.
- Health: staging `http://127.0.0.1:3004/api/health` -> {"status":"ok"} and `https://staging.timeful.fun/api/health` -> {"status":"ok"}, then staging was stopped per approval; production `http://127.0.0.1:3005/api/health` -> {"status":"ok"}, `https://timeful.fun/api/health` -> HTTP 200, `https://timeful.fun/` -> HTTP 200.
- timeful_backup can read `events` on the recreated production cluster.

### Evidence locations

- Remote: ~/timeful-backups/renamed-staging-20260915T115455Z/, ~/timeful-backups/renamed-production-20260915T115506Z/, and ~/timeful-backups/task-0233.01.01-evidence/ (deployment.txt, staging-health.txt, production-health.txt, both post-recreate catalogs, migration logs, ps snapshots, backup-role grant).
- Local session copies: /tmp/opencode/0233/.
- Deployed revision: 03c34a37e981c1953138e6f52816030d7d9c3b43 (branch main); both postgres data volumes were recreated and contain no pre-rename object.

No repository code or documentation changed, so the unit and e2e DoD items carry the release-verification evidence from TASK-0233.01/02/03/04; `npm run format:markdown:check` passes on the unchanged tree.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Recreated and verified staging and production from the renamed migration chain on release 03c34a37, with retained verified pre-recreate backups.

Pre-recreate (2026-09-15): both deployed databases were still on the pre-rename chain (goose 0, 20260912000000-20260914000000; postgres_events/postgres_event_responses; 11 prefixed relations, 35 constraints, 9 indexes; postgres_events_name_length validated), with every data table at 0 rows; only goose_db_version had rows (6). Staging and production had last been recreated from the baseline by TASK-0224.04, whose reset was already accepted.

Backups: each environment was dumped with the runbook custom-format procedure using the least-privilege timeful_backup role (pg_read_all_data re-granted first) and retained outside the data volumes under ~/timeful-backups/renamed-staging-20260915T115455Z/ and ~/timeful-backups/renamed-production-20260915T115506Z/. Staging sha256 be40d7138f0ab7d26c15f82d42ed798d39f4d35d5c178e9fcbb05dbd5ac1d8ad and production sha256 20b97eeb892c2a93f9467a401848ca706855238eee9e4ad9081b368529b129a6, each with 183 TOC entries and a scratch-database restore (timeful-restore-check) whose per-table row-count and md5 row-content digests reconciled exactly (reconcile.diff empty).

Recreate: the checkout was fast-forwarded to origin/main 03c34a37 so the renamed code and edited chain shipped together; images were pre-built; each environment's postgres data volume was removed and the stack restarted. postgres-migrate applied the full renamed chain through 20260915000000 at 11:59:37Z (staging) and 12:06:16Z (production). The user explicitly approved recreation without data carry-over since both databases were empty.

Post-recreate: both databases expose events and event_responses with 0 postgres-prefixed relations, constraints, and indexes; all 15 renamed constraint/index contract names are present; events_name_length convalidated = t; goose_db_version has 7 rows; all data tables 0 rows. Health: staging local and https://staging.timeful.fun/api/health both {"status":"ok"} (staging then stopped per approval); production {"status":"ok"} locally with HTTP 200 on https://timeful.fun/api/health and https://timeful.fun/.

Evidence: ~/timeful-backups/renamed-{staging,production}-20260915T*/ and ~/timeful-backups/task-0233.01.01-evidence/ (deployment revision, migration logs, pre/post-recreate catalogs, health, backup-role grant), with session copies under /tmp/opencode/0233/. No repository changes were made by this task; the deployed revision is 03c34a37e981c1953138e6f52816030d7d9c3b43 on branch main.
<!-- SECTION:FINAL_SUMMARY:END -->
