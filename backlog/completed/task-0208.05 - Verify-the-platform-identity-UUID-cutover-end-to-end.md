---
id: TASK-0208.05
title: Verify the platform identity UUID cutover end to end
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-11 21:03'
updated_date: '2026-09-12 20:12'
labels: []
dependencies:
  - TASK-0208.02
  - TASK-0208.03
  - TASK-0208.04
documentation:
  - AGENTS.md
  - compose.test.yaml
  - e2e/package.json
  - e2e/helpers/account-auth.ts
  - server/docs/postgres-data-boundaries.md
modified_files:
  - e2e/specs/timed-event-calendar-integration-firefox.spec.ts
  - e2e/specs/timed-event-signup-postgres-firefox.spec.ts
parent_task_id: TASK-0208
priority: medium
type: task
ordinal: 252000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Verify the consolidated account identity across the isolated test stack: a fresh sign-in receives a uuidv7 identifier, a session carrying an old 24-hex value resolves to no account, and signed-in journeys (profile, calendar integration, dashboard, folders, groups, signup forms, access transfers, account deletion) work against the consolidated schema. Produce rehearsal evidence for the one-time backfill over existing rows and confirm no 24-hex account reference remains in columns, stored payloads, or API responses. Depends on the backend boundary, schema consolidation, and frontend subtasks.

Constraints:
- Use the isolated test stack per AGENTS.md; never target a development database.
- Playwright runs from e2e/ with --project=firefox-desktop and owns the stack and Vite process.
- Keep test state retention behavior per the repo's server test and E2E workflows.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The isolated backend suite passes, including the account repository and account deletion packages
- [x] #2 Firefox desktop E2E passes for sign-in, profile, calendar connect/refresh/toggle/remove, dashboard, folders, groups, signup forms, access transfers, and account deletion
- [x] #3 Backfill rehearsal over representative existing rows rewrites every reference with zero orphans, and a repeated run is a no-op
- [x] #4 Grep, SQL, and API evidence shows no remaining 24-hex account identifier in runtime code, schema columns, or responses, and guest zero identity still resolves
- [x] #5 A recorded reconciliation of TASK-0177.01 exists, either as a dependency or an update to that task
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Fix the two stale legacy-column e2e queries recorded in this task's comment: in timed-event-calendar-integration-firefox.spec.ts replace the platform_identities.external_user_id filters with platform_identities.id (the sign-in userId is now the platform identity UUID), and in timed-event-signup-postgres-firefox.spec.ts select platform_identity_id from event_signup_responses.
2. Confirm every other e2e helper/spec already consumes the UUID account identifier (account-auth returns profile._id, which is now the platform identity UUID) and no legacy 24-hex reference remains in e2e/.
3. Run the isolated backend suite via compose.test.yaml.
4. Run the Firefox desktop Playwright suite across the sign-in, profile, calendar, dashboard, folders, groups, signup, access-transfer, and account-deletion journeys.
5. Gather AC evidence: grep/SQL/API checks for the retired 24-hex identifier and guest zero identity, plus the migration rehearsal and repeated-run no-op evidence from account_identity_migration_test.go.
6. Record the TASK-0177.01 reconciliation and finalize TASK-0208.05 and parent TASK-0208 when all evidence is green.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
User accepted the 3 access-transfer approval timeouts as known heavy journeys and asked not to re-run them; the other 53 Firefox desktop tests passed.

The backend test database was recreated by the focused evidence run after the Playwright teardown removed the shared test stack volume; goose reapplied 20260912120000 and 20260912130000 cleanly.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
author: opencode
created: 2026-09-12 19:15
---
Handoff input from the TASK-0208.02/.03 review (see TASK-0208.06): two Firefox specs still query the dropped legacy columns and will fail before any product assertion runs. `e2e/specs/timed-event-calendar-integration-firefox.spec.ts` uses `p.external_user_id = '${userId}'` in its calendar query helpers, and `e2e/specs/timed-event-signup-postgres-firefox.spec.ts` line 186 selects `account_user_id` from `event_signup_responses`. Both should read `platform_identity_id` from `platform_identities`/`event_signup_responses`.
---
<!-- COMMENTS:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Verified the platform-identity UUID cutover end to end on the isolated test stack.

Delivered:
- Fixed the two stale legacy-column e2e queries: e2e/specs/timed-event-calendar-integration-firefox.spec.ts now filters platform_identities.id, and e2e/specs/timed-event-signup-postgres-firefox.spec.ts selects event_signup_responses.platform_identity_id.
- Confirmed the remaining e2e helpers already consume the UUID account identifier: account-auth.ts returns the sign-in profile _id, which is the platform identity UUID.

Evidence:
- Isolated backend suite passes: docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test reports all packages ok, including accounts, postgres, routes, and services/auth.
- Firefox desktop suite: 53 passed, 1 skipped, and the 3 access-transfer approval journeys (guest/owner/signed-in) timed out at the 30-second test budget; the other 5 access-transfer tests pass, along with sign-in, profile, calendar connect/refresh/toggle/remove, dashboard, folders, groups, signup forms, owner authority, and visitor identity journeys. Per user direction the timeouts are accepted and were not re-run; bundled mode is the documented remedy for the heavy approval journeys.
- Account deletion: the spec is Chromium-only, so --project=chromium-desktop specs/account-deletion.spec.ts was run: 2 passed (deletion requires the account email and signs the visitor out; re-sign-in mints a fresh account and identity).
- e2e package lint, fmt:check, and typecheck pass after the spec fixes.
- Migration rehearsal: TestAccountIdentityMigrationBackfillsLegacyReferences rewrites every representative reference with zero orphans, clears unreachable tombstones, drops all ten legacy columns, and applies both migrations a second time as a no-op; TestAccountIdentityMigrationRejectsUnmappedReferences and TestAccountIdentityMigrationDownRefuses pass.
- SQL: 0 legacy columns remain in the migrated test database, 12 platform_identity_id uuid columns exist, and platform_identities.id is uuid with the uuidv7() default.
- Grep: no runtime Go source references external_user_id/account_user_id/owner_external_id, no frontend source imports bson-objectid or matches a 24-hex account id, and the remaining 24-hex literals are tests proving the retired form is rejected.
- Guest zero identity: TestAnonymousTimedEventCompatibilityContract/postgres asserts the zero UUID userId, and TestSignedInDashboardListsPostgresOwnedAndResponded asserts the zero UUID ownerId.
- API sign-in: the passing e2e calendar and signup joins match platform_identities.id/event_signup_responses.platform_identity_id to the sign-in _id; TestAccountOtpSignInUsesPostgresAuthority proves first and repeated sign-in reuse one account and identity.
- TASK-0177.01 is Done with a dependency on TASK-0208.04 and its bson-objectid removal reconciliation recorded in its comments and final summary.

Risks and follow-ups:
- The 3 timed-out access-transfer approval journeys remain over budget with the unbundled dev server; run them with E2E_FRONTEND=bundled if they need to be green.
- Frontend required checks were re-run for the staged change set and pass (1090 tests across 146 files).
<!-- SECTION:FINAL_SUMMARY:END -->
