---
id: TASK-0199.09.04
title: 'Remove MongoDB Compose, environment, CI, and E2E configuration'
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-11 08:30'
updated_date: '2026-09-11 13:38'
labels:
  - postgresql
  - migration
dependencies:
  - TASK-0199.09.01
  - TASK-0199.09.03
references:
  - compose.yaml
  - compose.test.yaml
  - compose.development.yaml
  - compose.staging.yaml
  - compose.production.yaml
  - e2e/isolated-test-stack.ts
  - e2e/helpers/mongo-inspect.ts
  - .github/workflows/backend-ci.yml
  - .github/workflows/e2e-ci.yml
  - docs/environments.md
parent_task_id: TASK-0199.09
ordinal: 234000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Remove MongoDB from local, test, staging, and production configuration and from the browser E2E stack.

Remove the mongo and mongo-test services, volumes, health checks, depends_on entries, and MONGODB_* environment from compose.yaml and every overlay (development, test, staging, production, edge), and remove the ./mongo/init mount.
Remove MONGODB_* values from tracked .env.*.example files and local env files, and delete mongo/ init scripts and scripts/mongo bootstrap tooling.
Update CI workflows so mongo-test is not started, mongo/** path filters are removed, and step names and failure-log collection describe a PostgreSQL-only stack.
Update the E2E isolated stack to start only postgres-test, postgres-test-bootstrap, and server-test; delete e2e/helpers/mongo-inspect.ts and remove the retained-document assertion steps from the calendar integration spec; update e2e/AGENTS.md.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Compose config validates with no MongoDB service, volume, health check, depends_on entry, or MONGODB_* variable in any overlay.
- [x] #2 Tracked env examples, CI workflows, and the E2E isolated stack no longer start mongo-test or reference MongoDB variables.
- [x] #3 mongo/ and scripts/mongo/ are deleted and no tracked file references them.
- [x] #4 E2E helpers and specs no longer inspect MongoDB, and e2e lint, fmt:check, and typecheck pass.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
## Implementation Plan

Dependencies TASK-0199.09.01 and TASK-0199.09.03 are Done; the mongo runtime, `server/db`, and the driver are already gone.

### Scope boundary (user-confirmed)
Execute only the work described in this task: Compose, tracked and local env files, `mongo/`, `scripts/mongo/`, CI workflows, the E2E isolated stack, `e2e/helpers/mongo-inspect.ts`, the calendar integration spec, and `e2e/AGENTS.md`.
Root `AGENTS.md`, `DEPLOYMENT.md`, `docs/ci.md`, `docs/environments.md`, and `server/README.md` keep their remaining MongoDB references and are completed by TASK-0199.09.05; record that in the final summary rather than expanding scope here.

### Work items
1. `compose.yaml`: delete the `mongo` service and `mongo_data` volume; drop `MONGODB_DATABASE`/`MONGODB_URI` from `server` environment; drop the `mongo` entry from `server.depends_on`.
2. `compose.development.yaml`: drop the `mongo` entry from `server.depends_on` (`!reset` list keeps only `postgres-migrate`).
3. `compose.test.yaml`: delete the `mongo-test` service and `mongo_test_data` volume; drop `MONGODB_URI`/`MONGODB_DATABASE` from `server-route-test`; drop `mongo-test` from `server-test` and `server-route-test` `depends_on`.
4. `compose.staging.yaml` / `compose.production.yaml`: delete the whole `mongo` service override block (environment, `./mongo/init` mount, healthcheck). `compose.edge*.yaml` have no MongoDB references.
5. Env files: remove every `MONGODB_*` value and MongoDB wording from `.env.development.example`, `.env.test.example`, `.env.staging.example`, `.env.production.example`; update the untracked local `.env.development` and `.env.test` the same way.
6. Delete `mongo/init/01-create-users.js`, `mongo/migration/create-users.js`, and `scripts/mongo/bootstrap-existing-users.sh` (git rm, including now-empty directories).
7. CI: `backend-ci.yml` drops the two `mongo/**` path filters; both workflows rename the Compose-validation and service-start steps to PostgreSQL-only and remove `mongo-test` from the `up -d` list.
8. E2E: `e2e/isolated-test-stack.ts` starts only `postgres-test`, `postgres-test-bootstrap`, `server-test`; delete `e2e/helpers/mongo-inspect.ts` and its four retained-document `test.step` blocks plus import in `timed-event-calendar-integration-firefox.spec.ts`; update the `e2e/AGENTS.md` stack list.

### Verification
- `docker compose --env-file .env.test.example -f compose.yaml -f compose.test.yaml config --quiet` and dev/staging/production overlay renders with stub env values, proving no MongoDB service, volume, healthcheck, `depends_on`, or `MONGODB_*` variable remains.
- Greps: no `mongo`/`MONGODB_` in Compose, tracked env examples, workflows, or E2E; AC3 grep plus an explicit list of remaining `.05`-owned doc references.
- Backend suite via the isolated Compose stack (PostgreSQL only) to prove the stack still starts without `mongo-test`.
- E2E `lint`, `fmt:check`, `typecheck`, and a focused calendar-integration Firefox spec; then full Firefox desktop.
- `npm run format:markdown` for `e2e/AGENTS.md`; `graphify update .`.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Implementation (2026-09-11, session 1)

Scoped to the task's own surfaces per user confirmation: Compose, tracked and local env files, `mongo/`, `scripts/mongo/`, CI workflows, and the E2E stack/helpers/specs. Root `AGENTS.md`, `DEPLOYMENT.md`, `docs/ci.md`, `docs/environments.md`, and `server/README.md` remain TASK-0199.09.05 scope.

- Compose: deleted the `mongo`/`mongo-test` services, `mongo_data`/`mongo_test_data` volumes, healthchecks, `./mongo/init` mounts, every `depends_on: mongo*` entry, and `MONGODB_URI`/`MONGODB_DATABASE` from `compose.yaml`, `compose.development.yaml`, `compose.test.yaml`, `compose.staging.yaml`, and `compose.production.yaml`. `compose.edge*.yaml` had no MongoDB references.
- Env: removed all `MONGODB_*` values and MongoDB wording from `.env.development.example`, `.env.test.example`, `.env.staging.example`, `.env.production.example`, plus the untracked local `.env.development` and `.env.test`.
- Deleted `mongo/init/01-create-users.js`, `mongo/migration/create-users.js`, and `scripts/mongo/bootstrap-existing-users.sh`.
- CI: removed `mongo/**` path filters from `backend-ci.yml`; renamed the Compose-validation step to "Validate PostgreSQL-only Compose configuration" and the start step to "Start PostgreSQL test services" in both workflows; dropped `mongo-test` from both `up -d` commands.
- E2E: `isolated-test-stack.ts` now starts only `postgres-test`, `postgres-test-bootstrap`, and `server-test`; deleted `e2e/helpers/mongo-inspect.ts`; removed the four retained-document `test.step` blocks and the now-unused `email` binding from the calendar integration spec; updated the stack list in `e2e/AGENTS.md`; removed a stale retained-MongoDB comment in `e2e/helpers/account-auth.ts`.

## Verification

- AC1: `docker compose config --quiet` passes for the test, development, staging, and production overlays and for both edge overlays; rendered configs for the four server overlays have zero case-insensitive `mongo` matches.
- AC2: `rg -i mongo` over the Compose files, all tracked `.env.*.example` files, `.github/workflows/`, and the E2E stack/spec/AGENTS.md returns no hits; local `.env.test`/`.env.development` are clean.
- AC3: the three tracked `mongo/` and `scripts/mongo/` files are deleted. The only remaining tracked references to the deleted paths are `docs/environments.md` and `DEPLOYMENT.md`, explicitly deferred to TASK-0199.09.05 by the user's scope decision, so no operational, config, CI, or E2E file references them.
- AC4: `e2e/helpers/mongo-inspect.ts` is deleted and no E2E file inspects MongoDB; `npm run lint`, `npm run fmt:check`, and `npm run typecheck` pass in `e2e/`; the focused calendar-integration Firefox spec passes 4/4 on the PostgreSQL-only stack.
- DoD #2: `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test` exits 0 with every package `ok` (`/tmp/opencode/task-0199.09.04-backend.log`).
- DoD #3: focused calendar spec 4/4 (`/tmp/opencode/task-0199.09.04-e2e-calendar.log`); full Firefox desktop at two workers with `E2E_FRONTEND=bundled` reported 53 passed, 1 skipped, and three `timed-event-access-transfer` 30s timeouts waiting for "Continue after approval" (`/tmp/opencode/task-0199.09.04-e2e-firefox.log`); those three pass in isolation at one worker with `--timeout=60000` in 15.3-16.3s (`/tmp/opencode/task-0199.09.04-e2e-transfer-rerun.log`), matching the host-load flake recorded in TASK-0199.09.02/.03. The user declined a second full rerun.
- DoD #4: `npm run format:markdown` and `npm run format:markdown:check` pass.
- `graphify update .`: 5568 nodes, 9776 edges, 405 communities.

## Behavior changes

- The isolated E2E stack starts only PostgreSQL services; `mongo-test` no longer exists in Compose or CI.
- Compose and tracked env examples no longer accept or require `MONGODB_*`; Backend CI no longer triggers on `mongo/**`.
- The calendar integration spec no longer asserts the removed retained-MongoDB non-authority condition.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Removed MongoDB from local, test, staging, production, and browser E2E configuration.

Delivered:
- `compose.yaml` and the development/test/staging/production overlays no longer define `mongo`/`mongo-test` services, volumes, healthchecks, `./mongo/init` mounts, `depends_on` entries, or `MONGODB_*` variables; every overlay (including both edge files) renders with `docker compose config --quiet`.
- All `MONGODB_*` values and MongoDB wording are gone from the tracked `.env.*.example` files and the local untracked `.env.test`/`.env.development`.
- Deleted `mongo/init/01-create-users.js`, `mongo/migration/create-users.js`, and `scripts/mongo/bootstrap-existing-users.sh`.
- Backend CI and E2E CI drop the `mongo/**` path filter, remove `mongo-test` from the start commands, and use PostgreSQL-only step names.
- The E2E isolated stack starts only `postgres-test`, `postgres-test-bootstrap`, and `server-test`; `e2e/helpers/mongo-inspect.ts` and the four retained-document assertions are deleted, and `e2e/AGENTS.md` is updated.

Evidence: PostgreSQL-only Compose validation for every overlay; backend suite exits 0 with every package `ok`; focused calendar-integration Firefox spec 4/4; e2e `lint`/`fmt:check`/`typecheck` pass; Markdown format checks pass; `graphify update .` completed. The full Firefox desktop run reported 53 passed, 1 skipped, and 3 known host-load 30s timeouts in the access-transfer spec, which all pass in isolation at one worker in ~16s.

Scope note: root `AGENTS.md`, `DEPLOYMENT.md`, `docs/ci.md`, `docs/environments.md`, and `server/README.md` still reference MongoDB services or the deleted `mongo/`/`scripts/mongo` paths; those are TASK-0199.09.05 scope per the user-confirmed scope decision for this task. No commit was made.
<!-- SECTION:FINAL_SUMMARY:END -->
