---
id: TASK-0233.01
title: >-
  Rename the schema objects and migration files and recreate the development and
  test databases
status: Done
assignee:
  - opencode
created_date: '2026-09-14 19:00'
updated_date: '2026-09-14 22:29'
labels:
  - cleanup
  - postgres
  - schema
  - migrations
dependencies: []
references:
  - server/migrations/20260912000000_baseline_schema.sql
  - server/migrations/20260913000001_supporting_indexes.sql
  - server/migrations/20260913000002_events_name_length.sql
  - server/migrations/20260914000000_validate_events_name_length.sql
  - >-
    server/migrations/20260915000000_event_responses_respondent_kind_not_null.sql
documentation:
  - docs/postgres-operations.md
  - server/docs/postgres-event-api-contract.md
  - server/docs/postgres-data-boundaries.md
parent_task_id: TASK-0233
priority: high
type: chore
ordinal: 233000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Outcome: the schema and migration chain stop carrying the migration-era postgres qualifier, and the development and isolated test databases are recreated from the edited chain so no old names remain.
The staging and production recreate after verified backups is tracked separately by subtask TASK-0233.01.01.

Required names (contract):
- tables: postgres_events -> events, postgres_event_responses -> event_responses
- constraints: drop the postgres_ prefix (postgres_events_short_id_format -> events_short_id_format, postgres_events_type -> events_type, postgres_events_payload_object -> events_payload_object, postgres_events_num_responses_check -> events_num_responses_check, postgres_events_owner_edit_token_hash_check -> events_owner_edit_token_hash_check, postgres_events_owner_visitor_fk -> events_owner_visitor_fk, postgres_events_name_length -> events_name_length, postgres_event_responses_kind -> event_responses_kind, postgres_event_responses_payload_object -> event_responses_payload_object), with the composite owner foreign key renamed to event_responses_visitor_event_fk
- indexes: drop the postgres_ prefix (postgres_events_owner_platform_idx -> events_owner_platform_idx, postgres_events_creator_created_at_idx -> events_creator_created_at_idx, postgres_event_responses_event_created_idx -> event_responses_event_created_idx, postgres_response_visitor_idx -> event_responses_visitor_idx, postgres_response_platform_identity_idx -> event_responses_platform_identity_idx, plus the retired index names that appear in Down paths)

Confirmed constraints:
- Edit server/migrations/20260912000000_baseline_schema.sql and the existing incremental migrations in place; do not add a rename migration.
- Rename migration files that contain postgres in their names while keeping goose version prefixes unchanged, and update Go test constants that reference those file names.
- Recreate the development and isolated test databases from the edited chain; TASK-0233.01.01 owns the staging and production recreate, including the verified retained backups and the recorded row counts and constraint validation state.
- Update raw SQL, constraint-name assertions, and application-role probes under server/postgres/** and server/routes/** tests.
- Update live docs that name the objects: docs/postgres-operations.md, server/docs/postgres-event-api-contract.md, server/docs/postgres-data-boundaries.md.
- Do not edit historical ADRs, handoffs, or completed task records.

The parent task records the full decisions, allowlist, and risks.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The baseline and every incremental migration create only events and event_responses and name every related constraint and index without a postgres prefix.
- [x] #2 Migration files containing postgres in their names are renamed with unchanged goose version prefixes, and Go test constants that reference those file names are updated.
- [x] #3 No raw SQL in server code or tests references postgres_events, postgres_event_responses, or a postgres-prefixed constraint or index.
- [x] #4 The development and isolated test databases were recreated from the edited chain and contain no postgres-prefixed relation, constraint, or index.
- [x] #5 The backend schema, repository, and migration test packages pass in the isolated Compose stack.
- [x] #6 docs/postgres-operations.md, server/docs/postgres-event-api-contract.md, and server/docs/postgres-data-boundaries.md use the new object names.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Rename schema objects in the baseline and incremental migrations and drop the postgres_ prefix from related constraints and indexes; rename the three postgres-named migration files with unchanged goose version prefixes and update the Go constants that name them.
2. Update raw SQL, constraint-name assertions, and application-role probes under server/postgres/** and server/routes/** tests to the new object names.
3. Update docs/postgres-operations.md, server/docs/postgres-event-api-contract.md, and server/docs/postgres-data-boundaries.md.
4. Recreate the development database and the isolated test database from the edited chain; confirm via pg_class, pg_constraint, and pg_indexes that no postgres-prefixed relation, constraint, or index remains.
5. Run the backend suite in the isolated Compose stack and format the changed Markdown.
6. Record evidence, check acceptance criteria, and finalize; TASK-0233.01.01 owns staging and production.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Status

Implementation is functionally complete; the task is intentionally left In Progress for a final verification/finalization pass by a later session.

## What changed

- Migrations edited in place (no rename migration) and three files renamed with goose version prefixes unchanged:
  - server/migrations/20260913000002_events_name_length.sql (was ..._postgres_events_name_length.sql)
  - server/migrations/20260914000000_validate_events_name_length.sql (was ..._validate_postgres_events_name_length.sql)
  - server/migrations/20260915000000_event_responses_respondent_kind_not_null.sql (was ..._postgres_event_responses_respondent_kind_not_null.sql)
- Object contract now in effect: tables events and event_responses; constraints events_short_id_format, events_type, events_payload_object, events_num_responses_check, events_owner_edit_token_hash_check, events_owner_visitor_fk, events_name_length, event_responses_kind, event_responses_payload_object, event_responses_visitor_event_fk; indexes events_owner_platform_idx, events_creator_created_at_idx, event_responses_event_created_idx, event_responses_visitor_idx, event_responses_platform_identity_idx, plus the retired events_active_creator_posthog_id_idx and event_responses_event_id_idx in the supporting-indexes Down path.
- All server Go files: postgres_events to events and postgres_event_responses to event_responses (SQL, constraint/index assertions, migration-name constants, comments). No postgres_ substring remains under server/** (verified with ripgrep).
- Live docs updated: docs/postgres-operations.md, server/docs/postgres-event-api-contract.md, server/docs/postgres-data-boundaries.md.
- npm run format:markdown was run; it reformatted server/docs/postgres-data-boundaries.md.

## Database recreation evidence

Development (project timeful-development):
- Pre-recreate custom-format backup retained at /tmp/opencode/timeful-development-pre-rename.dump (73 KB; pre-recreate dev held 0 events, 0 event_responses, events_name_length validated).
- Recreated with compose down, docker volume rm timeful-development_postgres_data, then compose up -d --build postgres postgres-migrate server; goose applied all six migrations through 20260915000000.
- Catalog verification: 0 postgres-prefixed relations, constraints, and indexes; events and event_responses present; events_name_length convalidated = t; every constraint and index name matches the contract; GET /api/health returned {"status":"ok"}.

Isolated test (project timeful-test):
- Gotcha for the next session: docker compose run reuses the cached postgres-test-migrate image, so rebuild it (docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml build postgres-test-migrate) after any migration change or the old chain is applied.
- Recreated with down -v, migrator rebuild, up -d postgres-test, then run --rm server-route-test.
- Catalog verification on timeful-test-postgres: 0 postgres-prefixed relations, constraints, and indexes; events and event_responses present; events_name_length convalidated = t.
- Full go test ./... -count=1 passed, including timeful/server/postgres and timeful/server/routes.
- timeful-test-postgres-test-1 is currently up and test state is retained (no down -v after the passing run).

## Remaining work and next steps

- e2e Firefox specs still query postgres_events directly; TASK-0233.03 owns the e2e helper/spec SQL and file renames, so e2e is expected to fail until that task lands. This task's DoD e2e item cannot pass in isolation.
- Staging and production recreation is TASK-0233.01.01 (depends on TASK-0233.04).
- Final pass: re-run the catalog checks if desired, verify acceptance criteria, record a final summary, and move the task to Done.
- backlog/backlog.md and task-0178 have pre-existing uncommitted edits from earlier work; do not revert them. No commit was made in this session.

## Review verification (2026-09-15)

Independent catalog queries via docker exec psql found 0 relations, constraints, and indexes matching 'postgres_%' in both recreated databases, and each contains only events and event_responses.

Development (timeful-development): events_name_length convalidated = t; goose applied versions 0, 20260912000000, 20260913000000, 20260913000001, 20260913000002, 20260914000000, 20260915000000; the events/event_responses indexes are events_creator_created_at_idx, events_owner_platform_idx, events_pkey, events_short_id_key, event_responses_event_created_idx, event_responses_platform_identity_idx, event_responses_pkey, event_responses_public_id_key, and event_responses_visitor_idx.

Isolated test (timeful-test-postgres): events_name_length convalidated = t; all six goose versions applied; named constraints include events_short_id_format, events_type, events_payload_object, events_num_responses_check, events_owner_edit_token_hash_check, events_owner_visitor_fk, events_name_length, event_responses_kind, event_responses_payload_object, and event_responses_visitor_event_fk.

Also verified during review: go build ./... passes in server/, npm run format:markdown:check passes, and the only remaining postgres_events/postgres_event_responses references are the e2e spec SQL owned by TASK-0233.03 plus historical ADR and completed-task records.

Playwright teardown runs docker compose down -v unless TEST_DB_PERSIST=true, so run any remaining catalog checks before the e2e final pass; the retained timeful-test stack is not a durable artifact.

## Finalization verification (2026-09-15)

- Catalog queries (docker exec psql) on the development database (timeful-postgres-development) and on a freshly migrated isolated test database (timeful-test-postgres): 0 pg_class, pg_constraint, and pg_indexes entries matching '^postgres_'; only events and event_responses; events_name_length convalidated = t; all six goose versions applied (0, 20260912000000, 20260913000000, 20260913000001, 20260913000002, 20260914000000, 20260915000000).
- docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test passed twice: full go test ./... including timeful/server/postgres (6.4s / 7.9s) and timeful/server/routes (2.5s / 3.2s). Logs: /tmp/opencode/task-0233.01-backend-suite.log and /tmp/opencode/task-0233.01-backend-suite-final.log.
- E2E_FRONTEND=bundled firefox-desktop at two workers: 54 passed, 1 skipped, 2 failures, both the documented 30s budget flakes in timed-event-access-transfer-firefox.spec.ts (guest and owner approval). Both pass in isolation (28.9s / 30.7s) and both passed first in a sequential --workers=1 run that the user halted after test 21. The renamed specs' direct SQL assertions ran against events and event_responses. Logs: /tmp/opencode/task-0233.01-firefox-desktop.log, /tmp/opencode/task-0233.01-approval-isolated.log, /tmp/opencode/task-0233.01-firefox-desktop-workers1.log.
- Repo-wide ripgrep for postgres_events, postgres_event_responses, and postgres-prefixed constraint/index names finds no live reference. npm run format:markdown:check passes at the repo root.
- No commit was made in this session; backlog/backlog.md and task-0178 retain their pre-existing uncommitted edits.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
author: opencode
created: 2026-09-14 21:52
---
Review note (2026-09-15): finalization sequencing. The staged schema rename is verified consistent (catalog checks pass in both recreated databases), but DoD #3 (e2e) cannot pass until TASK-0233.03 updates the spec SQL, which still queries postgres_events. Keep this task In Progress until TASK-0233.03 lands and the firefox-desktop project passes against the recreated chain, then check DoD #3 and finalize. A red firefox-desktop run before TASK-0233.03 is expected and is not evidence of a schema regression. The test stack migrator image was rebuilt at 23:54 on 2026-09-14, after the 23:21-23:22 migration edits, so the e2e stack applies the renamed chain; rebuild it again only if migrations change.
---
<!-- COMMENTS:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Finalized TASK-0233.01: the schema rename, migration-file renames, database recreates, and live-doc updates committed in 5c57b4b5 are verified against the current tree, and the e2e layer now runs against the renamed schema after TASK-0233.03 landed.

Changed (5c57b4b5): the baseline and incremental migrations create events and event_responses with unprefixed constraints and indexes (including event_responses_visitor_event_fk); the three postgres-named migration files were renamed with unchanged goose version prefixes; repository SQL, constraint/index assertions, migration-name constants, and docs/postgres-operations.md, server/docs/postgres-event-api-contract.md, and server/docs/postgres-data-boundaries.md use the new names; the development and isolated test databases were recreated from the edited chain.

Final verification: catalog queries on the development and freshly migrated isolated test databases show zero '^postgres_' relations, constraints, and indexes, with only events and event_responses, events_name_length validated, and all six goose versions applied; the full backend suite passes twice in the isolated Compose stack including timeful/server/postgres and timeful/server/routes; the bundled firefox-desktop project passes 54 tests with 1 skip and only the pre-existing two-worker 30s budget flakes in timed-event-access-transfer-firefox.spec.ts (both pass isolated and sequentially); npm run format:markdown:check passes.

Risks/follow-ups: staging and production recreates remain in TASK-0233.01.01; Go/e2e symbol renames and swagger/frontend regeneration remain in TASK-0233.02 and TASK-0233.04; the two access-transfer approval tests keep their 30s budget edge at two workers.
<!-- SECTION:FINAL_SUMMARY:END -->
