---
id: TASK-0233.04
title: >-
  Regenerate swagger and frontend API types and run the final cleanup
  verification
status: Done
assignee:
  - '@opencode'
created_date: '2026-09-14 19:01'
updated_date: '2026-09-15 09:47'
labels:
  - cleanup
  - postgres
  - frontend
  - swagger
  - verification
dependencies:
  - TASK-0233.02
  - TASK-0233.03
references:
  - server/routes/grants.go
  - server/routes/transfers.go
  - frontend/src/types/api.ts
  - server/docs/swagger.yaml
parent_task_id: TASK-0233
priority: medium
type: chore
ordinal: 236000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Outcome: user-facing and generated text uses the new names, and the cleanup is verified end to end.

Required scope:
- Drop the PostgreSQL-only qualifier from the two swag annotations in server/routes (the grant and transfer descriptions), regenerate server/docs artifacts with swag, then regenerate frontend/src/types/api.ts with npm run gen:api; never hand-edit generated files.
- Update hand-written frontend comments and test names that qualify events or responses as PostgreSQL-specific.
- Run the residual grep gates: no postgres_events or postgres_event_responses outside docs/design/architecture/adr/**, backlog/handoffs/**, backlog/completed/**, and graphify-out/**; no postgres-prefixed Go or e2e file name.
- Record that TASK-0216 is covered by TASK-0233.02 and note coordination with TASK-0228; the backlog.md inbox note about the table names is marked resolved in its own workflow, not from this task.

Dependencies: TASK-0233.02 renames the files that carry the swag annotations and TASK-0233.03 finishes the e2e wording, so take this task last.

This task closes out the parent outcome and is the place to run the final verification pass.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The two swag annotations no longer say PostgreSQL only, and server/docs generated files plus frontend/src/types/api.ts were regenerated through the documented swag and gen:api commands, not hand-edited.
- [x] #2 Hand-written frontend comments and test names no longer qualify events or responses as PostgreSQL-specific.
- [x] #3 The residual grep gates return no stale references outside docs/design/architecture/adr/**, backlog/handoffs/**, backlog/completed/**, and graphify-out/**, and no postgres-prefixed Go or e2e file name remains.
- [x] #4 Frontend required checks pass: lint, fmt:check, typecheck, build, and test:unit.
- [x] #5 Backlog records note TASK-0216 as covered by the Go subtask and TASK-0228 as the owner of the docs/ci.md stale-flag sentence.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Update this task's references to the renamed route files (TASK-0233.02 renamed postgres_grants.go -> grants.go and postgres_transfers.go -> transfers.go).
2. Drop the `PostgreSQL only. ` prefix from the two swag @Description lines in server/routes/grants.go and server/routes/transfers.go.
3. From server/: `go run github.com/swaggo/swag/cmd/swag@v1.16.6 init --parseDependency` to regenerate docs.go, swagger.json, and swagger.yaml; then from frontend/: `npm run gen:api` to regenerate src/types/api.ts. Never hand-edit generated files.
4. Reword the hand-written frontend comments and test titles that qualify events or responses as PostgreSQL-specific: InvitationDialog.vue and its test, EventItem.test.ts, RespondentsList.test.ts, Dashboard.test.ts fixture names, Event.test.ts, Event.vue comments, useEventEditing.test.ts, eventOwnership.test.ts, responseSubmissionBoundary.ts, and useAvailabilityData.ts.
5. Run the residual gates: `rg -n "postgres_events|postgres_event_responses"` over the live tree must only hit the allowed historical paths (docs/design/architecture/adr/**, backlog/handoffs/**, backlog/completed/**, graphify-out/**; the backlog.md inbox note and active rename task records stay with the backlog workflow), and no postgres-prefixed Go or e2e file name remains.
6. Verify: server gofmt/build/vet, the isolated Compose backend suite, frontend lint/fmt:check/typecheck/build/test:unit, and the mapped Firefox desktop e2e project if the code state warrants it.
7. Finalize: check acceptance criteria with evidence, note TASK-0216 as covered by TASK-0233.02 and TASK-0228 as owner of the docs/ci.md stale-flag sentence, record the final summary, and mark Done.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Implementation (2026-09-15)

- References updated to the renamed paths: server/routes/grants.go and server/routes/transfers.go (TASK-0233.02's follow-up note).
- Dropped the `PostgreSQL only. ` prefix from the two swag @Description lines: grants.go:38 and transfers.go:50.
- Regenerated server/docs with `go run github.com/swaggo/swag/cmd/swag@v1.16.6 init --parseDependency` and frontend/src/types/api.ts with `npm run gen:api`. Both diffs are exactly the two description strings (docs.go, swagger.json, swagger.yaml, api.ts); no generated file was hand-edited.
- Reworded the hand-written frontend PostgreSQL qualifiers: InvitationDialog.vue and its test, EventItem.test.ts, RespondentsList.test.ts, Dashboard.test.ts (fixture name `PostgreSQL event` -> `Canonical event` and its expectation), Event.test.ts, Event.vue (two comments), useEventEditing.test.ts (two titles), eventOwnership.test.ts, responseSubmissionBoundary.ts (two comments), and useAvailabilityData.ts. `rg -in postgres frontend` (excluding the generated api.ts) returns nothing, and api.ts itself is clean too.

## Verification evidence (2026-09-15)

- Residual gates: `rg -n "postgres_events|postgres_event_responses"` over the live tree returns no matches in server/**, frontend/**, e2e/**, docs/** (outside the allowed ADR path), scripts/**, or generated artifacts. The only repo matches are the four allowed historical paths plus backlog administrative records: the backlog.md inbox note (explicitly resolved in its own workflow, not here) and the active TASK-0233* rename work-order records. `find server -type f -name '*.go' -iname '*postgres*'` finds only files inside the kept server/postgres package, and `find e2e -iname '*postgres*'` finds nothing; `rg -n postgres_ server --glob '!postgres/**'` returns nothing.
- Server: `gofmt -l` clean for touched files; `go build ./...` and `go vet ./...` pass; isolated Compose `server-route-test` passes every package (routes 2.713s, postgres 6.679s, accounts 0.206s; log /tmp/opencode/task-0233.04-server-route-test.log).
- Frontend: lint (0 errors; two pre-existing NewSignUp.test.ts warnings), fmt:check, typecheck, build, and test:unit (146 files, 1097 tests) pass; root `npm run format:markdown:check` passes; `graphify update .` rebuilt 5718 nodes / 10794 edges.
- E2E: bundled `firefox-desktop` run passes 55, skips 1, and fails only the documented 30s-budget edge `Source approves the exact target code for guest access` (log /tmp/opencode/task-0233.04-firefox-desktop.log). The isolated re-run passes in 15.4s (log /tmp/opencode/task-0233.04-guest-approval-isolated.log); the user confirmed the access-transfer timeouts are the accepted pre-existing flake already recorded in TASK-0233.03. A second full run was started for a clean sweep and stopped by the user on that confirmation.
- Records: TASK-0216 is Done with a comment and final summary recording that TASK-0233.02 satisfied it; TASK-0228 received a coordination comment and remains the owner of the docs/ci.md stale anonymous-event-creation flag sentence, which was untouched here.
- Observation (not a gate failure and not in scope): server/docs/postgres-event-api-contract.md and server/docs/postgres-data-boundaries.md still use PostgreSQL-qualified event naming in their titles, while their object references already use events/event_responses. No follow-up task was created.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
TASK-0233.04 finishes the postgres-qualifier cleanup: it regenerates swagger and frontend API types after dropping the two PostgreSQL-only route descriptions, removes the remaining hand-written frontend qualifiers, and runs the final verification pass for TASK-0233.

Changed:
- server/routes/grants.go and server/routes/transfers.go: the grant-association and access-transfer @Description lines no longer start with "PostgreSQL only.".
- server/docs/docs.go, server/docs/swagger.json, and server/docs/swagger.yaml regenerated with `go run github.com/swaggo/swag/cmd/swag@v1.16.6 init --parseDependency`; frontend/src/types/api.ts regenerated with `npm run gen:api`. Both diffs are exactly the two description strings, with no hand edits.
- Hand-written frontend comments, test titles, and fixtures reworded in InvitationDialog.vue and its test, EventItem.test.ts, RespondentsList.test.ts, Dashboard.test.ts, Event.test.ts, Event.vue, useEventEditing.test.ts, eventOwnership.test.ts, responseSubmissionBoundary.ts, and useAvailabilityData.ts. No frontend file, including the generated api.ts, mentions postgres any more.
- References updated to the renamed server/routes/grants.go and transfers.go paths.

Verification:
- Residual gates: no postgres_events or postgres_event_responses reference remains in the live tree; the only repo matches are the allowed historical paths (ADRs and completed task records) plus backlog administrative records, including the explicitly out-of-scope backlog.md inbox note and the active TASK-0233 rename work orders. No postgres-named Go file exists outside the kept server/postgres package, and no e2e file or SQL references the old names. No `postgres_` substring remains under server/** outside server/postgres.
- Server: gofmt, `go build ./...`, and `go vet ./...` clean; the isolated Compose `server-route-test` suite passes every package.
- Frontend: lint (0 errors), fmt:check, typecheck, build, and test:unit (146 files, 1097 tests) all pass, as does the root `npm run format:markdown:check`.
- E2E: the bundled `firefox-desktop` project passed 55, skipped 1, with only the pre-existing access-transfer 30s-budget timeout, which the user confirmed is acceptable and which passes in isolation in 15.4s (the flake already recorded in TASK-0233.03).
- Records: TASK-0216 is Done and documents that TASK-0233.02 satisfied it; TASK-0228 received a coordination comment and remains the owner of the docs/ci.md stale anonymous-event-creation flag sentence, untouched here.

Risks/follow-ups: none functional; server/docs/postgres-event-api-contract.md and postgres-data-boundaries.md keep PostgreSQL-qualified titles, which is outside this task's gates and recorded in the implementation notes. No commit was made.
<!-- SECTION:FINAL_SUMMARY:END -->
