---
id: TASK-0328.01
title: Close TASK-0328 E2E optimization review gaps
status: Done
assignee:
  - opencode
created_date: '2026-09-24 18:33'
updated_date: '2026-09-24 19:46'
labels:
  - e2e
  - playwright
  - performance
  - follow-up
dependencies: []
references:
  - >-
    backlog/tasks/task-0328 -
    Reduce-browser-E2E-runtime-without-weakening-behavior.md
  - >-
    backlog/completed/task-0191.01 -
    Reduce-recorded-PostgreSQL-access-transfer-Firefox-journey-runtime-at-two-workers.md
  - backlog/tasks/task-0327 - Reduce-isolated-E2E-setup-latency.md
  - e2e/helpers/timed-event-helpers.ts
  - e2e/helpers/actor-context.ts
  - e2e/specs/timed-event-access-transfer-firefox.spec.ts
  - server/routes/anonymous_event_contract_test.go
  - >-
    backlog/tasks/task-0328.02 -
    Stabilize-dev-server-Vite-startup-for-browser-E2E.md
documentation:
  - e2e/AGENTS.md
  - docs/environments.md
modified_files:
  - docs/environments.md
  - e2e/AGENTS.md
  - e2e/helpers/timed-event-helpers.ts
  - e2e/specs/timed-event-access-transfer-firefox.spec.ts
  - e2e/specs/timed-event-helper-contracts-firefox.spec.ts
parent_task_id: TASK-0328
priority: medium
type: enhancement
ordinal: 0
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0328 is marked Done, but review found that its primary access-transfer benchmark does not demonstrate a speedup, delayed consent UI can be missed by the E2E helper, compatibility and failure-path coverage was weakened, and the task record does not clearly distinguish work already delivered by TASK-0191.01 and TASK-0327. This follow-up restores trustworthy performance evidence and coverage without weakening access-transfer behavior. The Vite dev-server startup failure is a separate follow-up and must not be counted as a passing default-mode result here.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A same-scope access-transfer benchmark compares the optimized path with the recorded baseline using identical project selection, worker count, frontend mode, recording, and artifact settings, with repeated runs and an explicit retain/revert decision for any concurrency change that does not improve runtime.
- [x] #2 A regression check demonstrates that a consent action appearing after DOMContentLoaded is handled without an unconditional fixed wait or ambiguous locator selection.
- [x] #3 Dedicated coverage exercises both the event short-ID fast path and the compatibility fallback, while existing authorization, persistence, reload, denial, and actor-isolation assertions remain covered.
- [x] #4 Failure during access-transfer setup cannot leave page or context creation promises unhandled, and target-before-stranger navigation, actor cleanup, and video retention remain verified.
- [x] #5 The task record and E2E documentation distinguish this work from TASK-0191.01 and TASK-0327, include the measured tradeoffs, and link the separately tracked Vite dev-server startup failure.
- [x] #6 Relevant E2E checks and repeat verification pass, with any unrelated pre-existing formatter or test failures explicitly scoped rather than treated as task success.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
- [x] #5 Swagger annotations changed: run `swag init` from `server/` and `npm run gen:api` from `frontend/`
- [x] #6 Code changed: run `codebase-memory-mcp cli index_repository --repo-path .` to refresh the code knowledge graph
- [x] #7 `scripts/` or `prettier/` changed: run root `npm run fmt:check`
- [x] #8 Contract-affecting changes update their documents. This includes `docs/environments.md`; `PLUGIN_API_README.md`; and migration and rollout notes
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
## Implementation plan

1. Add dedicated helper regression coverage before changing behavior: one browser check injects a consent action after DOMContentLoaded and requires exact, state-based handling; mocked request checks prove the returned-short-ID fast path skips `/ids` and the missing-short-ID path uses the compatibility endpoint.
2. Make the consent helper wait on a uniquely resolved visible action rather than a raw fixed delay, and restructure access-transfer page setup so page-creation rejections are observed immediately while target-before-stranger navigation, actor-context cleanup, and video retention remain intact.
3. Revert the access-transfer context/page/API overlap that the repeated same-scope runs did not improve, retain only independently measured parallelism and safe setup/teardown changes, and document the retain/revert decision separately from TASK-0191.01 and TASK-0327.
4. Run the fail-before/pass-after regression checks, repeat the Firefox desktop/two-worker/dev-server access-transfer benchmark with identical artifact and recording settings, run relevant E2E/lint/typecheck/format checks, refresh the code graph, and record objective evidence before finalizing.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Review evidence — 2026-09-24

The parent task's recorded access-transfer baseline was 1.1m and its final dev-server run was 1.2m; two independent review runs also completed in 1.2m each, so the primary optimization has no demonstrated speedup. The access-transfer artifacts are under `/tmp/opencode/timeful-e2e-artifacts/task-0328-baseline/`, `/tmp/opencode/timeful-e2e-artifacts/task-0328-final-access/`, `/tmp/opencode/timeful-e2e-artifacts/review-task-0328-access/`, and `/tmp/opencode/timeful-e2e-artifacts/review-task-0328-access-repeat/`.

The changed Firefox specific-times spec passed all 6 tests at two workers in 34.3s under `/tmp/opencode/timeful-e2e-artifacts/review-task-0328-specific/`; the access-transfer spec passed all 8 tests in 1.2m in both `/tmp/opencode/timeful-e2e-artifacts/review-task-0328-access/` and `/tmp/opencode/timeful-e2e-artifacts/review-task-0328-access-repeat/`.

The default dev-server combined Chromium run completed with 25 passed, 20 skipped, and 1 failed at `e2e/specs/event-page-days-only-layout.spec.ts:291`; the same test failed in a one-worker isolated run. Its trace records Vite module requests failing with `net::ERR_NETWORK_CHANGED`. The bundled control passed all 26 runnable tests in 38.6s. This startup issue is owned by `TASK-0328.02` and must not be counted as a default-mode pass here.

A direct delayed-consent check inserted an `Accept all` button one second after content setup; `dismissConsent` returned before it appeared and the button remained. The current frontend has no production `CookieConsent` mount and `frontend/index.html` has no Quantcast script, so the compatibility path is dead unless a supported external overlay is intentionally retained.

The current server returns `shortId` from event creation at `server/routes/event_routes.go:1284`; the staged fast path therefore bypasses `/api/events/:eventId/ids` for normal E2E seeds. Dedicated coverage must exercise both the returned-short-ID path and the missing-short-ID fallback while retaining the existing authorization, persistence, reload, denial, and actor-isolation assertions.

Access-transfer page creation starts before setup completes at `e2e/specs/timed-event-access-transfer-firefox.spec.ts:115-116` and `:423`, but is awaited much later at `:164` and `:464`; failure-path handling must ensure page and context promises cannot become unhandled while preserving target-before-stranger navigation and recording cleanup.

Repository-wide `e2e npm run fmt:check` still reports only the pre-existing `specs/schedule-overlap-mobile-touch-firefox.spec.ts` and `specs/timed-event-scheduling-response-selection-firefox.spec.ts`; these must remain explicitly scoped rather than treated as this task's success or silently reformatted.

Scope guard: do not change application authorization or consent semantics, timeout budgets, test selection, or isolation semantics; bundled-only evidence must not be presented as default dev-server evidence.

## Regression evidence — 2026-09-24

Before changing the helper, the new delayed-consent test `handles a consent action mounted after DOMContentLoaded` failed in Firefox desktop with `toHaveAttribute("data-consent-clicked", "true")` timing out; the injected `Accept all` action remained unclicked. Artifact: `/tmp/opencode/timeful-e2e-artifacts/task-0328.01-consent-fail-before-final/`. The same test initially passed because its assertion raced the delayed insertion, so the click marker was added before recording the fail-before result.

The first same-scope post-change benchmark (`task-0328.01-optimized-a`) passed all 8 access-transfer tests in 1.3m at Firefox desktop/two workers/dev-server, versus the recorded 1.1m baseline. The unchanged repeat (`task-0328.01-optimized-b`) passed 7/8 but the guest journey exceeded the existing 40s budget at 45.9s and then hit a closed-context request while teardown was underway; artifact: `/tmp/opencode/timeful-e2e-artifacts/task-0328.01-optimized-b/`. This is a failed repeat, not a successful benchmark result; diagnose the exact guest test in isolation before deciding whether it is host/Vite variance or a regression.

Checkpoint recorded: delayed-consent fail-before is captured under `/tmp/opencode/timeful-e2e-artifacts/task-0328.01-consent-fail-before-final/`; the pass-after check passes. The first same-scope benchmark passed 8/8 in 1.3m, while the identical repeat had one guest timeout at 45.9s, so the repeat remains explicitly unverified pending isolated diagnosis.

The failed repeat's exact guest journey passed in isolation at one worker in 17.8s (artifact `/tmp/opencode/timeful-e2e-artifacts/task-0328.01-guest-isolated-diagnosis/`). This supports host/Vite timing variance rather than a deterministic guest-assertion or consent regression; the full benchmark still needs a clean repeated pass.

Recorded verification was split by the failure loop: the four-journey `task-0328.01-video-retention` run passed guest, owner, and reload but hit the existing Manage Access readiness flake in signed-in; signed-in then passed alone with `E2E_VIDEO=on` in `/tmp/opencode/timeful-e2e-artifacts/task-0328.01-video-signed-in-isolated/`. `ffprobe` confirmed finite readable videos for source, target, and stranger actors in the successful guest/owner/signed-in runs, including the early-closed stranger, and source/target for the reload run. The failed subset is not counted as a clean four-test pass.

The clean repeated benchmark `task-0328.01-optimized-c` passed all 8 access-transfer tests in 1.2m with the exact baseline command/settings; together with `task-0328.01-optimized-a` (8/8, 1.3m), this is two successful full runs. The unproven pre-setup context/page/API overlap is reverted; the page/context promises are now awaited only after setup, so a setup failure cannot strand them. The actor teardown concurrency is retained only as cleanup behavior, not claimed as an access-transfer wall-time gain.

The clean recorded repeat `task-0328.01-video-retention-repeat` passed all 4 selected journeys in 54.6s with `E2E_VIDEO=on`; artifacts are under `/tmp/opencode/timeful-e2e-artifacts/task-0328.01-video-retention-repeat/`. `ffprobe` confirmed finite readable source/target/stranger recordings for guest, owner, and signed-in journeys, plus source/target for the reload journey, including the early-closed stranger.

The canonical isolated `server-route-test` command was attempted. Core packages and `timeful/server/routes` passed, but the aggregate `go test ./...` exited nonzero in pre-existing `server/scripts/20260923_gala_translation_probes` fixtures with missing `martianoff/gala` packages and intentionally invalid probe sources; this is explicitly unrelated to TASK-0328.01 and is not counted as task success.

The isolated targeted compatibility check `go test ./routes -count=1` passed in 2.492s via `server-route-test`; the aggregate failure is limited to the unrelated generated `gala` probe packages.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
## Summary

Closed the TASK-0328 review gaps without changing application authorization, consent semantics, timeout budgets, test selection, or isolation.

- Made `dismissConsent` wait for a uniquely resolved visible `Agree`/`Accept all` action, with a dedicated delayed-DOM regression test and fail-before/pass-after evidence.
- Added dedicated helper coverage proving the returned `shortId` fast path skips `/ids` and the missing-`shortId` path uses the compatibility endpoint.
- Reverted TASK-0328's unproven pre-setup access-transfer context/page/API overlap so context and page promises are awaited only after setup; retained the safe actor teardown and dialog-readiness changes, target-before-stranger ordering, authorization, persistence, reload, denial, and actor-isolation assertions.
- Updated `e2e/AGENTS.md` and `docs/environments.md` to distinguish TASK-0191.01, TASK-0327, and TASK-0328.01, document measured tradeoffs, and link TASK-0328.02.

## Performance evidence

- Recorded baseline: 1.1m with Firefox desktop, two workers, default dev-server frontend, default recording policy, and unique artifacts.
- Clean post-change runs: 8/8 in 1.3m (`task-0328.01-optimized-a`) and 8/8 in 1.2m (`task-0328.01-optimized-c`) under identical settings.
- One unchanged repeat had a guest timeout at 45.9s; the isolated guest journey passed in 17.8s, so the failed repeat is recorded as host/Vite variance and not counted as success.
- No access-transfer speedup is claimed; the unproven overlap was reverted.

## Verification

- Helper contract spec: 3/3 passed, including delayed consent and both short-ID paths.
- Access-transfer spec: clean 8/8 benchmark repeat; recorded four-journey subset passed 4/4 in 54.6s.
- Recorded videos: finite readable source, target, and stranger WebM files verified with `ffprobe`, including the early-closed stranger.
- Timed-event specific-times spec: 6/6 passed in 42.3s.
- Chromium days-only layout spec: 9/9 passed in 38.1s.
- E2E lint and typecheck passed; changed-file `oxfmt --check` passed; root Markdown format/check/lint and root `fmt:check` passed; `git diff --check` passed.
- E2E `fmt:check` reports only the two pre-existing files `specs/schedule-overlap-mobile-touch-firefox.spec.ts` and `specs/timed-event-scheduling-response-selection-firefox.spec.ts`; neither was reformatted.
- Targeted isolated `go test ./routes -count=1` passed. The aggregate server test remains nonzero only because unrelated pre-existing generated `gala` probe packages are not buildable; this is explicitly out of scope.
- Codebase-memory index refreshed after code changes.

The default dev-server Vite startup flake remains separately tracked by TASK-0328.02.
<!-- SECTION:FINAL_SUMMARY:END -->
