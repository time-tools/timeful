---
id: TASK-0124.03
title: >-
  Revive disabled NewSignUp mobile row e2e test once the sign-up form is
  reachable in the browser
status: To Do
assignee: []
created_date: '2026-09-01 14:33'
updated_date: '2026-09-13 15:22'
labels:
  - frontend
  - ui
  - e2e
milestone: TASK-0124
dependencies:
  - TASK-0124.02
parent_task_id: TASK-0124
priority: low
type: task
ordinal: 132500
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
During TASK-0124.02 browser verification, the NewEvent create form was fully verified at 360px/375px (one row, visible toggle-picker gap, no truncation, chip=menu width, fluid 100px→120px growth at 350→400px viewports). The NewSignUp form could not be verified the same way.

Blocker observed (2026-09-01, firefox-desktop isolated stack):
- Opening the create dialog from the signed-out landing page (`/`) shows only the event form; there is no "Sign up form" tab button (`tabButtons: []` in a probe), while the name input renders fine.
- NewDialog.vue hides all tabs when `groupsEnabled` is false (`_noTabs` computed), but the store default is `true` and no caller of `setGroupsEnabled(false)` was found before the session stopped. Root cause not diagnosed.

Work to do:
1. Find the real navigation path to NewSignUp (tab may be gated by sign-in, feature flags, or route; check NewDialog usage in views/Home.vue, Landing.vue, Event.vue and how signUpFormEnabled/groupsEnabled are set from backend feature flags).
2. Extend or revive the disabled test in e2e/specs/timed-event-timerange-width-firefox.spec.ts to verify the NewSignUp row at 375px (gap > 0, two chips, no text truncation).
3. Remove the test.fixme wrapper once the test passes.

Related files: frontend/src/components/NewDialog.vue (tab rendering, _noTabs), frontend/src/stores/main.ts (groupsEnabled, signUpFormEnabled), e2e/specs/timed-event-timerange-width-firefox.spec.ts (disabled test).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 A browser e2e spec covers the NewSignUp "What times might work?" one-row layout on mobile (<640px): 12h/24h toggle and TimeRangePicker share one row with a visible gap and no value truncation
- [ ] #2 The spec reaches NewSignUp through a real navigation path (diagnose why the signed-out landing create dialog shows no "Sign up form" tab; find the route/state that renders it, or use an authenticated route if that is the actual entry point)
- [ ] #3 The disabled test.fixme block in e2e/specs/timed-event-timerange-width-firefox.spec.ts is revived (test.fixme removed) and passes on --project=firefox-desktop together with the existing 3 tests in that file
- [ ] #4 npm run lint, typecheck, and build pass with the revived spec
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 All acceptance criteria are satisfied
- [ ] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [ ] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [ ] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
From TASK-0124.01 (2026-09-01): the NewSignUp browser verification of the time-range chip fix is deferred to this task. The fix lives in the shared frontend/src/components/TimeRangePicker.vue, so NewSignUp inherits it verbatim: chip height is knob-controlled via --time-range-chip-height (default 58px, overrides pinned at .v-field level because Vuetify's density rules re-pin vars there), balanced 0/0 field padding centers the selected text, the separator follows the knob, dropdown items stay 40px.

When reviving the fixme spec, also assert on NewSignUp: chip height 58px, selected-time text vertically centered (center offset 0 vs the .v-field box), separator centered. The signed-out create dialog was re-confirmed on 2026-09-01 to show no sign-up tab (a Playwright probe hung waiting for it), consistent with the blocker recorded above.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
author: opencode
created: 2026-09-13 15:22
---
2026-09-13: updated paths for the root e2e package move (TASK-0164); the spec now lives at e2e/specs/timed-event-timerange-width-firefox.spec.ts.
---
<!-- COMMENTS:END -->
