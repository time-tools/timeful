---
id: TASK-0240.03
title: Import latin-only font subsets
status: Done
assignee:
  - opencode
created_date: '2026-09-15 19:43'
updated_date: '2026-09-20 20:15'
labels:
  - frontend
  - bundle-size
dependencies: []
references:
  - frontend/src/main.ts
  - frontend/src/index.css
  - frontend/package.json
parent_task_id: TASK-0240
priority: low
type: chore
ordinal: 239000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## Outcome

Limit the self-hosted font weights and subsets to what the UI actually uses so the production build stops emitting unused woff and woff2 assets.

## Context

- `frontend/src/main.ts` imports `@fontsource/chivo-mono` 300/400/500 and `@fontsource/dm-sans` 400.
- The recorded build emits 23 font files (~270 KB) spanning latin, latin-ext, and vietnamese subsets in both woff and woff2 formats.
- Browsers only fetch subsets matched by `unicode-range`, so the user-facing transfer win is limited; the primary goal is emitted build weight and unused-asset cleanup.
- Chivo Mono must keep applying to mono elements per the TASK-0175 regression context.

## Constraints

- Keep the existing `@fontsource` approach; do not introduce new font tooling or self-hosted binaries.
- Record which subsets and weights remain and why each is needed.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Recorded before/after font file count and total bytes for frontend/dist
- [x] #2 Every imported subset and weight is required by the UI, and app copy keeps rendering in DM Sans and Chivo Mono with no unintended fallback
- [x] #3 Chivo Mono still applies to mono elements; verify in a browser (TASK-0175 regression context)
- [x] #4 Required frontend checks pass from frontend/: npm run lint, npm run fmt:check, npm run typecheck, npm run build, and npm run test:unit
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Record a baseline production build and measure emitted font asset count and total bytes in frontend/dist.
2. Audit which font subsets and weights the UI actually uses: `tw:font-mono` applies Chivo Mono at three call sites (Tooltip mono segments, time-grid gutter labels, collapsed-hour labels), DM Sans 400 is the body default.
3. Switch frontend/src/main.ts imports from the full fontsource CSS entrypoints to the latin-only entrypoints for the weights the audit confirms.
4. Rebuild, measure the after count/bytes, and capture the per-file delta.
5. Run the required frontend checks (lint, fmt:check, typecheck, build, test:unit).
6. Browser-verify that mono elements still resolve to Chivo Mono and body copy to DM Sans, using the existing TASK-0175 regression spec on the isolated E2E stack.
7. Update graphify, record the final summary with before/after evidence, and mark the task Done.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Evidence

### Before/after emitted fonts

| State | Font files in frontend/dist | Font bytes | frontend/dist total |
| ----- | --------------------------- | ---------- | ------------------- |
| Before | 22 | 249,864 (244.0 KiB) | 2,932,585 |
| After | 4 | 60,192 (58.8 KiB) | 2,739,069 |
| Delta | -18 | -189,672 (-75.9%) | -193,516 |

The built stylesheet now contains 2 @font-face rules referencing only `chivo-mono-latin-400` and `dm-sans-latin-400` (ff. `@fontsource` emits both a woff2 and a woff source per face; browsers fetch the woff2).

### Retained subsets and weights

- Chivo Mono latin 400: every `tw:font-mono` call site (Tooltip mono segments, timed-grid gutter labels, collapsed-hour labels) renders at weight 400; no mono element or ancestor selects 300 or 500, so those faces were dropped.
- DM Sans latin 400: the `--v-font-body` and `html` root defaults.
- App copy audit: the only non-ASCII characters in UI copy are U+00B7, U+2019, U+2014, and U+2026, all inside the latin subset unicode range; emoji and U+2500 already fall back to system fonts today.
- Recorded tradeoff: user-generated content in latin-ext or Vietnamese ranges now uses the system fallback instead of a self-hosted subset, which matches the task's latin-only scope.

### Checks

- lint exit 0 (0 errors; 2 pre-existing warnings), fmt:check exit 0, typecheck exit 0, build exit 0, test:unit 154 files / 1221 tests passed.
- `npm run test:e2e -- --project=chromium-desktop specs/schedule-overlap-mono-labels.spec.ts`: 1 passed (40.0s) on the isolated stack.
- The TASK-0175 regression spec was extended to assert that every rendered mono element computes weight 400 and that both retained latin faces load via `document.fonts.check`, so a future missing face fails loudly instead of silently falling back.
- graphify update ran with no topology changes; `npm run format:markdown:check` exit 0.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Switched both self-hosted fonts to latin-only @fontsource entrypoints and dropped the weights the UI never selects.

Changes:
- frontend/src/main.ts: replaced `@fontsource/chivo-mono/300.css`, `400.css`, `500.css` and `@fontsource/dm-sans/400.css` with `@fontsource/chivo-mono/latin-400.css` and `@fontsource/dm-sans/latin-400.css`.
- e2e/specs/schedule-overlap-mono-labels.spec.ts: extended the TASK-0175 regression spec with assertions that every rendered `tw:font-mono` element computes weight 400 and that both retained latin faces load via `document.fonts.check`, so a missing face fails the test instead of silently falling back.

Retained and why:
- Chivo Mono latin 400: the only weight selected by the three `tw:font-mono` call sites (Tooltip mono segments, timed-grid gutter labels, collapsed-hour labels); no mono element or ancestor sets 300/500.
- DM Sans latin 400: the `--v-font-body` and `html` root default. UI copy's non-ASCII characters (U+00B7, U+2019, U+2014, U+2026) all fall inside the latin unicode range.
- Recorded tradeoff: user-generated latin-ext or Vietnamese text now uses the system fallback, consistent with the task's latin-only scope.

Evidence:
- frontend/dist fonts: 22 files / 249,864 bytes before, 4 files / 60,192 bytes after (-18 files, -189,672 bytes, -75.9%); dist total 2,932,585 -> 2,739,069 bytes.
- Required checks: lint, fmt:check, typecheck, build, test:unit (154 files / 1221 tests) all pass.
- Browser: `npm run test:e2e -- --project=chromium-desktop specs/schedule-overlap-mono-labels.spec.ts` passed on the isolated stack (1 passed, 40.0s), with mono elements at weight 400 in Chivo Mono, body copy in DM Sans, and both faces reported loaded.
<!-- SECTION:FINAL_SUMMARY:END -->
