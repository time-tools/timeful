---
id: TASK-0224.01
title: Audit legacy PostgreSQL rows left by the MongoDB migration
status: Done
assignee: []
created_date: '2026-09-13 19:35'
updated_date: '2026-09-14 15:36'
labels:
  - postgres
  - migration
  - audit
dependencies: []
modified_files:
  - server/scripts/
parent_task_id: TASK-0224
priority: high
type: task
ordinal: 224000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Closed by the recreate-from-baseline policy instead of an in-place audit.

The audit target named for this phase, the development database timeful-postgres-development, was recreated from the baseline on 2026-09-14 (goose history starts at 20260912000000; no migration_ledger or migration_quarantine; baseline signature tables present) and holds no rows, so every class is empty by construction. The pre-recreation state was captured before that recreate as a retained backup and a read-only seven-class audit; both are preserved outside the repository at ~/timeful-backups/prebaseline-dev-20260914/ with provenance and checksums.

Outcome recorded in this task: the development database is baseline-created and empty; the retained pre-baseline evidence documents the legacy shapes that existed before the recreate; and the compatibility-retirement decisions for TASK-0224.03 are grounded in the recreate decision plus writer-path analysis rather than live row counts.

The seven-class repeatable script is dropped because no live in-scope database holds legacy rows. Staging and production are handled by TASK-0224.04 (recreate from baseline, verified backups retained); an environment that already turns out to be baseline-created only needs that state recorded.

Clarifications folded in from the 2026-09-13 and 2026-09-14 reviews: calendar_options SQL NULL is the intentional absent state, so the class 2 predicate excludes NULL; the epoch-millis/RFC3339 signup-instant decoding and the group manualAvailability dual decoding (legacy millisecond-keyed versus frontend ZonedDateTime-keyed) are not auditable from stored rows and are retained with that reason.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Development database state recorded: timeful-postgres-development is baseline-created and empty, with the detection basis (migration_ledger and migration_quarantine absent, goose_db_version starts at 20260912000000, baseline signature tables present) and zero rows for all seven classes.
- [x] #2 Retained pre-baseline evidence recorded: the pre-recreation dump and the 2026-09-14 11:07Z read-only audit are stored at ~/timeful-backups/prebaseline-dev-20260914/ with provenance and checksums, and the audit's nonzero historical counts are cited (class 5 = 27 rows; class 6 = 28 events and 27 responses).
- [x] #3 Safe-to-remove and retained statement recorded: TASK-0224.03 must ground removal in the recreate decision plus writer-path analysis (canonical_guest_name and respondent_kind still have current writers), while the signup-instant and group manualAvailability decodings stay retained as not auditable from stored rows and calendar_options SQL NULL is not a violation.
- [x] #4 No data was modified and no files were added under server/scripts/; the seven-class repeatable script is dropped because no live in-scope database holds legacy rows.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Comments

<!-- COMMENTS:BEGIN -->
author: review-task
created: 2026-09-13 19:57
---
Review (2026-09-13) - findings and agreed outcome.

Blocker: class #5 named guest columns on postgres_events; only postgres_event_responses has guest_id, canonical_guest_name, guest_edit_policy, guest_ownership_mode, and guest_edit_token (server/migrations/20260912000000_baseline_schema.sql:89-98).

Agreed decisions:
- Class #6 retired payload keys named explicitly: postgres_events.payload _id, ownerId, signUpResponses, responses; postgres_event_responses.payload userId, user (from the retired 20260912130000_rewrite_legacy_account_payloads.sql, recoverable at commit 2cb92649^). The canonical zero sentinel is handled explicitly because a UUID-shape predicate misses it.
- Pre-baseline databases are detected first and unsupported classes recorded as skipped instead of failing: event_signup_response_blocks does not exist before 20260913000000_signup_response_blocks.sql, which also silently drops dangling block_ids claims during backfill.
- canonical_guest_name counts are added to class #5; epoch-millis/RFC3339 signup-instant decoding is recorded as not auditable from stored rows and retained with that reason.
- The report and raw counts live in this task's notes/comments; no repo report file is added.
- Audit target for this phase is the development database timeful-postgres-development (container up 12 days, so it predates the 2026-09-12 baseline); staging, production, and retained backups are audited only if separately named (docs/environments.md:330-337).

Minor corrections folded into the description: calendar_options is JSONB, so the predicate is jsonb_typeof distinct from object rather than malformed JSON; empty respondent_kind is rejected by the row CHECK, so NULL is the legacy shape; canonical calendar_key is type-dependent because ICS labels are trimmed but not lowercased (server/utils/utils.go:210-218).

Compatibility sites verified: server/postgres/repository.go:274,284; server/routes/auth.go:218-240 and user.go:570-606; server/routes/postgres_event_routes.go:120-140,156-161; server/migrations/20260913000002_postgres_events_name_length.sql:10.

Unresolved: whether any deployed staging or production database and which retained backups are audited later remains deferred to the user.
---

author: review-task
created: 2026-09-14 15:36
---
Review and closure (2026-09-14) - agreed outcome.

Findings:
- Blocker: the live audit target was recreated from the baseline on 2026-09-14 (goose history 20260912000000 through 20260913000002, no migration_ledger) and is empty (0 events, 0 responses, 0 accounts, 0 calendar accounts, 0 signup responses). The 2026-09-13 review premise that it predates the baseline is stale.
- Important: a read-only seven-class audit (2026-09-14T11:07:21Z) and a pre-baseline pg_dump (2026-09-14 14:32 +03) already existed only in /tmp/opencode, unrecorded in this task and not repeatable from the repo.
- Important: migration_ledger alone cannot detect pre-baseline generation; the retained dump lacks it while being pre-baseline (goose versions 20260814170000 and 20260815100000), and migration_ledger came from the retired Mongo backfill tooling (ADR-017), not the goose chain.
- Minor: class 2's jsonb_typeof predicate would count SQL NULL calendar_options, but NULL is the intentional absent state (server/postgres/calendar.go:409, server/migrations/20260912000000_baseline_schema.sql:345).
- Minor: the group manualAvailability dual decoding (server/routes/postgres_group.go:331) is likewise not auditable from stored rows.

Decisions:
- Rewrite this subtask to record the closure outcome, then complete it; the repeatable seven-class script is dropped because no live in-scope database holds legacy rows.
- Preserve the dump and audit artifacts at ~/timeful-backups/prebaseline-dev-20260914/ with provenance and checksums.
- Create TASK-0224.04 to recreate staging and production from the baseline with verified backups retained; TASK-0224.02 and TASK-0224.03 depend on it.
- Class 2 excludes SQL NULL; the group manualAvailability decoding joins the signup-instant decoding on the retained not-auditable list.
- TASK-0224.03 evidence shifts to the recreate decision plus writer-path analysis (canonical_guest_name and respondent_kind still have current writers).
---
<!-- COMMENTS:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Closed as superseded by the recreate-from-baseline policy. The development database timeful-postgres-development was recreated from the baseline on 2026-09-14 and is empty, so all seven legacy-row classes are empty by construction. The pre-recreation dump and the 2026-09-14 11:07Z read-only audit were preserved at ~/timeful-backups/prebaseline-dev-20260914/ with provenance and checksums; the audit's historical nonzero counts (class 5: 27 rows; class 6: 28 events and 27 responses) are referenced for TASK-0224.03. No data was modified and no script was added; staging and production recreation is tracked by TASK-0224.04.
<!-- SECTION:FINAL_SUMMARY:END -->
