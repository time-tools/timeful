KIND=build_fail
ERR=cannot define new methods on non-local type DateTime
NOTE=type X Y stays a Go alias in 0.81.0 and after PR #529, so methods on named scalars remain illegal; models/datetime.go stays handwritten
