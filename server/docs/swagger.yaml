definitions:
  calendar.CalendarEventsWithError:
    properties:
      calendarEvents:
        items:
          $ref: '#/definitions/models.CalendarEvent'
        type: array
      error: {}
    type: object
  commands.Response:
    properties:
      blocks:
        items:
          additionalProperties: {}
          type: object
        type: array
      response_type:
        description: ephemeral or in_channel
        type: string
      text:
        type: string
    type: object
  models.AppleCalendarAuth:
    type: object
  models.BufferTimeOptions:
    properties:
      enabled:
        type: boolean
      time:
        type: integer
    type: object
  models.CalendarAccount:
    properties:
      appleCalendarAuth:
        $ref: '#/definitions/models.AppleCalendarAuth'
      calendarType:
        $ref: '#/definitions/models.CalendarType'
      email:
        description: Email is required for all calendar accounts
        type: string
      enabled:
        type: boolean
      icsCalendarAuth:
        $ref: '#/definitions/models.ICSCalendarAuth'
      oAuth2CalendarAuth:
        $ref: '#/definitions/models.OAuth2CalendarAuth'
      picture:
        type: string
      subCalendars:
        additionalProperties:
          $ref: '#/definitions/models.SubCalendar'
        type: object
    type: object
  models.CalendarEvent:
    properties:
      allDay:
        description: Whether the event is an all day event
        type: boolean
      calendarId:
        type: string
      endDate:
        type: integer
      free:
        description: Whether the user is free during this event
        type: boolean
      id:
        type: string
      startDate:
        type: integer
      summary:
        type: string
    type: object
  models.CalendarOptions:
    properties:
      bufferTime:
        $ref: '#/definitions/models.BufferTimeOptions'
      workingHours:
        $ref: '#/definitions/models.WorkingHoursOptions'
    type: object
  models.CalendarType:
    enum:
    - apple
    - google
    - outlook
    - ics
    type: string
    x-enum-varnames:
    - AppleCalendarType
    - GoogleCalendarType
    - OutlookCalendarType
    - ICSCalendarType
  models.Event:
    properties:
      _id:
        type: string
      activeSlots:
        items:
          type: integer
        type: array
      blindAvailabilityEnabled:
        description: Whether to enable blind availability
        type: boolean
      calendarEventId:
        type: string
      collectEmails:
        type: boolean
      creatorPosthogId:
        description: PostHog ID for the event creator
        type: string
      dates:
        items:
          type: integer
        type: array
      daysOnly:
        description: Whether to only poll for days, not times
        type: boolean
      description:
        type: string
      duration:
        type: number
      eventTimezone:
        type: string
      hasResponded:
        description: Whether the current viewer has responded to the availability
          group
        type: boolean
      hasSpecificTimes:
        description: Used for specific times for specific dates feature
        type: boolean
      isArchived:
        type: boolean
      isDeleted:
        type: boolean
      isSignUpForm:
        description: Sign up form details
        type: boolean
      name:
        type: string
      notificationsEnabled:
        type: boolean
      numResponses:
        description: Used to store the number of responses for the event
        type: integer
      ownerId:
        type: string
      remindees:
        description: Remindees
        items:
          $ref: '#/definitions/models.Remindee'
        type: array
      responses:
        additionalProperties:
          $ref: '#/definitions/models.Response'
        description: Availability responses
        type: object
      scheduledEvent:
        allOf:
        - $ref: '#/definitions/models.CalendarEvent'
        description: Scheduled event
      sendEmailAfterXResponses:
        type: integer
      shortId:
        type: string
      signUpBlocks:
        items:
          $ref: '#/definitions/models.SignUpBlock'
        type: array
      signUpResponses:
        additionalProperties:
          $ref: '#/definitions/models.SignUpResponse'
        type: object
      slotGeneration:
        $ref: '#/definitions/models.SlotGeneration'
      startOnMonday:
        description: Whether to start the event on Monday (as opposed to Sunday, used
          for DOW events)
        type: boolean
      timeIncrement:
        type: integer
      timedRecurrence:
        $ref: '#/definitions/models.TimedRecurrence'
      times:
        items:
          type: integer
        type: array
      type:
        $ref: '#/definitions/models.EventType'
      when2meetHref:
        type: string
    type: object
  models.EventType:
    enum:
    - specific_dates
    - dow
    - group
    type: string
    x-enum-varnames:
    - SPECIFIC_DATES
    - DOW
    - GROUP
  models.ICSCalendarAuth:
    properties:
      label:
        type: string
    type: object
  models.Location:
    properties:
      city:
        type: string
      country_code:
        type: string
      country_name:
        type: string
      latitude:
        type: number
      longitude:
        type: number
      postal:
        type: string
      state:
        type: string
    type: object
  models.OAuth2CalendarAuth:
    type: object
  models.Remindee:
    properties:
      email:
        type: string
      responded:
        type: boolean
    type: object
  models.Response:
    properties:
      availability:
        description: Availability
        items:
          type: integer
        type: array
      calendarOptions:
        $ref: '#/definitions/models.CalendarOptions'
      email:
        type: string
      enabledCalendars:
        additionalProperties:
          items:
            type: string
          type: array
        description: Maps email to an array of sub calendar ids
        type: object
      guestEditPolicy:
        type: string
      guestId:
        type: string
      guestOwnershipMode:
        type: string
      ifNeeded:
        items:
          type: integer
        type: array
      manualAvailability:
        additionalProperties:
          items:
            format: int64
            type: integer
          type: array
        description: Mapping from the start date of a day to the available times for
          that day
        type: object
      name:
        description: Guest information
        type: string
      useCalendarAvailability:
        description: Calendar availability variables for Availability Groups feature
        type: boolean
      user:
        $ref: '#/definitions/models.User'
      userId:
        description: User information
        type: string
    type: object
  models.SignUpBlock:
    properties:
      _id:
        description: |-
          Id is a client-provided opaque block identity. The server owns block
          identities in event_signup_blocks: only a canonical UUID that names an
          existing block on the event keeps that identity, and every other value is
          ignored on write.
        type: string
      capacity:
        type: integer
      endDate:
        type: integer
      name:
        type: string
      startDate:
        type: integer
    type: object
  models.SignUpResponse:
    properties:
      email:
        type: string
      name:
        description: Guest information
        type: string
      signUpBlockIds:
        description: The IDs of the sign up blocks that the user has signed up for
        items:
          type: string
        type: array
      user:
        $ref: '#/definitions/models.User'
      userId:
        description: User information
        type: string
    type: object
  models.SlotGeneration:
    properties:
      endTimeLocal:
        type: string
      startTimeLocal:
        type: string
      timeIncrementMinutes:
        type: integer
    type: object
  models.SubCalendar:
    properties:
      enabled:
        type: boolean
      name:
        type: string
    type: object
  models.TimedRecurrence:
    properties:
      kind:
        type: string
      selectedDays:
        items:
          type: string
        type: array
      selectedDaysOfWeek:
        items:
          type: integer
        type: array
      startOnMonday:
        type: boolean
    type: object
  models.User:
    properties:
      _id:
        description: Profile info
        type: string
      calendarAccounts:
        additionalProperties:
          $ref: '#/definitions/models.CalendarAccount'
        description: |-
          CalendarAccounts is a mapping from {`email_CALENDARTYPE` => CalendarAccount} that contains all the
          additional accounts the user wants to see google calendar events for
        type: object
      calendarOptions:
        allOf:
        - $ref: '#/definitions/models.CalendarOptions'
        description: Calendar options
      email:
        type: string
      firstName:
        type: string
      hasCustomName:
        description: Whether the user has set a custom name for themselves, i.e. don't
          change their name when they sign in
        type: boolean
      lastName:
        type: string
      numEventsCreated:
        type: integer
      picture:
        type: string
      primaryAccountKey:
        description: The calendarAccountKey of the account the user first signed in
          with
        type: string
      timezoneOffset:
        type: integer
    type: object
  models.WorkingHoursOptions:
    properties:
      enabled:
        type: boolean
      endTime:
        type: number
      startTime:
        type: number
    type: object
  responses.Error:
    properties:
      error: {}
    required:
    - error
    type: object
  routes.CreateFolderResponse:
    properties:
      id:
        type: string
    type: object
  routes.FolderResponse:
    properties:
      _id:
        type: string
      color:
        type: string
      eventIds:
        items:
          type: string
        type: array
      isDeleted:
        type: boolean
      name:
        type: string
      userId:
        type: string
    type: object
host: localhost:3002/api
info:
  contact: {}
  description: This is the API for Timeful.
  title: Timeful API
  version: "1.0"
paths:
  /analytics/monthly-active-event-creators:
    get:
      consumes:
      - application/json
      parameters:
      - description: Start date (YYYY-MM-DD) for the range
        in: query
        name: startDate
        required: true
        type: string
      - description: End date (YYYY-MM-DD) for the range
        in: query
        name: endDate
        required: true
        type: string
      - description: Client's timezone offset in minutes from UTC (e.g., -420 for
          UTC-7)
        in: query
        name: timezoneOffset
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              properties:
                count:
                  type: integer
                date:
                  type: string
              type: object
            type: array
        "400":
          description: Invalid date format, range, or timezone offset
          schema:
            properties:
              error:
                type: string
            type: object
        "500":
          description: Internal server error
          schema:
            properties:
              error:
                type: string
            type: object
      summary: Gets the daily count of monthly active event creators over a date range
      tags:
      - analytics
  /analytics/monthly-active-event-creators-with-more-than-x-events:
    get:
      consumes:
      - application/json
      parameters:
      - description: Start date (YYYY-MM-DD) for the range
        in: query
        name: startDate
        required: true
        type: string
      - description: End date (YYYY-MM-DD) for the range
        in: query
        name: endDate
        required: true
        type: string
      - description: Client's timezone offset in minutes from UTC (e.g., -420 for
          UTC-7)
        in: query
        name: timezoneOffset
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              properties:
                count:
                  type: integer
                date:
                  type: string
              type: object
            type: array
        "400":
          description: Invalid date format, range, or timezone offset
          schema:
            properties:
              error:
                type: string
            type: object
        "500":
          description: Internal server error
          schema:
            properties:
              error:
                type: string
            type: object
      summary: Gets the daily count of monthly active event creators over a date range
        with more than x events
      tags:
      - analytics
  /analytics/scanned-poster:
    post:
      consumes:
      - application/json
      parameters:
      - description: Object containing the location that poster was scanned from and
          the url that was scanned
        in: body
        name: payload
        required: true
        schema:
          properties:
            location:
              $ref: '#/definitions/models.Location'
            url:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Notifies us when poster QR code has been scanned
      tags:
      - analytics
  /analytics/user/{email}:
    get:
      consumes:
      - application/json
      parameters:
      - description: User email
        in: path
        name: email
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.User'
      summary: Gets the user by email
      tags:
      - analytics
  /auth/otp/check-email:
    post:
      consumes:
      - application/json
      parameters:
      - description: Email to check
        in: body
        name: payload
        required: true
        schema:
          properties:
            email:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Checks whether a user with the given email exists
      tags:
      - auth
  /auth/otp/send:
    post:
      consumes:
      - application/json
      parameters:
      - description: Email to send OTP to
        in: body
        name: payload
        required: true
        schema:
          properties:
            email:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Sends an OTP code to the given email
      tags:
      - auth
  /auth/otp/verify:
    post:
      consumes:
      - application/json
      parameters:
      - description: Email, OTP code, and timezone offset
        in: body
        name: payload
        required: true
        schema:
          properties:
            code:
              type: string
            email:
              type: string
            timezoneOffset:
              type: integer
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Verifies an OTP code and signs the user in
      tags:
      - auth
  /auth/sign-in:
    post:
      consumes:
      - application/json
      description: Signs user in and sets the access token session variable
      parameters:
      - description: Object containing the Google authorization code, scope, calendar
          type, and the user's timezone offset
        in: body
        name: payload
        required: true
        schema:
          properties:
            calendarType:
              type: string
            code:
              type: string
            scope:
              type: string
            timezoneOffset:
              type: integer
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Signs user in
      tags:
      - auth
  /auth/sign-in-mobile:
    post:
      consumes:
      - application/json
      description: Signs user in and sets the access token session variable
      parameters:
      - description: Object containing the Google authorization code, calendar type,
          and the user's timezone offset
        in: body
        name: payload
        required: true
        schema:
          properties:
            accessToken:
              type: string
            calendarType:
              type: string
            expiresIn:
              type: integer
            idToken:
              type: string
            refreshToken:
              type: string
            scope:
              type: string
            timezoneOffset:
              type: integer
            tokenOrigin:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Signs user in from mobile
      tags:
      - auth
  /auth/sign-out:
    post:
      consumes:
      - application/json
      description: Signs user out and deletes the session
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Signs user out
      tags:
      - auth
  /auth/status:
    get:
      description: Returns a 401 error if user is not signed in, 200 if they are
      responses:
        "200":
          description: OK
        "401":
          description: Error object
          schema:
            $ref: '#/definitions/responses.Error'
      summary: Gets whether the user is signed in or not
      tags:
      - auth
  /auth/visitor-identities:
    post:
      consumes:
      - application/json
      description: Source EVCC proof associates response identity; independent Event
        Owner Edit Token proof associates or moves event ownership without moving
        responses. Granted EVCC association awaits the transfer confirmation flow.
      parameters:
      - description: Browser-local public identities; matching HttpOnly credentials
          are required
        in: body
        name: payload
        required: true
        schema:
          properties:
            identities:
              items:
                properties:
                  eventId:
                    type: string
                  eventVisitorId:
                    type: string
                type: object
              type: array
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "401":
          description: Unauthorized
      summary: Associate browser Event Visitor Identities with the authenticated account
      tags:
      - auth
  /events:
    post:
      consumes:
      - application/json
      parameters:
      - description: Timed events require the complete canonical slot contract; day-only
          events require dates
        in: body
        name: payload
        required: true
        schema:
          properties:
            activeSlots:
              items:
                type: string
              type: array
            attendees:
              items:
                type: string
              type: array
            blindAvailabilityEnabled:
              type: boolean
            dates:
              items:
                type: string
              type: array
            daysOnly:
              type: boolean
            description:
              type: string
            eventTimezone:
              type: string
            isSignUpForm:
              type: boolean
            name:
              type: string
            notificationsEnabled:
              type: boolean
            remindees:
              items:
                type: string
              type: array
            sendEmailAfterXResponses:
              type: integer
            signUpBlocks:
              items:
                $ref: '#/definitions/models.SignUpBlock'
              type: array
            slotGeneration:
              $ref: '#/definitions/models.SlotGeneration'
            timedRecurrence:
              $ref: '#/definitions/models.TimedRecurrence'
            type:
              $ref: '#/definitions/models.EventType'
            when2meetHref:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "201":
          description: Creation returns eventVisitorId and issues separate HttpOnly
            EVCC and Event Owner Edit Token cookies
          schema:
            properties:
              eventId:
                type: string
              eventVisitorId:
                type: string
            type: object
        "400":
          description: Event name must be 100 characters or fewer
          schema:
            $ref: '#/definitions/responses.Error'
      summary: Creates a new event
      tags:
      - events
  /events/{eventId}:
    delete:
      description: Requires the same owner credentials as settings edits; deleted
        events and responses stop resolving.
      parameters:
      - description: Event ID
        in: path
        name: eventId
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "403":
          description: Owner authority required or event archived
          schema:
            $ref: '#/definitions/responses.Error'
        "404":
          description: Event not found
          schema:
            $ref: '#/definitions/responses.Error'
      summary: Deletes an event based on its id
      tags:
      - events
    get:
      parameters:
      - description: Event ID
        in: path
        name: eventId
        required: true
        type: string
      - description: Browser Event Visitor Identity public ID
        in: query
        name: eventVisitorId
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Returns server-proven owner capabilities and browser eventVisitorId;
            response entries add publicId and canEdit.
          schema:
            allOf:
            - $ref: '#/definitions/models.Event'
            - properties:
                canCreateResponse:
                  type: boolean
                canEditSettings:
                  type: boolean
                canManageEvent:
                  type: boolean
                eventVisitorId:
                  type: string
              type: object
      summary: Gets an event based on its id
      tags:
      - events
    put:
      description: Requires Event Owner Edit Token proof, the associated Platform
        Visitor Identity session, or an owner-issued Granted EVCC; base EVCCs never
        authorize settings edits. Archived events are read-only.
      parameters:
      - description: Event ID
        in: path
        name: eventId
        required: true
        type: string
      - description: Timed events require the complete canonical slot contract; day-only
          events require dates
        in: body
        name: payload
        required: true
        schema:
          properties:
            activeSlots:
              items:
                type: string
              type: array
            attendees:
              items:
                type: string
              type: array
            blindAvailabilityEnabled:
              type: boolean
            dates:
              items:
                type: string
              type: array
            daysOnly:
              type: boolean
            description:
              type: string
            eventTimezone:
              type: string
            name:
              type: string
            notificationsEnabled:
              type: boolean
            remindees:
              items:
                type: string
              type: array
            sendEmailAfterXResponses:
              type: integer
            signUpBlocks:
              items:
                $ref: '#/definitions/models.SignUpBlock'
              type: array
            slotGeneration:
              $ref: '#/definitions/models.SlotGeneration'
            timedRecurrence:
              $ref: '#/definitions/models.TimedRecurrence'
            type:
              $ref: '#/definitions/models.EventType'
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: Event name must be 100 characters or fewer
          schema:
            $ref: '#/definitions/responses.Error'
        "403":
          description: Owner authority required or event archived
          schema:
            $ref: '#/definitions/responses.Error'
        "404":
          description: Event not found
          schema:
            $ref: '#/definitions/responses.Error'
      summary: Edits an event based on its id
      tags:
      - events
  /events/{eventId}/archive:
    post:
      consumes:
      - application/json
      description: Requires the same owner credentials as settings edits; archive
        makes the event read-only and unarchive restores mutations.
      parameters:
      - description: Event ID
        in: path
        name: eventId
        required: true
        type: string
      - description: Archive status
        in: body
        name: payload
        required: true
        schema:
          properties:
            archive:
              type: boolean
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "403":
          description: Owner authority required or event archived
          schema:
            $ref: '#/definitions/responses.Error'
        "404":
          description: Event not found
          schema:
            $ref: '#/definitions/responses.Error'
      summary: Archive an event
      tags:
      - events
  /events/{eventId}/calendar-availabilities:
    get:
      consumes:
      - application/json
      parameters:
      - description: Event ID
        in: path
        name: eventId
        required: true
        type: string
      - description: Lower bound for event's start time to filter by
        in: query
        name: timeMin
        required: true
        type: string
      - description: Upper bound for event's end time to filter by
        in: query
        name: timeMax
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties:
              additionalProperties:
                $ref: '#/definitions/calendar.CalendarEventsWithError'
              type: object
            type: object
      summary: Return a map mapping user id to their calendar events that they have
        enabled for the given time range
      tags:
      - events
  /events/{eventId}/decline:
    post:
      consumes:
      - application/json
      parameters:
      - description: Event ID
        in: path
        name: eventId
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Decline the current user's invite to the event
      tags:
      - events
  /events/{eventId}/grant-association:
    post:
      consumes:
      - application/json
      description: An active Granted EVCC and signed-in session are required. Association
        preserves source response ownership and does not associate event ownership.
      parameters:
      - description: Event ID
        in: path
        name: eventId
        required: true
        type: string
      - description: Explicit consent; false only inspects
        in: body
        name: payload
        required: true
        schema:
          properties:
            confirm:
              type: boolean
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            properties:
              confirmationRequired:
                type: boolean
            type: object
        "403":
          description: Forbidden
      summary: Inspect or explicitly confirm granted response identity association
      tags:
      - events
  /events/{eventId}/ids:
    get:
      parameters:
      - description: Event public ID
        in: path
        name: eventId
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            properties:
              longId:
                type: string
              shortId:
                type: string
            type: object
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/responses.Error'
      summary: Resolves an event identifier to its public ID
      tags:
      - events
  /events/{eventId}/rename-user:
    post:
      consumes:
      - application/json
      parameters:
      - description: Event ID
        in: path
        name: eventId
        required: true
        type: string
      - description: Browser Event Visitor Identity public ID
        in: query
        name: eventVisitorId
        type: string
      - description: Object containing info about the guest response to rename; events
          require the opaque responseId instead of oldName
        in: body
        name: payload
        required: true
        schema:
          properties:
            newName:
              type: string
            oldName:
              type: string
            responseId:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: Guest name already exists
          schema:
            $ref: '#/definitions/responses.Error'
      summary: Rename a guest response
      tags:
      - events
  /events/{eventId}/response:
    delete:
      consumes:
      - application/json
      parameters:
      - description: Event ID
        in: path
        name: eventId
        required: true
        type: string
      - description: Browser Event Visitor Identity public ID
        in: query
        name: eventVisitorId
        type: string
      - description: Object containing info about the event response to delete; events
          require the opaque responseId
        in: body
        name: payload
        required: true
        schema:
          properties:
            guest:
              type: boolean
            name:
              type: string
            responseId:
              type: string
            userId:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Delete the current user's availability
      tags:
      - events
    post:
      consumes:
      - application/json
      parameters:
      - description: Event ID
        in: path
        name: eventId
        required: true
        type: string
      - description: Browser Event Visitor Identity public ID
        in: query
        name: eventVisitorId
        type: string
      - description: Object containing info about the event response to update; events
          require responseId or createResponse=true and return responseId with eventVisitorId;
          signup form blocks require explicit-selection authority and validate membership
          under atomic capacity
        in: body
        name: payload
        required: true
        schema:
          properties:
            availability:
              items:
                type: string
              type: array
            calendarOptions:
              $ref: '#/definitions/models.CalendarOptions'
            createResponse:
              type: boolean
            email:
              type: string
            enabledCalendars:
              additionalProperties:
                items:
                  type: string
                type: array
              type: object
            guest:
              type: boolean
            ifNeeded:
              items:
                type: string
              type: array
            manualAvailability:
              additionalProperties:
                items:
                  type: string
                type: array
              type: object
            name:
              type: string
            responseId:
              type: string
            signUpBlockIds:
              items:
                type: string
              type: array
            useCalendarAvailability:
              type: boolean
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: select-response-or-explicitly-create when a mutation omits
            both responseId and createResponse, or signup-block-not-found
          schema:
            $ref: '#/definitions/responses.Error'
        "409":
          description: signup-slot-full when a selected signup block is already at
            capacity
          schema:
            $ref: '#/definitions/responses.Error'
      summary: Updates the current user's availability
      tags:
      - events
  /events/{eventId}/responses:
    get:
      parameters:
      - description: Event ID
        in: path
        name: eventId
        required: true
        type: string
      - description: Browser Event Visitor Identity public ID
        in: query
        name: eventVisitorId
        type: string
      - description: Lower bound for start time to filter availability by
        in: query
        name: timeMin
        required: true
        type: string
      - description: Upper bound for end time to filter availability by
        in: query
        name: timeMax
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Responses are keyed by opaque publicId and each entry adds
            publicId and canEdit
          schema:
            additionalProperties:
              $ref: '#/definitions/models.Response'
            type: object
      summary: Gets responses for an event, filtering availability to be within the
        date ranges
      tags:
      - events
  /events/{eventId}/transfers:
    post:
      description: Requires a signed-in session or base EVCC; anonymous owners additionally
        prove their owner token. The link grants no authority.
      parameters:
      - description: Event ID
        in: path
        name: eventId
        required: true
        type: string
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            properties:
              expiresAt:
                type: string
              id:
                type: string
            type: object
        "403":
          description: Forbidden
      summary: Create a five-minute source-confirmed access transfer
      tags:
      - events
  /events/{eventId}/transfers/{transferId}/{action}:
    post:
      consumes:
      - application/json
      description: 'Actions: open (new target request, or the approved request back
        to its target), status (source lists codes), approve (source supplies requestId
        and exact code), redeem (target proof; replacing a different signed-in account
        requires confirmAccountSwitch), cancel, revoke. Approval is single-use and
        only the selected target can redeem before expiry; cancel works while the
        transfer is pending or approved but unredeemed. Revocation has no time limit.'
      parameters:
      - description: Event ID
        in: path
        name: eventId
        required: true
        type: string
      - description: Transfer ID
        in: path
        name: transferId
        required: true
        type: string
      - description: Transfer action
        in: path
        name: action
        required: true
        type: string
      - description: Approval selection, explicit account-switch consent, or empty
          object
        in: body
        name: payload
        required: true
        schema:
          properties:
            code:
              type: string
            confirmAccountSwitch:
              type: boolean
            requestId:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            properties:
              code:
                type: string
              requestId:
                type: string
              requests:
                items:
                  properties:
                    code:
                      type: string
                    id:
                      type: string
                  type: object
                type: array
              revocable:
                type: boolean
              state:
                type: string
            type: object
        "403":
          description: Forbidden
        "409":
          description: Explicit consent required to replace a different sign-in
          schema:
            properties:
              accountSwitchRequired:
                type: boolean
            type: object
      summary: Advance a source-confirmed transfer
      tags:
      - events
  /slackbot:
    post:
      consumes:
      - application/x-www-form-urlencoded
      produces:
      - application/json
      responses:
        "200":
          description: Text response
          schema:
            $ref: '#/definitions/commands.Response'
      summary: Gets the number of signed up users
      tags:
      - slackbot
  /user:
    delete:
      consumes:
      - application/json
      description: 'Requires the account email address as confirmation. Deletion is
        permanent and immediate: the account profile, platform identity, calendar
        connections, and historical user logs are removed, and events the account
        organized survive with ownership released.'
      parameters:
      - description: The account email address that must match the signed-in account
        in: body
        name: payload
        required: true
        schema:
          properties:
            email:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: The supplied email does not match the account
          schema:
            $ref: '#/definitions/responses.Error'
      summary: Deletes the currently signed in user
      tags:
      - user
  /user/add-apple-calendar-account:
    post:
      consumes:
      - application/json
      parameters:
      - description: Object containing the email and app password of the apple account
        in: body
        name: payload
        required: true
        schema:
          properties:
            email:
              type: string
            password:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Adds an apple calendar account
      tags:
      - user
  /user/add-google-calendar-account:
    post:
      consumes:
      - application/json
      parameters:
      - description: Object containing the Google authorization code and scope
        in: body
        name: payload
        required: true
        schema:
          properties:
            code:
              type: string
            scope:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Adds a new calendar account
      tags:
      - user
  /user/add-ics-calendar-account:
    post:
      consumes:
      - application/json
      parameters:
      - description: Object containing the feed URL and label of the ICS calendar
        in: body
        name: payload
        required: true
        schema:
          properties:
            feedUrl:
              type: string
            label:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Adds an ICS calendar account
      tags:
      - user
  /user/add-outlook-calendar-account:
    post:
      consumes:
      - application/json
      parameters:
      - description: Object containing the Outlook authorization code and scope
        in: body
        name: payload
        required: true
        schema:
          properties:
            code:
              type: string
            scope:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Adds a new outlook calendar account
      tags:
      - user
  /user/calendar-options:
    patch:
      consumes:
      - application/json
      parameters:
      - description: Object containing the updated options
        in: body
        name: payload
        required: true
        schema:
          properties:
            bufferTime:
              $ref: '#/definitions/models.BufferTimeOptions'
            workingHours:
              $ref: '#/definitions/models.WorkingHoursOptions'
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Updates the user's calendar options
      tags:
      - user
  /user/calendars:
    get:
      description: Gets the user's calendar events between "timeMin" and "timeMax"
      parameters:
      - description: Lower bound for event's start time to filter by
        in: query
        name: timeMin
        required: true
        type: string
      - description: Upper bound for event's end time to filter by
        in: query
        name: timeMax
        required: true
        type: string
      - description: Comma separated list of accounts to fetch calendar events from
        in: query
        name: accounts
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties:
              $ref: '#/definitions/calendar.CalendarEventsWithError'
            type: object
      summary: Gets the user's calendar events
      tags:
      - user
  /user/events:
    get:
      description: Returns an array containing all the user's events
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/models.Event'
            type: array
      summary: Gets all the user's events
      tags:
      - user
  /user/events/{eventId}/set-folder:
    post:
      consumes:
      - application/json
      parameters:
      - description: The ID of the event to set the folder for
        in: path
        name: eventId
        required: true
        type: string
      - description: The ID of the folder to set the event to
        in: body
        name: payload
        required: true
        schema:
          properties:
            folderId:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Sets the folder for the specified event
      tags:
      - user
  /user/folders:
    get:
      produces:
      - application/json
      responses:
        "200":
          description: A list of all folders for the user
          schema:
            items:
              $ref: '#/definitions/routes.FolderResponse'
            type: array
        "400":
          description: Invalid user ID
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Failed to get folders
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get all folders
      tags:
      - folders
    post:
      consumes:
      - application/json
      parameters:
      - description: Folder name and optional color
        in: body
        name: payload
        required: true
        schema:
          properties:
            color:
              type: string
            name:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "201":
          description: The ID of the created folder
          schema:
            $ref: '#/definitions/routes.CreateFolderResponse'
        "400":
          description: Invalid user ID or request body
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Failed to create folder
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Create a new folder
      tags:
      - folders
  /user/folders/{folderId}:
    delete:
      parameters:
      - description: Folder ID
        in: path
        name: folderId
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: Invalid user ID or folder ID
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Folder not found
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Failed to delete folder
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Delete a folder
      tags:
      - folders
    get:
      parameters:
      - description: Folder ID
        in: path
        name: folderId
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: The folder object with events
          schema:
            $ref: '#/definitions/routes.FolderResponse'
        "400":
          description: Invalid user ID or folder ID
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Folder not found
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Failed to get events in folder
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get a folder by its ID and its contents
      tags:
      - folders
    patch:
      consumes:
      - application/json
      parameters:
      - description: Folder ID
        in: path
        name: folderId
        required: true
        type: string
      - description: New folder name and/or color
        in: body
        name: payload
        required: true
        schema:
          properties:
            color:
              type: string
            name:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
        "400":
          description: Invalid user ID or folder ID
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Folder not found
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Failed to update folder
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Update a folder's name or color
      tags:
      - folders
  /user/name:
    patch:
      consumes:
      - application/json
      parameters:
      - description: Object containing the updated name
        in: body
        name: payload
        required: true
        schema:
          properties:
            firstName:
              type: string
            lastName:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Updates the user's name
      tags:
      - user
  /user/profile:
    get:
      produces:
      - application/json
      responses:
        "200":
          description: A user profile object
          schema:
            $ref: '#/definitions/models.User'
      summary: Gets the user's profile
      tags:
      - user
  /user/remove-calendar-account:
    delete:
      consumes:
      - application/json
      parameters:
      - description: Object containing the email + type of the calendar account to
          remove
        in: body
        name: payload
        required: true
        schema:
          properties:
            calendarType:
              $ref: '#/definitions/models.CalendarType'
            email:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Removes an existing calendar account
      tags:
      - user
  /user/searchContacts:
    get:
      parameters:
      - description: Query to search for
        in: query
        name: query
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/models.User'
            type: array
      summary: Searches the user's contacts based on the given query
      tags:
      - user
  /user/toggle-calendar:
    post:
      consumes:
      - application/json
      parameters:
      - description: Email of calendar account and whether to enable it
        in: body
        name: payload
        required: true
        schema:
          properties:
            calendarAccountKey:
              type: string
            enabled:
              type: boolean
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Toggles whether the specified calendar is enabled or disabled for the
        user
      tags:
      - user
  /user/toggle-sub-calendar:
    post:
      consumes:
      - application/json
      parameters:
      - description: Email of calendar account, the sub calendar id, and whether to
          enable it
        in: body
        name: payload
        required: true
        schema:
          properties:
            calendarAccountKey:
              type: string
            enabled:
              type: boolean
            subCalendarId:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: Toggles whether the specified sub-calendar is enabled or disabled for
        the user
      tags:
      - user
  /users/{userId}:
    get:
      parameters:
      - description: User ID
        in: path
        name: userId
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.User'
      summary: Returns a minimal public user profile (safe for unauthenticated clients)
      tags:
      - users
swagger: "2.0"
