---
description: Create a git commit following the repository commit conventions.
---

Load the `commit` skill with the skill tool and follow it to create the commit.

When `$ARGUMENTS` is non-empty, treat it as optional user guidance for the commit; the commit skill's message format, staged-changes-only rule, and model-identity rules still apply unchanged.
