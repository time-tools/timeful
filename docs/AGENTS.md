# Markdown Authoring

These instructions apply to Markdown documentation across the repository, including agent instruction files.

- Write each Markdown sentence on one physical source line, and never split a sentence across lines.
- Keep each Markdown table row on one physical source line, even when its cells contain multiple sentences.
- Before authoring or changing prose that refers to controlled terms, follow `terminology/README.md` for canonicalization and linking rules.
