name: E2E CI

on:
  pull_request:
    paths:
      - "frontend/**"
      - "server/**"
      - "e2e/**"
      - "compose*.yaml"
      - ".env.test.example"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/e2e-ci.yml"
  push:
    branches:
      - main
    paths:
      - "frontend/**"
      - "server/**"
      - "e2e/**"
      - "compose*.yaml"
      - ".env.test.example"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/e2e-ci.yml"

concurrency:
  group: e2e-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  browser-e2e:
    name: browser-e2e (${{ matrix.suite }})
    runs-on: ubuntu-latest
    permissions:
      contents: read
    timeout-minutes: 60

    # One job per browser suite, each on its own runner with its own isolated
    # `timeful-test` Compose stack on the fixed isolated ports, so parallel
    # jobs never share ports or a database. Duplicated setup compute is the
    # deliberate trade-off: wall-clock is set by the slowest single suite
    # instead of the sum of all suites.
    strategy:
      # Keep going when one suite fails so every suite still reports its result
      # and uploads its failure artifacts.
      fail-fast: false
      matrix:
        include:
          # Chromium also owns the single-writer cache saves: every job
          # restores the Nix, Go, npm, and image caches, but only this entry
          # saves them, so concurrent same-key saves cannot race.
          - suite: chromium
            projects: >-
              --project=chromium-desktop
              --project=chromium-mobile
              --project=chromium-production-desktop
              --project=chromium-production-mobile
            workers: "2"
            frontend: ""
            save-cache: true
          # All browser suites create events in PostgreSQL by default now, so
          # each suite is its own job on its own isolated stack. Bundled mode
          # keeps the heavier access-transfer journeys within budget at two
          # workers.
          - suite: firefox-desktop
            projects: --project=firefox-desktop
            workers: "2"
            frontend: bundled
            save-cache: false
          # The touch suite matches a single serial spec file that cannot
          # parallelize further.
          - suite: firefox-touch
            projects: --project=firefox-touch
            workers: "1"
            frontend: ""
            save-cache: false

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: nixbuild/nix-quick-install-action@9f63be77f412a248c9d9a65a4c82cf066cdf8f0c # v35
        with:
          nix_conf: |
            keep-env-derivations = true
            keep-outputs = true

      # Every matrix job restores the Nix store; only the designated
      # single-writer job saves it back, so parallel jobs cannot race on the
      # same cache key. `save` and `purge` are both gated on the designated
      # writer because the action purges before it checks the save guard.
      - name: Restore and save Nix store
        uses: nix-community/cache-nix-action@7df957e333c1e5da7721f60227dbba6d06080569 # v7.0.2
        with:
          save: ${{ matrix['save-cache'] }}
          # restore and save a cache using this key
          primary-key: nix-${{ runner.os }}-${{ hashFiles('**/*.nix', '**/flake.lock') }}
          # if there's no cache hit, restore a cache by this prefix
          restore-prefixes-first-match: nix-${{ runner.os }}-
          # collect garbage until the Nix store size (in bytes) is at most this number
          # before trying to save a new cache
          # 1G = 1073741824
          gc-max-store-size-linux: 5G
          # purge only on the designated single-writer job: the purge runs
          # before the save guard, so a restore-only job must not purge caches
          # it is not about to replace
          purge: ${{ matrix['save-cache'] }}
          # purge all versions of the cache
          purge-prefixes: nix-${{ runner.os }}-
          # created more than this number of seconds ago
          purge-created: 0
          # or last accessed this duration (ISO 8601 duration format)
          # before the start of the `Post Restore and save Nix store` phase
          purge-last-accessed: P1DT12H
          # except any version with the key that is the same as the `primary-key`
          purge-primary-key: never

      - name: Configure isolated test environment
        run: cp .env.test.example .env.test

      - name: Validate PostgreSQL-only Compose configuration
        run: docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml config --quiet

      # Hosted runners are ephemeral, so the default docker-driver BuildKit
      # cache never persists across runs. Keep building the server test image
      # with a docker-container builder backed by the GitHub Actions cache,
      # sharing this cache scope with Backend CI so both workflows stay warm.
      # The testbase-pg target adds only the small PostgreSQL-client layer
      # needed by the backup/restore rehearsal (the E2E test container mounts
      # a cached GOMODCACHE volume instead), so the gha cache only carries
      # small blobs; buildx gha re-uploads every layer blob on each run, so
      # large layers would make cache I/O the bottleneck. Only the designated
      # single-writer job exports to the cache; every job still imports from it.
      - name: Set up Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Build cached server test image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: ./server
          target: testbase-pg
          tags: timeful-test-server-route-test
          cache-from: type=gha
          cache-to: ${{ matrix['save-cache'] && 'type=gha,mode=max' || '' }}
          load: true

      # The migrator image is cached as a docker save/load tarball instead of
      # buildx gha cache records: measured runs restore FROM-stage records but
      # never RUN-step results (the goose install), so every run recompiled
      # goose for ~55s. The tarball is keyed on the Dockerfile and migrations;
      # a miss rebuilds via Compose and saves ~70MB, a hit loads in seconds.
      # Every job restores it; only the designated single-writer job saves it.
      - name: Restore migrator image
        id: migrator-image
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .migrator-image.tar
          key: migrator-image-linux-${{ hashFiles('server/Dockerfile', 'server/migrations/**') }}

      - name: Build or load migrator image
        run: |
          if [ -f .migrator-image.tar ]; then
            docker load -i .migrator-image.tar
          else
            docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml build postgres-test-migrate
            docker save timeful-test-postgres-test-migrate -o .migrator-image.tar
          fi

      - name: Save migrator image
        if: matrix['save-cache'] && steps.migrator-image.outputs.cache-primary-key != steps.migrator-image.outputs.cache-matched-key
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .migrator-image.tar
          key: ${{ steps.migrator-image.outputs.cache-primary-key }}

      - name: Restore Go build cache
        id: go-build-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .go-build-cache
          key: go-build-linux-${{ hashFiles('server/go.sum') }}
          restore-keys: go-build-linux-

      # The module cache rides the same restore/save pattern as the Go build
      # cache: exact go.sum key hits skip saving, unlike buildx gha which
      # re-uploads layer blobs on each run. The E2E test container mounts it
      # at /go/pkg/mod (see compose.test.yaml). Both Go caches use the split
      # restore/save actions because the monolithic actions/cache exposes its
      # keys only as runner state, never as step outputs, and the copy-out
      # gating below depends on those outputs.
      - name: Restore Go module cache
        id: go-mod-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .go-mod-cache
          key: go-mod-linux-${{ hashFiles('server/go.sum') }}
          restore-keys: go-mod-linux-

      # The E2E steps' `nix run .#e2e` runs `npm ci` in frontend/ and e2e/,
      # which normally takes ~10s but a slow registry edge was observed taking
      # 5m. Serve tarballs from a cached npm store instead. Every job restores
      # it; only the designated single-writer job saves it back below.
      - name: Restore npm cache
        id: npm-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.npm
          key: npm-${{ runner.os }}-${{ hashFiles('frontend/package-lock.json', 'e2e/package-lock.json') }}
          restore-keys: npm-${{ runner.os }}-

      # Compose never creates external volumes, and the Go caches are external so
      # `down -v` retains them. Fresh CI runners have no volumes, so create them
      # (idempotent) before any service that mounts them starts, mirroring the
      # ensureExternalVolume calls in e2e/isolated-test-stack.ts, then
      # seed them from the restored caches so the E2E server's `go run`
      # compiles incrementally without re-downloading modules.
      - name: Seed external Go cache volumes
        run: |
          docker volume create timeful-test-go-build-cache
          docker volume create timeful-test-go-mod-cache
          if [ -n "$(ls -A .go-build-cache 2>/dev/null)" ]; then
            docker run --rm \
              -v timeful-test-go-build-cache:/tgt \
              -v "$PWD/.go-build-cache:/src:ro" \
              alpine:3.24 sh -c 'cp -a /src/. /tgt/'
          fi
          if [ -n "$(ls -A .go-mod-cache 2>/dev/null)" ]; then
            docker run --rm \
              -v timeful-test-go-mod-cache:/tgt \
              -v "$PWD/.go-mod-cache:/src:ro" \
              alpine:3.24 sh -c 'cp -a /src/. /tgt/'
          fi

      # Pre-start the stack without --build so the one-shot migrate service
      # completes from the cached migrator image. The E2E global setup then
      # boots the stack with `up -d --build ... server-test`, and Compose
      # satisfies its `postgres-test-migrate` completed-successfully
      # dependency from the already-finished container instead of rebuilding
      # the goose image. No `--build` here: the buildable services above
      # already have their Compose image names present in the docker daemon
      # (loaded from the Buildx and tarball steps), so Compose uses them as-is.
      - name: Start PostgreSQL test services
        run: docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml up -d postgres-test postgres-test-bootstrap postgres-test-migrate

      # Playwright writes each run's artifacts (traces, screenshots, videos)
      # under E2E_ARTIFACTS_DIR (see e2e/config/tooling.ts); the upload step
      # below collects them after failures. `--workers` overrides the CI
      # default of one worker per project. `E2E_FRONTEND=bundled` makes the
      # webServer build and preview a test-mode frontend.
      - name: Run ${{ matrix.suite }} browser E2E suite
        env:
          E2E_ARTIFACTS_DIR: ${{ runner.temp }}/e2e-artifacts
          E2E_FRONTEND: ${{ matrix.frontend }}
        run: nix run .#e2e -- ${{ matrix.projects }} --workers=${{ matrix.workers }}

      # Parallel jobs upload under distinct names so the artifacts do not
      # collide.
      - name: Upload Playwright failure artifacts
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-failure-artifacts-${{ matrix.suite }}
          path: ${{ runner.temp }}/e2e-artifacts
          retention-days: 7
          if-no-files-found: ignore

      # Copy the warmed caches back out of the external volumes so the explicit
      # save steps below upload them for the next run. Runs after the E2E
      # steps so the isolated server's `go run` compile work is captured too.
      # Only the designated single-writer job copies out and saves; the other
      # jobs restore and seed their own volumes without writing caches back.
      # Skips exact primary-key hits (nothing new to save) and refuses to copy
      # out an empty volume, so an aborted run can never save an empty entry
      # over a key.
      - name: Copy warmed Go caches out of the external volumes
        id: go-cache-copyout
        if: always() && matrix['save-cache']
        run: |
          mkdir -p .go-build-cache .go-mod-cache
          if [ "${{ steps.go-build-cache.outputs.cache-primary-key }}" != "${{ steps.go-build-cache.outputs.cache-matched-key }}" ] \
            && docker volume inspect timeful-test-go-build-cache > /dev/null 2>&1 \
            && [ -n "$(docker run --rm -v timeful-test-go-build-cache:/src:ro alpine:3.24 sh -c 'ls -A /src' 2>/dev/null)" ]; then
            docker run --rm \
              -v timeful-test-go-build-cache:/src:ro \
              -v "$PWD/.go-build-cache:/out" \
              alpine:3.24 sh -c 'cp -a /src/. /out/'
            echo "build=true" >> "$GITHUB_OUTPUT"
          fi
          if [ "${{ steps.go-mod-cache.outputs.cache-primary-key }}" != "${{ steps.go-mod-cache.outputs.cache-matched-key }}" ] \
            && docker volume inspect timeful-test-go-mod-cache > /dev/null 2>&1 \
            && [ -n "$(docker run --rm -v timeful-test-go-mod-cache:/src:ro alpine:3.24 sh -c 'ls -A /src' 2>/dev/null)" ]; then
            docker run --rm \
              -v timeful-test-go-mod-cache:/src:ro \
              -v "$PWD/.go-mod-cache:/out" \
              alpine:3.24 sh -c 'cp -a /src/. /out/'
            echo "mod=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Save Go build cache
        if: always() && matrix['save-cache'] && steps.go-cache-copyout.outputs.build == 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .go-build-cache
          key: ${{ steps.go-build-cache.outputs.cache-primary-key }}

      - name: Save Go module cache
        if: always() && matrix['save-cache'] && steps.go-cache-copyout.outputs.mod == 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .go-mod-cache
          key: ${{ steps.go-mod-cache.outputs.cache-primary-key }}

      - name: Save npm cache
        if: always() && matrix['save-cache'] && steps.npm-cache.outputs.cache-primary-key != steps.npm-cache.outputs.cache-matched-key
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.npm
          key: ${{ steps.npm-cache.outputs.cache-primary-key }}

      - name: Dump one-shot service logs on failure
        if: failure()
        run: docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml logs postgres-test-bootstrap postgres-test-migrate

      - name: Remove isolated test stack
        if: always()
        run: docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml down -v
